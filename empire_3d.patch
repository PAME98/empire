diff --git a/README.md b/README.md
index f100134..b2c3be0 100644
--- a/README.md
+++ b/README.md
@@ -78,22 +78,31 @@ together.
 
 ## Controls
 
-| Action                         | Input              |
-|---------------------------------|--------------------|
-| Select unit/building/resource    | Left click         |
-| Box select (multiple units)     | Left click + drag  |
-| Move / gather / attack / build  | Right click        |
-| Pan camera                      | WASD / Arrow keys  |
-| Zoom                            | Mouse wheel        |
-| Cancel building placement       | Right click / Esc  |
-| Deselect                        | Esc                |
+The game is **3D** (an angled, Empire-Earth-style top-down camera over a flat
+ground plane; units and buildings are primitive meshes for now).
+
+| Action                          | Input                       |
+|---------------------------------|-----------------------------|
+| Select unit/building/resource   | Left click                  |
+| Box select (multiple units)     | Left click + drag           |
+| Move / gather / attack / build  | Right click                 |
+| Pan camera                      | WASD / Arrows / screen edge |
+| Rotate camera                   | Q / E                       |
+| Zoom                            | Mouse wheel                 |
+| Artillery area strike           | T, then left-click ground   |
+| Cancel placement / targeting    | Right click / Esc           |
+| Deselect                        | Esc                         |
+
+Panning is **screen-relative** — it follows whichever way the camera is
+rotated. Selection and orders use a ray from the cursor into the world (objects
+are picked by their colliders; empty ground is the ray's hit on the y=0 plane).
 
 Box-select grabs **any mix of citizens and soldiers** at once — right-click
 afterward and every selected unit gets a context-appropriate order:
 
 - right-click empty ground → everyone moves there
 - right-click a tree → selected citizens go chop it; soldiers ignore it
-- right-click a farm/lumber camp/quarry → citizens join it as a worker
+- right-click a farm/lumber camp/quarry/mine → citizens join it as a worker
 - right-click a construction site → citizens help build it
 - right-click an enemy → soldiers attack it; citizens ignore it
 
@@ -104,12 +113,18 @@ afterward and every selected unit gets a context-appropriate order:
   Elder**, and can die of **starvation** (not enough food at year-end) or
   **old age**.
 - Adult citizens auto-assign themselves to open jobs at **Farms**, **Lumber
-  Camps**, and **Quarries** if you haven't given them a direct order. Each
-  building has a small number of worker slots and a stockpile that workers
-  draw from and carry back to the **Village Center**.
-- **Trees** are a separate, depletable world resource — citizens chop them
-  directly (no building required) and the tree shrinks and disappears as
-  it's harvested.
+  Camps**, **Quarries**, and **Mines** if you haven't given them a direct
+  order. Each building has a small number of worker slots and a stockpile that
+  workers draw from and carry back to the **Village Center**.
+- **Mountains** are solid terrain holding two independent mineral pools: every
+  mountain has **stone**, and some also have **iron** (shown by a darker vein).
+  You don't mine a mountain by hand — you build a **Quarry** on it for stone
+  and/or a **Mine** on it for iron. Because the pools are separate, one mountain
+  can host both at once, each building draining only its own pool.
+- **Trees** and **water** are depletable resources citizens gather directly
+  (no building): trees give **wood**, river tiles give **water**. Water is
+  spent recruiting citizens; iron is spent training **artillery**.
+- The six tracked resources are **food, wood, stone, iron, water, gold**.
 - A direct player order (right-click) always overrides the autonomous job
   AI. Selecting a citizen and giving it an order is exactly as direct as
   commanding a soldier — that uniformity is the core fix from the old
@@ -132,28 +147,63 @@ afterward and every selected unit gets a context-appropriate order:
 
 ## Project structure
 
+Scripts and scenes are grouped by **domain**, and `scenes/` mirrors
+`scripts/`. The folders also read as the game's layering, roughly in the order
+a frame flows through them:
+
 ```
-scripts/
-  game_manager.gd     Autoload: resources, population, time/seasons, selection state
-  unit.gd              Base class: movement, selection, health, combat
-  citizen.gd           extends Unit — the economy unit (aging, jobs, gather/build/move orders)
-  soldier.gd           extends Unit — the military unit (attack/move orders, auto-aggro)
-  building.gd          Base class: construction progress, health, selection
-  resource_building.gd extends Building — shared worker-slot + stockpile logic
-  farm.gd / lumber_camp.gd / quarry.gd   extend ResourceBuilding (one-liners now — see below)
-  resource_node.gd     Depletable world resource (trees)
-  house.gd             extends Building — adds housing capacity
-  village_center.gd    extends Building — recruits citizens, resource drop-off
-  barracks.gd          extends Building — trains soldiers
-  camera_controller.gd RTS input layer: pan/zoom/box-select/right-click commands/placement
-  ui_controller.gd     Wires GameManager signals to the HUD
-  main.gd              Registers hand-placed starting citizens on boot
+core/       Boot + shared state
+  game_manager.gd   Autoload — the single source of truth: resources,
+                    population, time/seasons, selection, placement. Everything
+                    else talks to the game THROUGH this.
+  map_settings.gd   Autoload — carries the chosen map size from menu to game.
+  main.gd           Registers the hand-placed starting citizens on boot.
+  main_menu.gd      Map-size picker (the project's main scene).
+
+world/      The map and its raw resources
+  map_generator.gd  Builds ground, rivers, mountain ranges and forests on load.
+  mountain.gd       Solid terrain holding separate stone + iron pools; a
+                    quarry and/or mine is built on it to extract them.
+  resource_node.gd  Depletable, hand-gathered world resource (trees, water).
+
+units/      Everything that moves
+  unit.gd           Base class: movement, selection, health, combat.
+  citizen.gd        Economy unit — aging, jobs, gather/build/move orders.
+  soldier.gd        Melee RTS unit (auto-defends when idle).
+  artillery.gd      Siege RTS unit (area strikes; fires only on command).
+
+buildings/  Everything you construct
+  building.gd            Base class: construction progress, health, selection.
+  resource_building.gd   Worker slots + stockpile + mountain-deposit binding.
+  farm/lumber_camp/quarry/mine.gd   Thin subclasses — just tuning numbers.
+  house.gd               Adds housing capacity.
+  village_center.gd      Recruits citizens; resource drop-off point.
+  barracks.gd            Trains soldiers and artillery.
+
+input/      Turning mouse/keys into orders
+  camera_controller.gd   RTS input: pan/zoom/edge-pan, box-select, right-click
+                         commands, building placement.
+
+ui/         Reflecting state back to the player
+  ui_controller.gd       Binds GameManager signals to the HUD.
+
+effects/    Throwaway visuals
+  explosion_effect.gd / attack_area_indicator.gd
 ```
 
-`Farm`, `LumberCamp`, and `Quarry` are now thin subclasses of
-`ResourceBuilding` that just set tuning numbers (worker cap, production
-rate, group name) — the worker-slot/stockpile logic that used to be copied
-three times with slightly different bugs now lives in one place.
+`scenes/` uses the same folders (e.g. `scenes/units/citizen.tscn` pairs with
+`scripts/units/citizen.gd`).
+
+**To understand any feature, start at `GameManager` and follow the signal.**
+Units and buildings never mutate each other's resources or population
+directly — they call `GameManager`, which emits `resources_changed` /
+`selection_changed` / `notification` / etc., and `ui_controller` turns those
+into what you see on screen.
+
+`Farm`, `LumberCamp`, `Quarry`, and `Mine` are thin subclasses of
+`ResourceBuilding` that only set tuning numbers (worker cap, production rate,
+group name, yielded resource) — the shared worker-slot/stockpile/deposit logic
+lives in one place instead of being copied per building.
 
 ## Extending it
 
diff --git a/project.godot b/project.godot
index 70504e2..00e1d28 100644
--- a/project.godot
+++ b/project.godot
@@ -11,13 +11,13 @@ config_version=5
 [application]
 
 config/name="Empire: Settlers at War"
-run/main_scene="res://scenes/main_menu.tscn"
+run/main_scene="res://scenes/core/main_menu.tscn"
 config/features=PackedStringArray("4.6", "Forward Plus")
 config/icon="res://icon.svg"
 
 [autoload]
 
-GameManager="*res://scripts/game_manager.gd"
+GameManager="*res://scripts/core/game_manager.gd"
 MapSettings="*uid://f5osw15nbhyj"
 
 [display]
diff --git a/scenes/artillery.tscn b/scenes/artillery.tscn
deleted file mode 100644
index d7172f3..0000000
--- a/scenes/artillery.tscn
+++ /dev/null
@@ -1,44 +0,0 @@
-[gd_scene load_steps=3 format=3]
-
-[ext_resource type="Script" path="res://scripts/artillery.gd" id="1_artillery"]
-
-[sub_resource type="CircleShape2D" id="CircleShape2D_artillery"]
-radius = 17.0
-
-[node name="Artillery" type="CharacterBody2D" groups=["units", "artillery"]]
-script = ExtResource("1_artillery")
-
-[node name="Sprite2D" type="ColorRect" parent="."]
-offset_left = -15.0
-offset_top = -12.0
-offset_right = 15.0
-offset_bottom = 12.0
-color = Color(0.32, 0.36, 0.22, 1)
-
-[node name="Barrel" type="Node2D" parent="."]
-
-[node name="BarrelRect" type="ColorRect" parent="Barrel"]
-offset_left = 0.0
-offset_top = -3.0
-offset_right = 26.0
-offset_bottom = 3.0
-color = Color(0.16, 0.16, 0.16, 1)
-
-[node name="CollisionShape2D" type="CollisionShape2D" parent="."]
-shape = SubResource("CircleShape2D_artillery")
-
-[node name="SelectionRing" type="ColorRect" parent="."]
-visible = false
-offset_left = -19.0
-offset_top = -19.0
-offset_right = 19.0
-offset_bottom = 19.0
-color = Color(0, 1, 0, 0.55)
-
-[node name="HealthBar" type="ProgressBar" parent="."]
-offset_left = -18.0
-offset_top = -28.0
-offset_right = 18.0
-offset_bottom = -22.0
-show_percentage = false
-value = 100.0
diff --git a/scenes/attack_area_indicator.tscn b/scenes/attack_area_indicator.tscn
deleted file mode 100644
index b15e5b5..0000000
--- a/scenes/attack_area_indicator.tscn
+++ /dev/null
@@ -1,6 +0,0 @@
-[gd_scene load_steps=2 format=3]
-
-[ext_resource type="Script" path="res://scripts/attack_area_indicator.gd" id="1_indicator"]
-
-[node name="AttackAreaIndicator" type="Node2D"]
-script = ExtResource("1_indicator")
diff --git a/scenes/barracks.tscn b/scenes/barracks.tscn
deleted file mode 100644
index abfdc49..0000000
--- a/scenes/barracks.tscn
+++ /dev/null
@@ -1,54 +0,0 @@
-[gd_scene load_steps=3 format=3]
-
-[ext_resource type="Script" path="res://scripts/barracks.gd" id="1_b"]
-
-[sub_resource type="RectangleShape2D" id="RectangleShape2D_b"]
-size = Vector2(74, 74)
-
-[node name="Barracks" type="StaticBody2D" groups=["buildings", "barracks"]]
-script = ExtResource("1_b")
-build_time = 10.0
-max_health = 600
-
-[node name="FinishedSprite" type="ColorRect" parent="."]
-offset_left = -37.0
-offset_top = -37.0
-offset_right = 37.0
-offset_bottom = 37.0
-color = Color(0.45, 0.18, 0.18, 1)
-
-[node name="ConstructionSprite" type="ColorRect" parent="."]
-visible = false
-offset_left = -37.0
-offset_top = -37.0
-offset_right = 37.0
-offset_bottom = 37.0
-color = Color(0.45, 0.18, 0.18, 0.45)
-
-[node name="CollisionShape2D" type="CollisionShape2D" parent="."]
-shape = SubResource("RectangleShape2D_b")
-
-[node name="SelectionRing" type="ColorRect" parent="."]
-visible = false
-offset_left = -42.0
-offset_top = -42.0
-offset_right = 42.0
-offset_bottom = 42.0
-color = Color(0, 1, 0, 0.5)
-
-[node name="HealthBar" type="ProgressBar" parent="."]
-offset_left = -37.0
-offset_top = -50.0
-offset_right = 37.0
-offset_bottom = -44.0
-show_percentage = false
-value = 100.0
-
-[node name="TrainBar" type="ProgressBar" parent="."]
-visible = false
-offset_left = -37.0
-offset_top = -58.0
-offset_right = 37.0
-offset_bottom = -51.0
-value = 0.0
-
diff --git a/scenes/buildings/barracks.tscn b/scenes/buildings/barracks.tscn
new file mode 100644
index 0000000..c6a2d8e
--- /dev/null
+++ b/scenes/buildings/barracks.tscn
@@ -0,0 +1,56 @@
+[gd_scene load_steps=9 format=3]
+
+[ext_resource type="Script" path="res://scripts/buildings/barracks.gd" id="1_s"]
+
+[sub_resource type="StandardMaterial3D" id="FinMat"]
+albedo_color = Color(0.45, 0.18, 0.18, 1.0)
+
+[sub_resource type="StandardMaterial3D" id="ConMat"]
+albedo_color = Color(0.45, 0.18, 0.18, 0.4)
+transparency = 1
+
+[sub_resource type="BoxShape3D" id="Shape"]
+size = Vector3(74, 40, 74)
+
+[sub_resource type="StandardMaterial3D" id="SelectionRing_mat"]
+albedo_color = Color(0, 1, 0, 0.5)
+transparency = 1
+shading_mode = 0
+
+[sub_resource type="BoxMesh" id="FinMesh"]
+size = Vector3(74, 40, 74)
+material = SubResource("FinMat")
+
+[sub_resource type="BoxMesh" id="ConMesh"]
+size = Vector3(74, 40, 74)
+material = SubResource("ConMat")
+
+[sub_resource type="CylinderMesh" id="SelectionRing_mesh"]
+top_radius = 48
+bottom_radius = 48
+height = 0.5
+material = SubResource("SelectionRing_mat")
+
+[node name="Barracks" type="StaticBody3D" groups=["buildings", "barracks"]]
+script = ExtResource("1_s")
+build_time = 10.0
+max_health = 600
+
+[node name="FinishedMesh" type="MeshInstance3D" parent="."]
+transform = Transform3D(1, 0, 0, 0, 1, 0, 0, 0, 1, 0, 20.0, 0)
+mesh = SubResource("FinMesh")
+
+[node name="ConstructionMesh" type="MeshInstance3D" parent="."]
+transform = Transform3D(1, 0, 0, 0, 1, 0, 0, 0, 1, 0, 20.0, 0)
+visible = false
+mesh = SubResource("ConMesh")
+
+[node name="CollisionShape3D" type="CollisionShape3D" parent="."]
+transform = Transform3D(1, 0, 0, 0, 1, 0, 0, 0, 1, 0, 20.0, 0)
+shape = SubResource("Shape")
+
+[node name="SelectionRing" type="MeshInstance3D" parent="."]
+transform = Transform3D(1, 0, 0, 0, 1, 0, 0, 0, 1, 0, 0.3, 0)
+visible = false
+mesh = SubResource("SelectionRing_mesh")
+
diff --git a/scenes/buildings/farm.tscn b/scenes/buildings/farm.tscn
new file mode 100644
index 0000000..4f6e2c7
--- /dev/null
+++ b/scenes/buildings/farm.tscn
@@ -0,0 +1,55 @@
+[gd_scene load_steps=9 format=3]
+
+[ext_resource type="Script" path="res://scripts/buildings/farm.gd" id="1_s"]
+
+[sub_resource type="StandardMaterial3D" id="FinMat"]
+albedo_color = Color(0.85, 0.78, 0.25, 1.0)
+
+[sub_resource type="StandardMaterial3D" id="ConMat"]
+albedo_color = Color(0.85, 0.78, 0.25, 0.4)
+transparency = 1
+
+[sub_resource type="BoxShape3D" id="Shape"]
+size = Vector3(64, 18, 64)
+
+[sub_resource type="StandardMaterial3D" id="SelectionRing_mat"]
+albedo_color = Color(0, 1, 0, 0.5)
+transparency = 1
+shading_mode = 0
+
+[sub_resource type="BoxMesh" id="FinMesh"]
+size = Vector3(64, 18, 64)
+material = SubResource("FinMat")
+
+[sub_resource type="BoxMesh" id="ConMesh"]
+size = Vector3(64, 18, 64)
+material = SubResource("ConMat")
+
+[sub_resource type="CylinderMesh" id="SelectionRing_mesh"]
+top_radius = 40
+bottom_radius = 40
+height = 0.5
+material = SubResource("SelectionRing_mat")
+
+[node name="Farm" type="StaticBody3D" groups=["buildings", "farms", "food_sources"]]
+script = ExtResource("1_s")
+build_time = 6.0
+
+[node name="FinishedMesh" type="MeshInstance3D" parent="."]
+transform = Transform3D(1, 0, 0, 0, 1, 0, 0, 0, 1, 0, 9.0, 0)
+mesh = SubResource("FinMesh")
+
+[node name="ConstructionMesh" type="MeshInstance3D" parent="."]
+transform = Transform3D(1, 0, 0, 0, 1, 0, 0, 0, 1, 0, 9.0, 0)
+visible = false
+mesh = SubResource("ConMesh")
+
+[node name="CollisionShape3D" type="CollisionShape3D" parent="."]
+transform = Transform3D(1, 0, 0, 0, 1, 0, 0, 0, 1, 0, 9.0, 0)
+shape = SubResource("Shape")
+
+[node name="SelectionRing" type="MeshInstance3D" parent="."]
+transform = Transform3D(1, 0, 0, 0, 1, 0, 0, 0, 1, 0, 0.3, 0)
+visible = false
+mesh = SubResource("SelectionRing_mesh")
+
diff --git a/scenes/buildings/house.tscn b/scenes/buildings/house.tscn
new file mode 100644
index 0000000..e0c3b6a
--- /dev/null
+++ b/scenes/buildings/house.tscn
@@ -0,0 +1,55 @@
+[gd_scene load_steps=9 format=3]
+
+[ext_resource type="Script" path="res://scripts/buildings/house.gd" id="1_s"]
+
+[sub_resource type="StandardMaterial3D" id="FinMat"]
+albedo_color = Color(0.8, 0.6, 0.3, 1.0)
+
+[sub_resource type="StandardMaterial3D" id="ConMat"]
+albedo_color = Color(0.8, 0.6, 0.3, 0.4)
+transparency = 1
+
+[sub_resource type="BoxShape3D" id="Shape"]
+size = Vector3(50, 30, 50)
+
+[sub_resource type="StandardMaterial3D" id="SelectionRing_mat"]
+albedo_color = Color(0, 1, 0, 0.5)
+transparency = 1
+shading_mode = 0
+
+[sub_resource type="BoxMesh" id="FinMesh"]
+size = Vector3(50, 30, 50)
+material = SubResource("FinMat")
+
+[sub_resource type="BoxMesh" id="ConMesh"]
+size = Vector3(50, 30, 50)
+material = SubResource("ConMat")
+
+[sub_resource type="CylinderMesh" id="SelectionRing_mesh"]
+top_radius = 34
+bottom_radius = 34
+height = 0.5
+material = SubResource("SelectionRing_mat")
+
+[node name="House" type="StaticBody3D" groups=["buildings", "houses"]]
+script = ExtResource("1_s")
+build_time = 6.0
+
+[node name="FinishedMesh" type="MeshInstance3D" parent="."]
+transform = Transform3D(1, 0, 0, 0, 1, 0, 0, 0, 1, 0, 15.0, 0)
+mesh = SubResource("FinMesh")
+
+[node name="ConstructionMesh" type="MeshInstance3D" parent="."]
+transform = Transform3D(1, 0, 0, 0, 1, 0, 0, 0, 1, 0, 15.0, 0)
+visible = false
+mesh = SubResource("ConMesh")
+
+[node name="CollisionShape3D" type="CollisionShape3D" parent="."]
+transform = Transform3D(1, 0, 0, 0, 1, 0, 0, 0, 1, 0, 15.0, 0)
+shape = SubResource("Shape")
+
+[node name="SelectionRing" type="MeshInstance3D" parent="."]
+transform = Transform3D(1, 0, 0, 0, 1, 0, 0, 0, 1, 0, 0.3, 0)
+visible = false
+mesh = SubResource("SelectionRing_mesh")
+
diff --git a/scenes/buildings/lumber_camp.tscn b/scenes/buildings/lumber_camp.tscn
new file mode 100644
index 0000000..b214dff
--- /dev/null
+++ b/scenes/buildings/lumber_camp.tscn
@@ -0,0 +1,55 @@
+[gd_scene load_steps=9 format=3]
+
+[ext_resource type="Script" path="res://scripts/buildings/lumber_camp.gd" id="1_s"]
+
+[sub_resource type="StandardMaterial3D" id="FinMat"]
+albedo_color = Color(0.5, 0.35, 0.15, 1.0)
+
+[sub_resource type="StandardMaterial3D" id="ConMat"]
+albedo_color = Color(0.5, 0.35, 0.15, 0.4)
+transparency = 1
+
+[sub_resource type="BoxShape3D" id="Shape"]
+size = Vector3(58, 28, 58)
+
+[sub_resource type="StandardMaterial3D" id="SelectionRing_mat"]
+albedo_color = Color(0, 1, 0, 0.5)
+transparency = 1
+shading_mode = 0
+
+[sub_resource type="BoxMesh" id="FinMesh"]
+size = Vector3(58, 28, 58)
+material = SubResource("FinMat")
+
+[sub_resource type="BoxMesh" id="ConMesh"]
+size = Vector3(58, 28, 58)
+material = SubResource("ConMat")
+
+[sub_resource type="CylinderMesh" id="SelectionRing_mesh"]
+top_radius = 38
+bottom_radius = 38
+height = 0.5
+material = SubResource("SelectionRing_mat")
+
+[node name="LumberCamp" type="StaticBody3D" groups=["buildings", "lumber_camps"]]
+script = ExtResource("1_s")
+build_time = 7.0
+
+[node name="FinishedMesh" type="MeshInstance3D" parent="."]
+transform = Transform3D(1, 0, 0, 0, 1, 0, 0, 0, 1, 0, 14.0, 0)
+mesh = SubResource("FinMesh")
+
+[node name="ConstructionMesh" type="MeshInstance3D" parent="."]
+transform = Transform3D(1, 0, 0, 0, 1, 0, 0, 0, 1, 0, 14.0, 0)
+visible = false
+mesh = SubResource("ConMesh")
+
+[node name="CollisionShape3D" type="CollisionShape3D" parent="."]
+transform = Transform3D(1, 0, 0, 0, 1, 0, 0, 0, 1, 0, 14.0, 0)
+shape = SubResource("Shape")
+
+[node name="SelectionRing" type="MeshInstance3D" parent="."]
+transform = Transform3D(1, 0, 0, 0, 1, 0, 0, 0, 1, 0, 0.3, 0)
+visible = false
+mesh = SubResource("SelectionRing_mesh")
+
diff --git a/scenes/buildings/mine.tscn b/scenes/buildings/mine.tscn
new file mode 100644
index 0000000..3d018d5
--- /dev/null
+++ b/scenes/buildings/mine.tscn
@@ -0,0 +1,55 @@
+[gd_scene load_steps=9 format=3]
+
+[ext_resource type="Script" path="res://scripts/buildings/mine.gd" id="1_s"]
+
+[sub_resource type="StandardMaterial3D" id="FinMat"]
+albedo_color = Color(0.45, 0.35, 0.25, 1.0)
+
+[sub_resource type="StandardMaterial3D" id="ConMat"]
+albedo_color = Color(0.45, 0.35, 0.25, 0.4)
+transparency = 1
+
+[sub_resource type="BoxShape3D" id="Shape"]
+size = Vector3(58, 28, 58)
+
+[sub_resource type="StandardMaterial3D" id="SelectionRing_mat"]
+albedo_color = Color(0, 1, 0, 0.5)
+transparency = 1
+shading_mode = 0
+
+[sub_resource type="BoxMesh" id="FinMesh"]
+size = Vector3(58, 28, 58)
+material = SubResource("FinMat")
+
+[sub_resource type="BoxMesh" id="ConMesh"]
+size = Vector3(58, 28, 58)
+material = SubResource("ConMat")
+
+[sub_resource type="CylinderMesh" id="SelectionRing_mesh"]
+top_radius = 38
+bottom_radius = 38
+height = 0.5
+material = SubResource("SelectionRing_mat")
+
+[node name="Mine" type="StaticBody3D" groups=["buildings", "mines"]]
+script = ExtResource("1_s")
+build_time = 8.0
+
+[node name="FinishedMesh" type="MeshInstance3D" parent="."]
+transform = Transform3D(1, 0, 0, 0, 1, 0, 0, 0, 1, 0, 14.0, 0)
+mesh = SubResource("FinMesh")
+
+[node name="ConstructionMesh" type="MeshInstance3D" parent="."]
+transform = Transform3D(1, 0, 0, 0, 1, 0, 0, 0, 1, 0, 14.0, 0)
+visible = false
+mesh = SubResource("ConMesh")
+
+[node name="CollisionShape3D" type="CollisionShape3D" parent="."]
+transform = Transform3D(1, 0, 0, 0, 1, 0, 0, 0, 1, 0, 14.0, 0)
+shape = SubResource("Shape")
+
+[node name="SelectionRing" type="MeshInstance3D" parent="."]
+transform = Transform3D(1, 0, 0, 0, 1, 0, 0, 0, 1, 0, 0.3, 0)
+visible = false
+mesh = SubResource("SelectionRing_mesh")
+
diff --git a/scenes/buildings/quarry.tscn b/scenes/buildings/quarry.tscn
new file mode 100644
index 0000000..8e88ee7
--- /dev/null
+++ b/scenes/buildings/quarry.tscn
@@ -0,0 +1,55 @@
+[gd_scene load_steps=9 format=3]
+
+[ext_resource type="Script" path="res://scripts/buildings/quarry.gd" id="1_s"]
+
+[sub_resource type="StandardMaterial3D" id="FinMat"]
+albedo_color = Color(0.55, 0.55, 0.6, 1.0)
+
+[sub_resource type="StandardMaterial3D" id="ConMat"]
+albedo_color = Color(0.55, 0.55, 0.6, 0.4)
+transparency = 1
+
+[sub_resource type="BoxShape3D" id="Shape"]
+size = Vector3(58, 28, 58)
+
+[sub_resource type="StandardMaterial3D" id="SelectionRing_mat"]
+albedo_color = Color(0, 1, 0, 0.5)
+transparency = 1
+shading_mode = 0
+
+[sub_resource type="BoxMesh" id="FinMesh"]
+size = Vector3(58, 28, 58)
+material = SubResource("FinMat")
+
+[sub_resource type="BoxMesh" id="ConMesh"]
+size = Vector3(58, 28, 58)
+material = SubResource("ConMat")
+
+[sub_resource type="CylinderMesh" id="SelectionRing_mesh"]
+top_radius = 38
+bottom_radius = 38
+height = 0.5
+material = SubResource("SelectionRing_mat")
+
+[node name="Quarry" type="StaticBody3D" groups=["buildings", "quarries"]]
+script = ExtResource("1_s")
+build_time = 7.0
+
+[node name="FinishedMesh" type="MeshInstance3D" parent="."]
+transform = Transform3D(1, 0, 0, 0, 1, 0, 0, 0, 1, 0, 14.0, 0)
+mesh = SubResource("FinMesh")
+
+[node name="ConstructionMesh" type="MeshInstance3D" parent="."]
+transform = Transform3D(1, 0, 0, 0, 1, 0, 0, 0, 1, 0, 14.0, 0)
+visible = false
+mesh = SubResource("ConMesh")
+
+[node name="CollisionShape3D" type="CollisionShape3D" parent="."]
+transform = Transform3D(1, 0, 0, 0, 1, 0, 0, 0, 1, 0, 14.0, 0)
+shape = SubResource("Shape")
+
+[node name="SelectionRing" type="MeshInstance3D" parent="."]
+transform = Transform3D(1, 0, 0, 0, 1, 0, 0, 0, 1, 0, 0.3, 0)
+visible = false
+mesh = SubResource("SelectionRing_mesh")
+
diff --git a/scenes/buildings/village_center.tscn b/scenes/buildings/village_center.tscn
new file mode 100644
index 0000000..a25fc62
--- /dev/null
+++ b/scenes/buildings/village_center.tscn
@@ -0,0 +1,55 @@
+[gd_scene load_steps=9 format=3]
+
+[ext_resource type="Script" path="res://scripts/buildings/village_center.gd" id="1_s"]
+
+[sub_resource type="StandardMaterial3D" id="FinMat"]
+albedo_color = Color(0.6, 0.3, 0.1, 1.0)
+
+[sub_resource type="StandardMaterial3D" id="ConMat"]
+albedo_color = Color(0.6, 0.3, 0.1, 0.4)
+transparency = 1
+
+[sub_resource type="BoxShape3D" id="Shape"]
+size = Vector3(84, 55, 84)
+
+[sub_resource type="StandardMaterial3D" id="SelectionRing_mat"]
+albedo_color = Color(0, 1, 0, 0.5)
+transparency = 1
+shading_mode = 0
+
+[sub_resource type="BoxMesh" id="FinMesh"]
+size = Vector3(84, 55, 84)
+material = SubResource("FinMat")
+
+[sub_resource type="BoxMesh" id="ConMesh"]
+size = Vector3(84, 55, 84)
+material = SubResource("ConMat")
+
+[sub_resource type="CylinderMesh" id="SelectionRing_mesh"]
+top_radius = 54
+bottom_radius = 54
+height = 0.5
+material = SubResource("SelectionRing_mat")
+
+[node name="VillageCenter" type="StaticBody3D" groups=["village_centers", "buildings"]]
+script = ExtResource("1_s")
+max_health = 800
+
+[node name="FinishedMesh" type="MeshInstance3D" parent="."]
+transform = Transform3D(1, 0, 0, 0, 1, 0, 0, 0, 1, 0, 27.5, 0)
+mesh = SubResource("FinMesh")
+
+[node name="ConstructionMesh" type="MeshInstance3D" parent="."]
+transform = Transform3D(1, 0, 0, 0, 1, 0, 0, 0, 1, 0, 27.5, 0)
+visible = false
+mesh = SubResource("ConMesh")
+
+[node name="CollisionShape3D" type="CollisionShape3D" parent="."]
+transform = Transform3D(1, 0, 0, 0, 1, 0, 0, 0, 1, 0, 27.5, 0)
+shape = SubResource("Shape")
+
+[node name="SelectionRing" type="MeshInstance3D" parent="."]
+transform = Transform3D(1, 0, 0, 0, 1, 0, 0, 0, 1, 0, 0.3, 0)
+visible = false
+mesh = SubResource("SelectionRing_mesh")
+
diff --git a/scenes/citizen.tscn b/scenes/citizen.tscn
deleted file mode 100644
index 0114518..0000000
--- a/scenes/citizen.tscn
+++ /dev/null
@@ -1,51 +0,0 @@
-[gd_scene load_steps=3 format=3]
-
-[ext_resource type="Script" path="res://scripts/citizen.gd" id="1_citizen"]
-
-[sub_resource type="CircleShape2D" id="CircleShape2D_citizen"]
-radius = 12.0
-
-[node name="Citizen" type="CharacterBody2D" groups=["units", "citizens"]]
-script = ExtResource("1_citizen")
-
-[node name="Sprite2D" type="ColorRect" parent="."]
-offset_left = -10.0
-offset_top = -10.0
-offset_right = 10.0
-offset_bottom = 10.0
-color = Color(0.22, 0.52, 0.88, 1)
-
-[node name="JobIcon" type="ColorRect" parent="."]
-offset_left = -4.0
-offset_top = -17.0
-offset_right = 4.0
-offset_bottom = -11.0
-color = Color(1, 1, 1, 0)
-
-[node name="CarryIcon" type="ColorRect" parent="."]
-visible = false
-offset_left = -5.0
-offset_top = 11.0
-offset_right = 5.0
-offset_bottom = 18.0
-color = Color(0.9, 0.75, 0.2, 1)
-
-[node name="CollisionShape2D" type="CollisionShape2D" parent="."]
-shape = SubResource("CircleShape2D_citizen")
-
-[node name="SelectionRing" type="ColorRect" parent="."]
-visible = false
-offset_left = -14.0
-offset_top = -14.0
-offset_right = 14.0
-offset_bottom = 14.0
-color = Color(0, 1, 0, 0.55)
-
-[node name="HealthBar" type="ProgressBar" parent="."]
-offset_left = -15.0
-offset_top = -24.0
-offset_right = 15.0
-offset_bottom = -18.0
-show_percentage = false
-value = 100.0
-
diff --git a/scenes/main.tscn b/scenes/core/main.tscn
similarity index 54%
rename from scenes/main.tscn
rename to scenes/core/main.tscn
index 617684e..203b9f2 100644
--- a/scenes/main.tscn
+++ b/scenes/core/main.tscn
@@ -1,91 +1,116 @@
-[gd_scene format=3 uid="uid://bruaxsgaowtlv"]
-
-[ext_resource type="Script" uid="uid://bm6hswb5o3att" path="res://scripts/main.gd" id="1_main"]
-[ext_resource type="Script" uid="uid://br1f8mq7tjqk3" path="res://scripts/camera_controller.gd" id="2_cam"]
-[ext_resource type="PackedScene" path="res://scenes/attack_area_indicator.tscn" id="3_tipki"]
-[ext_resource type="PackedScene" path="res://scenes/village_center.tscn" id="3_vc"]
-[ext_resource type="PackedScene" path="res://scenes/house.tscn" id="4_house"]
-[ext_resource type="PackedScene" path="res://scenes/tree.tscn" id="5_tree"]
-[ext_resource type="Script" uid="uid://ryj3wh71g50v" path="res://scripts/ui_controller.gd" id="6_ui"]
-[ext_resource type="PackedScene" path="res://scenes/citizen.tscn" id="7_citizen"]
-[ext_resource type="Script" uid="uid://b23jp0da2u5ms" path="res://scripts/map_generator.gd" id="9_85g3d"]
-
-[node name="Main" type="Node2D" unique_id=1200903233]
+[gd_scene load_steps=10 format=3]
+
+[ext_resource type="Script" path="res://scripts/core/main.gd" id="1_main"]
+[ext_resource type="Script" path="res://scripts/input/camera_controller.gd" id="2_cam"]
+[ext_resource type="PackedScene" path="res://scenes/effects/attack_area_indicator.tscn" id="3_tipki"]
+[ext_resource type="PackedScene" path="res://scenes/buildings/village_center.tscn" id="3_vc"]
+[ext_resource type="PackedScene" path="res://scenes/buildings/house.tscn" id="4_house"]
+[ext_resource type="PackedScene" path="res://scenes/world/tree.tscn" id="5_tree"]
+[ext_resource type="Script" path="res://scripts/ui/ui_controller.gd" id="6_ui"]
+[ext_resource type="PackedScene" path="res://scenes/units/citizen.tscn" id="7_citizen"]
+[ext_resource type="Script" path="res://scripts/world/map_generator.gd" id="9_85g3d"]
+
+[sub_resource type="Environment" id="Env"]
+background_mode = 1
+background_color = Color(0.53, 0.62, 0.74, 1)
+ambient_light_source = 2
+ambient_light_color = Color(1, 1, 1, 1)
+ambient_light_energy = 0.55
+
+[sub_resource type="StandardMaterial3D" id="GroundMat"]
+albedo_color = Color(0.26, 0.5, 0.2, 1)
+
+[sub_resource type="StandardMaterial3D" id="GhostMat"]
+transparency = 1
+albedo_color = Color(0.9, 0.8, 0.3, 0.5)
+
+[sub_resource type="PlaneMesh" id="GroundMesh"]
+size = Vector2(1280, 720)
+material = SubResource("GroundMat")
+
+[sub_resource type="BoxMesh" id="GhostMesh"]
+size = Vector3(50, 30, 50)
+material = SubResource("GhostMat")
+
+[node name="Main" type="Node3D"]
 script = ExtResource("1_main")
 
-[node name="Camera" type="Camera2D" parent="." unique_id=8333148]
-position = Vector2(640, 360)
+[node name="WorldEnvironment" type="WorldEnvironment" parent="."]
+environment = SubResource("Env")
+
+[node name="Sun" type="DirectionalLight3D" parent="."]
+transform = Transform3D(1, 0, 0, 0, 0.5736, 0.8192, 0, -0.8192, 0.5736, 0, 600, 0)
+
+[node name="Camera" type="Node3D" parent="."]
+transform = Transform3D(1, 0, 0, 0, 1, 0, 0, 0, 1, 640, 0, 360)
 script = ExtResource("2_cam")
 
-[node name="SelectionBox" type="ColorRect" parent="Camera" unique_id=1575047284]
-visible = false
-offset_right = 40.0
-offset_bottom = 40.0
-color = Color(0, 0.588235, 1, 0.28)
+[node name="Camera3D" type="Camera3D" parent="Camera"]
+transform = Transform3D(1, 0, 0, 0, 0.5736, 0.8192, 0, -0.8192, 0.5736, 0, 817, 573)
+far = 12000.0
 
-[node name="PlacementGhost" type="ColorRect" parent="Camera" unique_id=709272229]
+[node name="PlacementGhost" type="MeshInstance3D" parent="."]
 visible = false
-offset_right = 60.0
-offset_bottom = 60.0
-color = Color(0.9, 0.8, 0.3, 0.5)
+mesh = SubResource("GhostMesh")
 
-[node name="AttackAreaGhost" parent="Camera" unique_id=1787893553 instance=ExtResource("3_tipki")]
+[node name="AttackAreaGhost" parent="." instance=ExtResource("3_tipki")]
+visible = false
 
-[node name="Ground" type="ColorRect" parent="." unique_id=1863042247]
-offset_right = 3200.0
-offset_bottom = 3200.0
-color = Color(0.26, 0.5, 0.2, 1)
+[node name="Ground" type="MeshInstance3D" parent="."]
+transform = Transform3D(1, 0, 0, 0, 1, 0, 0, 0, 1, 640, 0, 360)
+mesh = SubResource("GroundMesh")
 
-[node name="Buildings" type="Node2D" parent="." unique_id=1967958134]
+[node name="Buildings" type="Node3D" parent="."]
 
-[node name="VillageCenter" parent="Buildings" unique_id=561050386 instance=ExtResource("3_vc")]
-position = Vector2(640, 360)
+[node name="VillageCenter" parent="Buildings" instance=ExtResource("3_vc")]
+transform = Transform3D(1, 0, 0, 0, 1, 0, 0, 0, 1, 640, 0, 360)
 
-[node name="House" parent="Buildings" unique_id=163827978 instance=ExtResource("4_house")]
-position = Vector2(520, 460)
+[node name="House" parent="Buildings" instance=ExtResource("4_house")]
+transform = Transform3D(1, 0, 0, 0, 1, 0, 0, 0, 1, 520, 0, 460)
+starts_built = true
 
-[node name="Resources" type="Node2D" parent="." unique_id=1091846300]
+[node name="Resources" type="Node3D" parent="."]
 
-[node name="Tree1" parent="Resources" unique_id=641553773 instance=ExtResource("5_tree")]
-position = Vector2(220, 220)
+[node name="Tree1" parent="Resources" instance=ExtResource("5_tree")]
+transform = Transform3D(1, 0, 0, 0, 1, 0, 0, 0, 1, 220, 0, 220)
 
-[node name="Tree2" parent="Resources" unique_id=1041564659 instance=ExtResource("5_tree")]
-position = Vector2(380, 160)
+[node name="Tree2" parent="Resources" instance=ExtResource("5_tree")]
+transform = Transform3D(1, 0, 0, 0, 1, 0, 0, 0, 1, 380, 0, 160)
 
-[node name="Tree3" parent="Resources" unique_id=1654984344 instance=ExtResource("5_tree")]
-position = Vector2(160, 440)
+[node name="Tree3" parent="Resources" instance=ExtResource("5_tree")]
+transform = Transform3D(1, 0, 0, 0, 1, 0, 0, 0, 1, 160, 0, 440)
 
-[node name="Tree4" parent="Resources" unique_id=1289703804 instance=ExtResource("5_tree")]
-position = Vector2(980, 260)
+[node name="Tree4" parent="Resources" instance=ExtResource("5_tree")]
+transform = Transform3D(1, 0, 0, 0, 1, 0, 0, 0, 1, 980, 0, 260)
 
-[node name="Tree5" parent="Resources" unique_id=1674332243 instance=ExtResource("5_tree")]
-position = Vector2(880, 540)
+[node name="Tree5" parent="Resources" instance=ExtResource("5_tree")]
+transform = Transform3D(1, 0, 0, 0, 1, 0, 0, 0, 1, 880, 0, 540)
 
-[node name="Tree6" parent="Resources" unique_id=2063354302 instance=ExtResource("5_tree")]
-position = Vector2(1180, 420)
+[node name="Tree6" parent="Resources" instance=ExtResource("5_tree")]
+transform = Transform3D(1, 0, 0, 0, 1, 0, 0, 0, 1, 1180, 0, 420)
 
-[node name="Tree7" parent="Resources" unique_id=1109364152 instance=ExtResource("5_tree")]
-position = Vector2(420, 640)
+[node name="Tree7" parent="Resources" instance=ExtResource("5_tree")]
+transform = Transform3D(1, 0, 0, 0, 1, 0, 0, 0, 1, 420, 0, 640)
 
-[node name="Tree8" parent="Resources" unique_id=786690682 instance=ExtResource("5_tree")]
-position = Vector2(760, 160)
+[node name="Tree8" parent="Resources" instance=ExtResource("5_tree")]
+transform = Transform3D(1, 0, 0, 0, 1, 0, 0, 0, 1, 760, 0, 160)
 
-[node name="Units" type="Node2D" parent="." unique_id=46621321]
+[node name="Units" type="Node3D" parent="."]
 
-[node name="Citizen1" parent="Units" unique_id=1218477273 instance=ExtResource("7_citizen")]
-position = Vector2(590, 420)
+[node name="Citizen1" parent="Units" instance=ExtResource("7_citizen")]
+transform = Transform3D(1, 0, 0, 0, 1, 0, 0, 0, 1, 590, 0, 420)
 
-[node name="Citizen2" parent="Units" unique_id=723182950 instance=ExtResource("7_citizen")]
-position = Vector2(680, 420)
+[node name="Citizen2" parent="Units" instance=ExtResource("7_citizen")]
+transform = Transform3D(1, 0, 0, 0, 1, 0, 0, 0, 1, 680, 0, 420)
 
-[node name="Citizen3" parent="Units" unique_id=1910704256 instance=ExtResource("7_citizen")]
-position = Vector2(640, 460)
+[node name="Citizen3" parent="Units" instance=ExtResource("7_citizen")]
+transform = Transform3D(1, 0, 0, 0, 1, 0, 0, 0, 1, 640, 0, 460)
 
-[node name="Citizen4" parent="Units" unique_id=1415153736 instance=ExtResource("7_citizen")]
-position = Vector2(560, 320)
+[node name="Citizen4" parent="Units" instance=ExtResource("7_citizen")]
+transform = Transform3D(1, 0, 0, 0, 1, 0, 0, 0, 1, 560, 0, 320)
 
-[node name="Citizen5" parent="Units" unique_id=2021494376 instance=ExtResource("7_citizen")]
-position = Vector2(720, 320)
+[node name="Citizen5" parent="Units" instance=ExtResource("7_citizen")]
+transform = Transform3D(1, 0, 0, 0, 1, 0, 0, 0, 1, 720, 0, 320)
 
 [node name="UI" type="CanvasLayer" parent="." unique_id=1259264752]
 script = ExtResource("6_ui")
@@ -93,7 +118,7 @@ script = ExtResource("6_ui")
 [node name="TopBar" type="Panel" parent="UI" unique_id=1272342631]
 offset_left = 16.0
 offset_top = 16.0
-offset_right = 560.0
+offset_right = 812.0
 offset_bottom = 56.0
 
 [node name="Resources" type="HBoxContainer" parent="UI/TopBar" unique_id=1667056422]
@@ -124,6 +149,16 @@ layout_mode = 2
 theme_override_font_sizes/font_size = 16
 text = "Stone: 100"
 
+[node name="IronLabel" type="Label" parent="UI/TopBar/Resources"]
+layout_mode = 2
+theme_override_font_sizes/font_size = 16
+text = "Iron: 0"
+
+[node name="WaterLabel" type="Label" parent="UI/TopBar/Resources"]
+layout_mode = 2
+theme_override_font_sizes/font_size = 16
+text = "Water: 60"
+
 [node name="GoldLabel" type="Label" parent="UI/TopBar/Resources" unique_id=497072382]
 layout_mode = 2
 theme_override_font_sizes/font_size = 16
@@ -272,9 +307,9 @@ anchor_left = 0.5
 anchor_top = 1.0
 anchor_right = 0.5
 anchor_bottom = 1.0
-offset_left = -360.0
+offset_left = -420.0
 offset_top = -76.0
-offset_right = 360.0
+offset_right = 420.0
 offset_bottom = -16.0
 grow_horizontal = 2
 grow_vertical = 0
@@ -283,43 +318,52 @@ grow_vertical = 0
 layout_mode = 0
 offset_left = 10.0
 offset_top = 8.0
-offset_right = 140.0
+offset_right = 138.0
 offset_bottom = 52.0
 text = "House
 40W 10S"
 
 [node name="FarmButton" type="Button" parent="UI/BuildMenu" unique_id=908458807]
 layout_mode = 0
-offset_left = 148.0
+offset_left = 146.0
 offset_top = 8.0
-offset_right = 278.0
+offset_right = 274.0
 offset_bottom = 52.0
 text = "Farm
 50W"
 
 [node name="LumberButton" type="Button" parent="UI/BuildMenu" unique_id=2009600931]
 layout_mode = 0
-offset_left = 286.0
+offset_left = 282.0
 offset_top = 8.0
-offset_right = 416.0
+offset_right = 410.0
 offset_bottom = 52.0
 text = "Lumber Camp
 30W 10S"
 
 [node name="QuarryButton" type="Button" parent="UI/BuildMenu" unique_id=2132193470]
 layout_mode = 0
-offset_left = 424.0
+offset_left = 418.0
 offset_top = 8.0
-offset_right = 554.0
+offset_right = 546.0
 offset_bottom = 52.0
 text = "Quarry
 40W"
 
+[node name="MineButton" type="Button" parent="UI/BuildMenu"]
+layout_mode = 0
+offset_left = 554.0
+offset_top = 8.0
+offset_right = 682.0
+offset_bottom = 52.0
+text = "Mine
+40W 20S"
+
 [node name="BarracksButton" type="Button" parent="UI/BuildMenu" unique_id=1825593179]
 layout_mode = 0
-offset_left = 562.0
+offset_left = 690.0
 offset_top = 8.0
-offset_right = 692.0
+offset_right = 818.0
 offset_bottom = 52.0
 text = "Barracks
 80W 40S"
@@ -339,5 +383,11 @@ theme_override_colors/font_color = Color(1, 0.85, 0.3, 1)
 theme_override_font_sizes/font_size = 13
 horizontal_alignment = 1
 
-[node name="MapGenerator" type="Node2D" parent="." unique_id=1797434724]
+[node name="SelectionBox" type="ColorRect" parent="UI"]
+visible = false
+offset_right = 40.0
+offset_bottom = 40.0
+color = Color(0.2, 0.55, 1, 0.25)
+
+[node name="MapGenerator" type="Node3D" parent="."]
 script = ExtResource("9_85g3d")
diff --git a/scenes/main_menu.tscn b/scenes/core/main_menu.tscn
similarity index 97%
rename from scenes/main_menu.tscn
rename to scenes/core/main_menu.tscn
index 5ca1d24..8c43426 100644
--- a/scenes/main_menu.tscn
+++ b/scenes/core/main_menu.tscn
@@ -1,6 +1,6 @@
 [gd_scene format=3 uid="uid://dgh7n4rlfal57"]
 
-[ext_resource type="Script" uid="uid://bqumej66ntsqm" path="res://scripts/main_menu.gd" id="1_menu"]
+[ext_resource type="Script" uid="uid://bqumej66ntsqm" path="res://scripts/core/main_menu.gd" id="1_menu"]
 
 [node name="MainMenu" type="Control" unique_id=490282865]
 layout_mode = 3
diff --git a/scenes/effects/attack_area_indicator.tscn b/scenes/effects/attack_area_indicator.tscn
new file mode 100644
index 0000000..397afa8
--- /dev/null
+++ b/scenes/effects/attack_area_indicator.tscn
@@ -0,0 +1,7 @@
+[gd_scene load_steps=2 format=3]
+
+[ext_resource type="Script" path="res://scripts/effects/attack_area_indicator.gd" id="1_s"]
+
+[node name="AttackAreaIndicator" type="Node3D"]
+script = ExtResource("1_s")
+
diff --git a/scenes/effects/explosion_effect.tscn b/scenes/effects/explosion_effect.tscn
new file mode 100644
index 0000000..87de66c
--- /dev/null
+++ b/scenes/effects/explosion_effect.tscn
@@ -0,0 +1,7 @@
+[gd_scene load_steps=2 format=3]
+
+[ext_resource type="Script" path="res://scripts/effects/explosion_effect.gd" id="1_s"]
+
+[node name="ExplosionEffect" type="Node3D"]
+script = ExtResource("1_s")
+
diff --git a/scenes/explosion_effect.tscn b/scenes/explosion_effect.tscn
deleted file mode 100644
index be55227..0000000
--- a/scenes/explosion_effect.tscn
+++ /dev/null
@@ -1,6 +0,0 @@
-[gd_scene load_steps=2 format=3]
-
-[ext_resource type="Script" path="res://scripts/explosion_effect.gd" id="1_explosion"]
-
-[node name="ExplosionEffect" type="Node2D"]
-script = ExtResource("1_explosion")
diff --git a/scenes/farm.tscn b/scenes/farm.tscn
deleted file mode 100644
index 085dc6a..0000000
--- a/scenes/farm.tscn
+++ /dev/null
@@ -1,45 +0,0 @@
-[gd_scene load_steps=3 format=3]
-
-[ext_resource type="Script" path="res://scripts/farm.gd" id="1_farm"]
-
-[sub_resource type="RectangleShape2D" id="RectangleShape2D_farm"]
-size = Vector2(64, 64)
-
-[node name="Farm" type="StaticBody2D" groups=["buildings", "farms", "food_sources"]]
-script = ExtResource("1_farm")
-build_time = 6.0
-
-[node name="FinishedSprite" type="ColorRect" parent="."]
-offset_left = -32.0
-offset_top = -32.0
-offset_right = 32.0
-offset_bottom = 32.0
-color = Color(0.85, 0.78, 0.25, 1)
-
-[node name="ConstructionSprite" type="ColorRect" parent="."]
-visible = false
-offset_left = -32.0
-offset_top = -32.0
-offset_right = 32.0
-offset_bottom = 32.0
-color = Color(0.85, 0.78, 0.25, 0.45)
-
-[node name="CollisionShape2D" type="CollisionShape2D" parent="."]
-shape = SubResource("RectangleShape2D_farm")
-
-[node name="SelectionRing" type="ColorRect" parent="."]
-visible = false
-offset_left = -37.0
-offset_top = -37.0
-offset_right = 37.0
-offset_bottom = 37.0
-color = Color(0, 1, 0, 0.5)
-
-[node name="HealthBar" type="ProgressBar" parent="."]
-offset_left = -32.0
-offset_top = -45.0
-offset_right = 32.0
-offset_bottom = -39.0
-show_percentage = false
-value = 100.0
-
diff --git a/scenes/house.tscn b/scenes/house.tscn
deleted file mode 100644
index 7e2a3db..0000000
--- a/scenes/house.tscn
+++ /dev/null
@@ -1,45 +0,0 @@
-[gd_scene load_steps=3 format=3]
-
-[ext_resource type="Script" path="res://scripts/house.gd" id="1_house"]
-
-[sub_resource type="RectangleShape2D" id="RectangleShape2D_house"]
-size = Vector2(50, 50)
-
-[node name="House" type="StaticBody2D" groups=["buildings", "houses"]]
-script = ExtResource("1_house")
-build_time = 6.0
-
-[node name="FinishedSprite" type="ColorRect" parent="."]
-offset_left = -25.0
-offset_top = -25.0
-offset_right = 25.0
-offset_bottom = 25.0
-color = Color(0.8, 0.6, 0.3, 1)
-
-[node name="ConstructionSprite" type="ColorRect" parent="."]
-visible = false
-offset_left = -25.0
-offset_top = -25.0
-offset_right = 25.0
-offset_bottom = 25.0
-color = Color(0.8, 0.6, 0.3, 0.45)
-
-[node name="CollisionShape2D" type="CollisionShape2D" parent="."]
-shape = SubResource("RectangleShape2D_house")
-
-[node name="SelectionRing" type="ColorRect" parent="."]
-visible = false
-offset_left = -30.0
-offset_top = -30.0
-offset_right = 30.0
-offset_bottom = 30.0
-color = Color(0, 1, 0, 0.5)
-
-[node name="HealthBar" type="ProgressBar" parent="."]
-offset_left = -25.0
-offset_top = -38.0
-offset_right = 25.0
-offset_bottom = -32.0
-show_percentage = false
-value = 100.0
-
diff --git a/scenes/iron_ore.tscn b/scenes/iron_ore.tscn
deleted file mode 100644
index 7764d2a..0000000
--- a/scenes/iron_ore.tscn
+++ /dev/null
@@ -1,22 +0,0 @@
-[gd_scene load_steps=3 format=3]
-
-[ext_resource type="Script" path="res://scripts/resource_node.gd" id="1_node"]
-
-[sub_resource type="RectangleShape2D" id="RectangleShape2D_i"]
-size = Vector2(48, 40)
-
-[node name="ResourceNode" type="StaticBody2D" groups=["resources", "iron_sources", "iron_deposits"]]
-script = ExtResource("1_node")
-resource_type = "iron"
-amount = 300
-resource_group = "iron_sources"
-
-[node name="Sprite2D" type="ColorRect" parent="."]
-offset_left = -24.0
-offset_top = -20.0
-offset_right = 24.0
-offset_bottom = 20.0
-color = Color(0.55, 0.42, 0.3, 1)
-
-[node name="CollisionShape2D" type="CollisionShape2D" parent="."]
-shape = SubResource("RectangleShape2D_i")
diff --git a/scenes/lumber_camp.tscn b/scenes/lumber_camp.tscn
deleted file mode 100644
index d34f88f..0000000
--- a/scenes/lumber_camp.tscn
+++ /dev/null
@@ -1,45 +0,0 @@
-[gd_scene load_steps=3 format=3]
-
-[ext_resource type="Script" path="res://scripts/lumber_camp.gd" id="1_lc"]
-
-[sub_resource type="RectangleShape2D" id="RectangleShape2D_lc"]
-size = Vector2(58, 58)
-
-[node name="LumberCamp" type="StaticBody2D" groups=["buildings", "lumber_camps"]]
-script = ExtResource("1_lc")
-build_time = 7.0
-
-[node name="FinishedSprite" type="ColorRect" parent="."]
-offset_left = -29.0
-offset_top = -29.0
-offset_right = 29.0
-offset_bottom = 29.0
-color = Color(0.5, 0.35, 0.15, 1)
-
-[node name="ConstructionSprite" type="ColorRect" parent="."]
-visible = false
-offset_left = -29.0
-offset_top = -29.0
-offset_right = 29.0
-offset_bottom = 29.0
-color = Color(0.5, 0.35, 0.15, 0.45)
-
-[node name="CollisionShape2D" type="CollisionShape2D" parent="."]
-shape = SubResource("RectangleShape2D_lc")
-
-[node name="SelectionRing" type="ColorRect" parent="."]
-visible = false
-offset_left = -34.0
-offset_top = -34.0
-offset_right = 34.0
-offset_bottom = 34.0
-color = Color(0, 1, 0, 0.5)
-
-[node name="HealthBar" type="ProgressBar" parent="."]
-offset_left = -29.0
-offset_top = -42.0
-offset_right = 29.0
-offset_bottom = -36.0
-show_percentage = false
-value = 100.0
-
diff --git a/scenes/main.tscn516592240.tmp b/scenes/main.tscn516592240.tmp
deleted file mode 100644
index ec4e619..0000000
--- a/scenes/main.tscn516592240.tmp
+++ /dev/null
@@ -1,123 +0,0 @@
-[gd_scene format=3 uid="uid://b61bmgv0csuav"]
-
-[ext_resource type="Script" uid="uid://bm6hswb5o3att" path="res://scripts/main.gd" id="1_abc12"]
-[ext_resource type="Script" uid="uid://br1f8mq7tjqk3" path="res://scripts/camera_controller.gd" id="2_def34"]
-[ext_resource type="PackedScene" path="res://scenes/village_center.tscn" id="3_ghi56"]
-[ext_resource type="PackedScene" path="res://scenes/tree.tscn" id="4_jkl78"]
-[ext_resource type="Script" uid="uid://ryj3wh71g50v" path="res://scripts/ui_controller.gd" id="5_mno90"]
-
-[node name="Main" type="Node2D" unique_id=1732611410]
-script = ExtResource("1_abc12")
-
-[node name="Camera" type="Camera2D" parent="." unique_id=175337008]
-position = Vector2(576, 324)
-script = ExtResource("2_def34")
-
-[node name="SelectionBox" type="ColorRect" parent="Camera" unique_id=1866654882]
-visible = false
-offset_right = 40.0
-offset_bottom = 40.0
-color = Color(0, 0.588235, 1, 0.3)
-
-[node name="Ground" type="ColorRect" parent="." unique_id=85159542]
-offset_right = 2000.0
-offset_bottom = 2000.0
-color = Color(0.2, 0.45, 0.15, 1)
-
-[node name="VillageCenter" parent="." unique_id=331787969 instance=ExtResource("3_ghi56")]
-position = Vector2(576, 324)
-
-[node name="Trees" type="Node2D" parent="." unique_id=1395855366]
-
-[node name="Tree" parent="Trees" unique_id=348202000 instance=ExtResource("4_jkl78")]
-position = Vector2(200, 200)
-
-[node name="Tree2" parent="Trees" unique_id=1950232823 instance=ExtResource("4_jkl78")]
-position = Vector2(350, 150)
-
-[node name="Tree3" parent="Trees" unique_id=1713030014 instance=ExtResource("4_jkl78")]
-position = Vector2(150, 400)
-
-[node name="Tree4" parent="Trees" unique_id=505959592 instance=ExtResource("4_jkl78")]
-position = Vector2(900, 250)
-
-[node name="Tree5" parent="Trees" unique_id=812799217 instance=ExtResource("4_jkl78")]
-position = Vector2(800, 500)
-
-[node name="Tree6" parent="Trees" unique_id=720612466 instance=ExtResource("4_jkl78")]
-position = Vector2(1100, 400)
-
-[node name="UI" type="CanvasLayer" parent="." unique_id=958812626]
-script = ExtResource("5_mno90")
-
-[node name="Resources" type="VBoxContainer" parent="UI" unique_id=1703386770]
-offset_left = 20.0
-offset_top = 20.0
-offset_right = 200.0
-offset_bottom = 120.0
-
-[node name="FoodLabel" type="Label" parent="UI/Resources" unique_id=1526918947]
-layout_mode = 2
-theme_override_font_sizes/font_size = 20
-text = "Food: 200"
-
-[node name="WoodLabel" type="Label" parent="UI/Resources" unique_id=2092588299]
-layout_mode = 2
-theme_override_font_sizes/font_size = 20
-text = "Wood: 150"
-
-[node name="PopLabel" type="Label" parent="UI/Resources" unique_id=279992480]
-layout_mode = 2
-theme_override_font_sizes/font_size = 20
-text = "Pop: 0/10"
-
-[node name="UnitPanel" type="Panel" parent="UI" unique_id=1554682073]
-visible = false
-offset_left = 20.0
-offset_top = 500.0
-offset_right = 300.0
-offset_bottom = 620.0
-
-[node name="UnitName" type="Label" parent="UI/UnitPanel" unique_id=605497440]
-layout_mode = 0
-offset_left = 10.0
-offset_top = 10.0
-offset_right = 270.0
-offset_bottom = 40.0
-theme_override_font_sizes/font_size = 18
-text = "Unit Selected"
-
-[node name="BuildingPanel" type="Panel" parent="UI" unique_id=2119722606]
-visible = false
-offset_left = 20.0
-offset_top = 500.0
-offset_right = 300.0
-offset_bottom = 620.0
-
-[node name="BuildingName" type="Label" parent="UI/BuildingPanel" unique_id=1177288142]
-layout_mode = 0
-offset_left = 10.0
-offset_top = 10.0
-offset_right = 270.0
-offset_bottom = 40.0
-theme_override_font_sizes/font_size = 18
-text = "Village Center"
-
-[node name="SpawnWorkerButton" type="Button" parent="UI/BuildingPanel" unique_id=417506166]
-layout_mode = 0
-offset_left = 10.0
-offset_top = 50.0
-offset_right = 270.0
-offset_bottom = 85.0
-text = "Spawn Worker (50 Food)"
-
-[node name="SpawnSoldierButton" type="Button" parent="UI/BuildingPanel" unique_id=695316647]
-layout_mode = 0
-offset_left = 10.0
-offset_top = 95.0
-offset_right = 270.0
-offset_bottom = 130.0
-text = "Spawn Soldier (75F, 25W)"
-
-[connection signal="pressed" from="UI/BuildingPanel/SpawnWorkerButton" to="UI" method="_on_spawn_worker_pressed"]
-[connection signal="pressed" from="UI/BuildingPanel/SpawnSoldierButton" to="UI" method="_on_spawn_soldier_pressed"]
diff --git a/scenes/main.tscn548099280.tmp b/scenes/main.tscn548099280.tmp
deleted file mode 100644
index ec4e619..0000000
--- a/scenes/main.tscn548099280.tmp
+++ /dev/null
@@ -1,123 +0,0 @@
-[gd_scene format=3 uid="uid://b61bmgv0csuav"]
-
-[ext_resource type="Script" uid="uid://bm6hswb5o3att" path="res://scripts/main.gd" id="1_abc12"]
-[ext_resource type="Script" uid="uid://br1f8mq7tjqk3" path="res://scripts/camera_controller.gd" id="2_def34"]
-[ext_resource type="PackedScene" path="res://scenes/village_center.tscn" id="3_ghi56"]
-[ext_resource type="PackedScene" path="res://scenes/tree.tscn" id="4_jkl78"]
-[ext_resource type="Script" uid="uid://ryj3wh71g50v" path="res://scripts/ui_controller.gd" id="5_mno90"]
-
-[node name="Main" type="Node2D" unique_id=1732611410]
-script = ExtResource("1_abc12")
-
-[node name="Camera" type="Camera2D" parent="." unique_id=175337008]
-position = Vector2(576, 324)
-script = ExtResource("2_def34")
-
-[node name="SelectionBox" type="ColorRect" parent="Camera" unique_id=1866654882]
-visible = false
-offset_right = 40.0
-offset_bottom = 40.0
-color = Color(0, 0.588235, 1, 0.3)
-
-[node name="Ground" type="ColorRect" parent="." unique_id=85159542]
-offset_right = 2000.0
-offset_bottom = 2000.0
-color = Color(0.2, 0.45, 0.15, 1)
-
-[node name="VillageCenter" parent="." unique_id=331787969 instance=ExtResource("3_ghi56")]
-position = Vector2(576, 324)
-
-[node name="Trees" type="Node2D" parent="." unique_id=1395855366]
-
-[node name="Tree" parent="Trees" unique_id=348202000 instance=ExtResource("4_jkl78")]
-position = Vector2(200, 200)
-
-[node name="Tree2" parent="Trees" unique_id=1950232823 instance=ExtResource("4_jkl78")]
-position = Vector2(350, 150)
-
-[node name="Tree3" parent="Trees" unique_id=1713030014 instance=ExtResource("4_jkl78")]
-position = Vector2(150, 400)
-
-[node name="Tree4" parent="Trees" unique_id=505959592 instance=ExtResource("4_jkl78")]
-position = Vector2(900, 250)
-
-[node name="Tree5" parent="Trees" unique_id=812799217 instance=ExtResource("4_jkl78")]
-position = Vector2(800, 500)
-
-[node name="Tree6" parent="Trees" unique_id=720612466 instance=ExtResource("4_jkl78")]
-position = Vector2(1100, 400)
-
-[node name="UI" type="CanvasLayer" parent="." unique_id=958812626]
-script = ExtResource("5_mno90")
-
-[node name="Resources" type="VBoxContainer" parent="UI" unique_id=1703386770]
-offset_left = 20.0
-offset_top = 20.0
-offset_right = 200.0
-offset_bottom = 120.0
-
-[node name="FoodLabel" type="Label" parent="UI/Resources" unique_id=1526918947]
-layout_mode = 2
-theme_override_font_sizes/font_size = 20
-text = "Food: 200"
-
-[node name="WoodLabel" type="Label" parent="UI/Resources" unique_id=2092588299]
-layout_mode = 2
-theme_override_font_sizes/font_size = 20
-text = "Wood: 150"
-
-[node name="PopLabel" type="Label" parent="UI/Resources" unique_id=279992480]
-layout_mode = 2
-theme_override_font_sizes/font_size = 20
-text = "Pop: 0/10"
-
-[node name="UnitPanel" type="Panel" parent="UI" unique_id=1554682073]
-visible = false
-offset_left = 20.0
-offset_top = 500.0
-offset_right = 300.0
-offset_bottom = 620.0
-
-[node name="UnitName" type="Label" parent="UI/UnitPanel" unique_id=605497440]
-layout_mode = 0
-offset_left = 10.0
-offset_top = 10.0
-offset_right = 270.0
-offset_bottom = 40.0
-theme_override_font_sizes/font_size = 18
-text = "Unit Selected"
-
-[node name="BuildingPanel" type="Panel" parent="UI" unique_id=2119722606]
-visible = false
-offset_left = 20.0
-offset_top = 500.0
-offset_right = 300.0
-offset_bottom = 620.0
-
-[node name="BuildingName" type="Label" parent="UI/BuildingPanel" unique_id=1177288142]
-layout_mode = 0
-offset_left = 10.0
-offset_top = 10.0
-offset_right = 270.0
-offset_bottom = 40.0
-theme_override_font_sizes/font_size = 18
-text = "Village Center"
-
-[node name="SpawnWorkerButton" type="Button" parent="UI/BuildingPanel" unique_id=417506166]
-layout_mode = 0
-offset_left = 10.0
-offset_top = 50.0
-offset_right = 270.0
-offset_bottom = 85.0
-text = "Spawn Worker (50 Food)"
-
-[node name="SpawnSoldierButton" type="Button" parent="UI/BuildingPanel" unique_id=695316647]
-layout_mode = 0
-offset_left = 10.0
-offset_top = 95.0
-offset_right = 270.0
-offset_bottom = 130.0
-text = "Spawn Soldier (75F, 25W)"
-
-[connection signal="pressed" from="UI/BuildingPanel/SpawnWorkerButton" to="UI" method="_on_spawn_worker_pressed"]
-[connection signal="pressed" from="UI/BuildingPanel/SpawnSoldierButton" to="UI" method="_on_spawn_soldier_pressed"]
diff --git a/scenes/main.tscn622414641.tmp b/scenes/main.tscn622414641.tmp
deleted file mode 100644
index ec4e619..0000000
--- a/scenes/main.tscn622414641.tmp
+++ /dev/null
@@ -1,123 +0,0 @@
-[gd_scene format=3 uid="uid://b61bmgv0csuav"]
-
-[ext_resource type="Script" uid="uid://bm6hswb5o3att" path="res://scripts/main.gd" id="1_abc12"]
-[ext_resource type="Script" uid="uid://br1f8mq7tjqk3" path="res://scripts/camera_controller.gd" id="2_def34"]
-[ext_resource type="PackedScene" path="res://scenes/village_center.tscn" id="3_ghi56"]
-[ext_resource type="PackedScene" path="res://scenes/tree.tscn" id="4_jkl78"]
-[ext_resource type="Script" uid="uid://ryj3wh71g50v" path="res://scripts/ui_controller.gd" id="5_mno90"]
-
-[node name="Main" type="Node2D" unique_id=1732611410]
-script = ExtResource("1_abc12")
-
-[node name="Camera" type="Camera2D" parent="." unique_id=175337008]
-position = Vector2(576, 324)
-script = ExtResource("2_def34")
-
-[node name="SelectionBox" type="ColorRect" parent="Camera" unique_id=1866654882]
-visible = false
-offset_right = 40.0
-offset_bottom = 40.0
-color = Color(0, 0.588235, 1, 0.3)
-
-[node name="Ground" type="ColorRect" parent="." unique_id=85159542]
-offset_right = 2000.0
-offset_bottom = 2000.0
-color = Color(0.2, 0.45, 0.15, 1)
-
-[node name="VillageCenter" parent="." unique_id=331787969 instance=ExtResource("3_ghi56")]
-position = Vector2(576, 324)
-
-[node name="Trees" type="Node2D" parent="." unique_id=1395855366]
-
-[node name="Tree" parent="Trees" unique_id=348202000 instance=ExtResource("4_jkl78")]
-position = Vector2(200, 200)
-
-[node name="Tree2" parent="Trees" unique_id=1950232823 instance=ExtResource("4_jkl78")]
-position = Vector2(350, 150)
-
-[node name="Tree3" parent="Trees" unique_id=1713030014 instance=ExtResource("4_jkl78")]
-position = Vector2(150, 400)
-
-[node name="Tree4" parent="Trees" unique_id=505959592 instance=ExtResource("4_jkl78")]
-position = Vector2(900, 250)
-
-[node name="Tree5" parent="Trees" unique_id=812799217 instance=ExtResource("4_jkl78")]
-position = Vector2(800, 500)
-
-[node name="Tree6" parent="Trees" unique_id=720612466 instance=ExtResource("4_jkl78")]
-position = Vector2(1100, 400)
-
-[node name="UI" type="CanvasLayer" parent="." unique_id=958812626]
-script = ExtResource("5_mno90")
-
-[node name="Resources" type="VBoxContainer" parent="UI" unique_id=1703386770]
-offset_left = 20.0
-offset_top = 20.0
-offset_right = 200.0
-offset_bottom = 120.0
-
-[node name="FoodLabel" type="Label" parent="UI/Resources" unique_id=1526918947]
-layout_mode = 2
-theme_override_font_sizes/font_size = 20
-text = "Food: 200"
-
-[node name="WoodLabel" type="Label" parent="UI/Resources" unique_id=2092588299]
-layout_mode = 2
-theme_override_font_sizes/font_size = 20
-text = "Wood: 150"
-
-[node name="PopLabel" type="Label" parent="UI/Resources" unique_id=279992480]
-layout_mode = 2
-theme_override_font_sizes/font_size = 20
-text = "Pop: 0/10"
-
-[node name="UnitPanel" type="Panel" parent="UI" unique_id=1554682073]
-visible = false
-offset_left = 20.0
-offset_top = 500.0
-offset_right = 300.0
-offset_bottom = 620.0
-
-[node name="UnitName" type="Label" parent="UI/UnitPanel" unique_id=605497440]
-layout_mode = 0
-offset_left = 10.0
-offset_top = 10.0
-offset_right = 270.0
-offset_bottom = 40.0
-theme_override_font_sizes/font_size = 18
-text = "Unit Selected"
-
-[node name="BuildingPanel" type="Panel" parent="UI" unique_id=2119722606]
-visible = false
-offset_left = 20.0
-offset_top = 500.0
-offset_right = 300.0
-offset_bottom = 620.0
-
-[node name="BuildingName" type="Label" parent="UI/BuildingPanel" unique_id=1177288142]
-layout_mode = 0
-offset_left = 10.0
-offset_top = 10.0
-offset_right = 270.0
-offset_bottom = 40.0
-theme_override_font_sizes/font_size = 18
-text = "Village Center"
-
-[node name="SpawnWorkerButton" type="Button" parent="UI/BuildingPanel" unique_id=417506166]
-layout_mode = 0
-offset_left = 10.0
-offset_top = 50.0
-offset_right = 270.0
-offset_bottom = 85.0
-text = "Spawn Worker (50 Food)"
-
-[node name="SpawnSoldierButton" type="Button" parent="UI/BuildingPanel" unique_id=695316647]
-layout_mode = 0
-offset_left = 10.0
-offset_top = 95.0
-offset_right = 270.0
-offset_bottom = 130.0
-text = "Spawn Soldier (75F, 25W)"
-
-[connection signal="pressed" from="UI/BuildingPanel/SpawnWorkerButton" to="UI" method="_on_spawn_worker_pressed"]
-[connection signal="pressed" from="UI/BuildingPanel/SpawnSoldierButton" to="UI" method="_on_spawn_soldier_pressed"]
diff --git a/scenes/main.tscn670170028.tmp b/scenes/main.tscn670170028.tmp
deleted file mode 100644
index ec4e619..0000000
--- a/scenes/main.tscn670170028.tmp
+++ /dev/null
@@ -1,123 +0,0 @@
-[gd_scene format=3 uid="uid://b61bmgv0csuav"]
-
-[ext_resource type="Script" uid="uid://bm6hswb5o3att" path="res://scripts/main.gd" id="1_abc12"]
-[ext_resource type="Script" uid="uid://br1f8mq7tjqk3" path="res://scripts/camera_controller.gd" id="2_def34"]
-[ext_resource type="PackedScene" path="res://scenes/village_center.tscn" id="3_ghi56"]
-[ext_resource type="PackedScene" path="res://scenes/tree.tscn" id="4_jkl78"]
-[ext_resource type="Script" uid="uid://ryj3wh71g50v" path="res://scripts/ui_controller.gd" id="5_mno90"]
-
-[node name="Main" type="Node2D" unique_id=1732611410]
-script = ExtResource("1_abc12")
-
-[node name="Camera" type="Camera2D" parent="." unique_id=175337008]
-position = Vector2(576, 324)
-script = ExtResource("2_def34")
-
-[node name="SelectionBox" type="ColorRect" parent="Camera" unique_id=1866654882]
-visible = false
-offset_right = 40.0
-offset_bottom = 40.0
-color = Color(0, 0.588235, 1, 0.3)
-
-[node name="Ground" type="ColorRect" parent="." unique_id=85159542]
-offset_right = 2000.0
-offset_bottom = 2000.0
-color = Color(0.2, 0.45, 0.15, 1)
-
-[node name="VillageCenter" parent="." unique_id=331787969 instance=ExtResource("3_ghi56")]
-position = Vector2(576, 324)
-
-[node name="Trees" type="Node2D" parent="." unique_id=1395855366]
-
-[node name="Tree" parent="Trees" unique_id=348202000 instance=ExtResource("4_jkl78")]
-position = Vector2(200, 200)
-
-[node name="Tree2" parent="Trees" unique_id=1950232823 instance=ExtResource("4_jkl78")]
-position = Vector2(350, 150)
-
-[node name="Tree3" parent="Trees" unique_id=1713030014 instance=ExtResource("4_jkl78")]
-position = Vector2(150, 400)
-
-[node name="Tree4" parent="Trees" unique_id=505959592 instance=ExtResource("4_jkl78")]
-position = Vector2(900, 250)
-
-[node name="Tree5" parent="Trees" unique_id=812799217 instance=ExtResource("4_jkl78")]
-position = Vector2(800, 500)
-
-[node name="Tree6" parent="Trees" unique_id=720612466 instance=ExtResource("4_jkl78")]
-position = Vector2(1100, 400)
-
-[node name="UI" type="CanvasLayer" parent="." unique_id=958812626]
-script = ExtResource("5_mno90")
-
-[node name="Resources" type="VBoxContainer" parent="UI" unique_id=1703386770]
-offset_left = 20.0
-offset_top = 20.0
-offset_right = 200.0
-offset_bottom = 120.0
-
-[node name="FoodLabel" type="Label" parent="UI/Resources" unique_id=1526918947]
-layout_mode = 2
-theme_override_font_sizes/font_size = 20
-text = "Food: 200"
-
-[node name="WoodLabel" type="Label" parent="UI/Resources" unique_id=2092588299]
-layout_mode = 2
-theme_override_font_sizes/font_size = 20
-text = "Wood: 150"
-
-[node name="PopLabel" type="Label" parent="UI/Resources" unique_id=279992480]
-layout_mode = 2
-theme_override_font_sizes/font_size = 20
-text = "Pop: 0/10"
-
-[node name="UnitPanel" type="Panel" parent="UI" unique_id=1554682073]
-visible = false
-offset_left = 20.0
-offset_top = 500.0
-offset_right = 300.0
-offset_bottom = 620.0
-
-[node name="UnitName" type="Label" parent="UI/UnitPanel" unique_id=605497440]
-layout_mode = 0
-offset_left = 10.0
-offset_top = 10.0
-offset_right = 270.0
-offset_bottom = 40.0
-theme_override_font_sizes/font_size = 18
-text = "Unit Selected"
-
-[node name="BuildingPanel" type="Panel" parent="UI" unique_id=2119722606]
-visible = false
-offset_left = 20.0
-offset_top = 500.0
-offset_right = 300.0
-offset_bottom = 620.0
-
-[node name="BuildingName" type="Label" parent="UI/BuildingPanel" unique_id=1177288142]
-layout_mode = 0
-offset_left = 10.0
-offset_top = 10.0
-offset_right = 270.0
-offset_bottom = 40.0
-theme_override_font_sizes/font_size = 18
-text = "Village Center"
-
-[node name="SpawnWorkerButton" type="Button" parent="UI/BuildingPanel" unique_id=417506166]
-layout_mode = 0
-offset_left = 10.0
-offset_top = 50.0
-offset_right = 270.0
-offset_bottom = 85.0
-text = "Spawn Worker (50 Food)"
-
-[node name="SpawnSoldierButton" type="Button" parent="UI/BuildingPanel" unique_id=695316647]
-layout_mode = 0
-offset_left = 10.0
-offset_top = 95.0
-offset_right = 270.0
-offset_bottom = 130.0
-text = "Spawn Soldier (75F, 25W)"
-
-[connection signal="pressed" from="UI/BuildingPanel/SpawnWorkerButton" to="UI" method="_on_spawn_worker_pressed"]
-[connection signal="pressed" from="UI/BuildingPanel/SpawnSoldierButton" to="UI" method="_on_spawn_soldier_pressed"]
diff --git a/scenes/main.tscn711373797.tmp b/scenes/main.tscn711373797.tmp
deleted file mode 100644
index ec4e619..0000000
--- a/scenes/main.tscn711373797.tmp
+++ /dev/null
@@ -1,123 +0,0 @@
-[gd_scene format=3 uid="uid://b61bmgv0csuav"]
-
-[ext_resource type="Script" uid="uid://bm6hswb5o3att" path="res://scripts/main.gd" id="1_abc12"]
-[ext_resource type="Script" uid="uid://br1f8mq7tjqk3" path="res://scripts/camera_controller.gd" id="2_def34"]
-[ext_resource type="PackedScene" path="res://scenes/village_center.tscn" id="3_ghi56"]
-[ext_resource type="PackedScene" path="res://scenes/tree.tscn" id="4_jkl78"]
-[ext_resource type="Script" uid="uid://ryj3wh71g50v" path="res://scripts/ui_controller.gd" id="5_mno90"]
-
-[node name="Main" type="Node2D" unique_id=1732611410]
-script = ExtResource("1_abc12")
-
-[node name="Camera" type="Camera2D" parent="." unique_id=175337008]
-position = Vector2(576, 324)
-script = ExtResource("2_def34")
-
-[node name="SelectionBox" type="ColorRect" parent="Camera" unique_id=1866654882]
-visible = false
-offset_right = 40.0
-offset_bottom = 40.0
-color = Color(0, 0.588235, 1, 0.3)
-
-[node name="Ground" type="ColorRect" parent="." unique_id=85159542]
-offset_right = 2000.0
-offset_bottom = 2000.0
-color = Color(0.2, 0.45, 0.15, 1)
-
-[node name="VillageCenter" parent="." unique_id=331787969 instance=ExtResource("3_ghi56")]
-position = Vector2(576, 324)
-
-[node name="Trees" type="Node2D" parent="." unique_id=1395855366]
-
-[node name="Tree" parent="Trees" unique_id=348202000 instance=ExtResource("4_jkl78")]
-position = Vector2(200, 200)
-
-[node name="Tree2" parent="Trees" unique_id=1950232823 instance=ExtResource("4_jkl78")]
-position = Vector2(350, 150)
-
-[node name="Tree3" parent="Trees" unique_id=1713030014 instance=ExtResource("4_jkl78")]
-position = Vector2(150, 400)
-
-[node name="Tree4" parent="Trees" unique_id=505959592 instance=ExtResource("4_jkl78")]
-position = Vector2(900, 250)
-
-[node name="Tree5" parent="Trees" unique_id=812799217 instance=ExtResource("4_jkl78")]
-position = Vector2(800, 500)
-
-[node name="Tree6" parent="Trees" unique_id=720612466 instance=ExtResource("4_jkl78")]
-position = Vector2(1100, 400)
-
-[node name="UI" type="CanvasLayer" parent="." unique_id=958812626]
-script = ExtResource("5_mno90")
-
-[node name="Resources" type="VBoxContainer" parent="UI" unique_id=1703386770]
-offset_left = 20.0
-offset_top = 20.0
-offset_right = 200.0
-offset_bottom = 120.0
-
-[node name="FoodLabel" type="Label" parent="UI/Resources" unique_id=1526918947]
-layout_mode = 2
-theme_override_font_sizes/font_size = 20
-text = "Food: 200"
-
-[node name="WoodLabel" type="Label" parent="UI/Resources" unique_id=2092588299]
-layout_mode = 2
-theme_override_font_sizes/font_size = 20
-text = "Wood: 150"
-
-[node name="PopLabel" type="Label" parent="UI/Resources" unique_id=279992480]
-layout_mode = 2
-theme_override_font_sizes/font_size = 20
-text = "Pop: 0/10"
-
-[node name="UnitPanel" type="Panel" parent="UI" unique_id=1554682073]
-visible = false
-offset_left = 20.0
-offset_top = 500.0
-offset_right = 300.0
-offset_bottom = 620.0
-
-[node name="UnitName" type="Label" parent="UI/UnitPanel" unique_id=605497440]
-layout_mode = 0
-offset_left = 10.0
-offset_top = 10.0
-offset_right = 270.0
-offset_bottom = 40.0
-theme_override_font_sizes/font_size = 18
-text = "Unit Selected"
-
-[node name="BuildingPanel" type="Panel" parent="UI" unique_id=2119722606]
-visible = false
-offset_left = 20.0
-offset_top = 500.0
-offset_right = 300.0
-offset_bottom = 620.0
-
-[node name="BuildingName" type="Label" parent="UI/BuildingPanel" unique_id=1177288142]
-layout_mode = 0
-offset_left = 10.0
-offset_top = 10.0
-offset_right = 270.0
-offset_bottom = 40.0
-theme_override_font_sizes/font_size = 18
-text = "Village Center"
-
-[node name="SpawnWorkerButton" type="Button" parent="UI/BuildingPanel" unique_id=417506166]
-layout_mode = 0
-offset_left = 10.0
-offset_top = 50.0
-offset_right = 270.0
-offset_bottom = 85.0
-text = "Spawn Worker (50 Food)"
-
-[node name="SpawnSoldierButton" type="Button" parent="UI/BuildingPanel" unique_id=695316647]
-layout_mode = 0
-offset_left = 10.0
-offset_top = 95.0
-offset_right = 270.0
-offset_bottom = 130.0
-text = "Spawn Soldier (75F, 25W)"
-
-[connection signal="pressed" from="UI/BuildingPanel/SpawnWorkerButton" to="UI" method="_on_spawn_worker_pressed"]
-[connection signal="pressed" from="UI/BuildingPanel/SpawnSoldierButton" to="UI" method="_on_spawn_soldier_pressed"]
diff --git a/scenes/mine.tscn b/scenes/mine.tscn
deleted file mode 100644
index 7c448b1..0000000
--- a/scenes/mine.tscn
+++ /dev/null
@@ -1,44 +0,0 @@
-[gd_scene load_steps=3 format=3]
-
-[ext_resource type="Script" path="res://scripts/mine.gd" id="1_mine"]
-
-[sub_resource type="RectangleShape2D" id="RectangleShape2D_mine"]
-size = Vector2(58, 58)
-
-[node name="Mine" type="StaticBody2D" groups=["buildings", "mines"]]
-script = ExtResource("1_mine")
-build_time = 8.0
-
-[node name="FinishedSprite" type="ColorRect" parent="."]
-offset_left = -29.0
-offset_top = -29.0
-offset_right = 29.0
-offset_bottom = 29.0
-color = Color(0.45, 0.35, 0.25, 1)
-
-[node name="ConstructionSprite" type="ColorRect" parent="."]
-visible = false
-offset_left = -29.0
-offset_top = -29.0
-offset_right = 29.0
-offset_bottom = 29.0
-color = Color(0.45, 0.35, 0.25, 0.45)
-
-[node name="CollisionShape2D" type="CollisionShape2D" parent="."]
-shape = SubResource("RectangleShape2D_mine")
-
-[node name="SelectionRing" type="ColorRect" parent="."]
-visible = false
-offset_left = -34.0
-offset_top = -34.0
-offset_right = 34.0
-offset_bottom = 34.0
-color = Color(0, 1, 0, 0.5)
-
-[node name="HealthBar" type="ProgressBar" parent="."]
-offset_left = -29.0
-offset_top = -42.0
-offset_right = 29.0
-offset_bottom = -36.0
-show_percentage = false
-value = 100.0
diff --git a/scenes/mountain.tscn b/scenes/mountain.tscn
deleted file mode 100644
index 7641719..0000000
--- a/scenes/mountain.tscn
+++ /dev/null
@@ -1,22 +0,0 @@
-[gd_scene load_steps=3 format=3]
-
-[ext_resource type="Script" path="res://scripts/resource_node.gd" id="1_node"]
-
-[sub_resource type="RectangleShape2D" id="RectangleShape2D_m"]
-size = Vector2(56, 44)
-
-[node name="ResourceNode" type="StaticBody2D" groups=["resources", "stone_sources", "mountains"]]
-script = ExtResource("1_node")
-resource_type = "stone"
-amount = 400
-resource_group = "stone_sources"
-
-[node name="Sprite2D" type="ColorRect" parent="."]
-offset_left = -28.0
-offset_top = -22.0
-offset_right = 28.0
-offset_bottom = 22.0
-color = Color(0.42, 0.38, 0.34, 1)
-
-[node name="CollisionShape2D" type="CollisionShape2D" parent="."]
-shape = SubResource("RectangleShape2D_m")
diff --git a/scenes/quarry.tscn b/scenes/quarry.tscn
deleted file mode 100644
index b110c1e..0000000
--- a/scenes/quarry.tscn
+++ /dev/null
@@ -1,45 +0,0 @@
-[gd_scene load_steps=3 format=3]
-
-[ext_resource type="Script" path="res://scripts/quarry.gd" id="1_q"]
-
-[sub_resource type="RectangleShape2D" id="RectangleShape2D_q"]
-size = Vector2(58, 58)
-
-[node name="Quarry" type="StaticBody2D" groups=["buildings", "quarries"]]
-script = ExtResource("1_q")
-build_time = 7.0
-
-[node name="FinishedSprite" type="ColorRect" parent="."]
-offset_left = -29.0
-offset_top = -29.0
-offset_right = 29.0
-offset_bottom = 29.0
-color = Color(0.55, 0.55, 0.6, 1)
-
-[node name="ConstructionSprite" type="ColorRect" parent="."]
-visible = false
-offset_left = -29.0
-offset_top = -29.0
-offset_right = 29.0
-offset_bottom = 29.0
-color = Color(0.55, 0.55, 0.6, 0.45)
-
-[node name="CollisionShape2D" type="CollisionShape2D" parent="."]
-shape = SubResource("RectangleShape2D_q")
-
-[node name="SelectionRing" type="ColorRect" parent="."]
-visible = false
-offset_left = -34.0
-offset_top = -34.0
-offset_right = 34.0
-offset_bottom = 34.0
-color = Color(0, 1, 0, 0.5)
-
-[node name="HealthBar" type="ProgressBar" parent="."]
-offset_left = -29.0
-offset_top = -42.0
-offset_right = 29.0
-offset_bottom = -36.0
-show_percentage = false
-value = 100.0
-
diff --git a/scenes/soldier.tscn b/scenes/soldier.tscn
deleted file mode 100644
index 7d8d322..0000000
--- a/scenes/soldier.tscn
+++ /dev/null
@@ -1,36 +0,0 @@
-[gd_scene load_steps=3 format=3]
-
-[ext_resource type="Script" path="res://scripts/soldier.gd" id="1_soldier"]
-
-[sub_resource type="CircleShape2D" id="CircleShape2D_soldier"]
-radius = 13.0
-
-[node name="Soldier" type="CharacterBody2D" groups=["units", "soldiers"]]
-script = ExtResource("1_soldier")
-
-[node name="Sprite2D" type="ColorRect" parent="."]
-offset_left = -11.0
-offset_top = -11.0
-offset_right = 11.0
-offset_bottom = 11.0
-color = Color(0.78, 0.18, 0.18, 1)
-
-[node name="CollisionShape2D" type="CollisionShape2D" parent="."]
-shape = SubResource("CircleShape2D_soldier")
-
-[node name="SelectionRing" type="ColorRect" parent="."]
-visible = false
-offset_left = -15.0
-offset_top = -15.0
-offset_right = 15.0
-offset_bottom = 15.0
-color = Color(0, 1, 0, 0.55)
-
-[node name="HealthBar" type="ProgressBar" parent="."]
-offset_left = -16.0
-offset_top = -25.0
-offset_right = 16.0
-offset_bottom = -19.0
-show_percentage = false
-value = 100.0
-
diff --git a/scenes/tree.tscn b/scenes/tree.tscn
deleted file mode 100644
index 5db66aa..0000000
--- a/scenes/tree.tscn
+++ /dev/null
@@ -1,23 +0,0 @@
-[gd_scene load_steps=3 format=3]
-
-[ext_resource type="Script" path="res://scripts/resource_node.gd" id="1_tree"]
-
-[sub_resource type="CircleShape2D" id="CircleShape2D_tree"]
-radius = 20.0
-
-[node name="ResourceNode" type="StaticBody2D" groups=["resources", "wood_sources"]]
-script = ExtResource("1_tree")
-resource_type = "wood"
-amount = 120
-resource_group = "wood_sources"
-
-[node name="Sprite2D" type="ColorRect" parent="."]
-offset_left = -18.0
-offset_top = -26.0
-offset_right = 18.0
-offset_bottom = 20.0
-color = Color(0.12, 0.5, 0.12, 1)
-
-[node name="CollisionShape2D" type="CollisionShape2D" parent="."]
-shape = SubResource("CircleShape2D_tree")
-
diff --git a/scenes/units/artillery.tscn b/scenes/units/artillery.tscn
new file mode 100644
index 0000000..1f89c79
--- /dev/null
+++ b/scenes/units/artillery.tscn
@@ -0,0 +1,55 @@
+[gd_scene load_steps=9 format=3]
+
+[ext_resource type="Script" path="res://scripts/units/artillery.gd" id="1_s"]
+
+[sub_resource type="StandardMaterial3D" id="BodyMat"]
+albedo_color = Color(0.32, 0.36, 0.22, 1.0)
+
+[sub_resource type="StandardMaterial3D" id="BarMat"]
+albedo_color = Color(0.16, 0.16, 0.16, 1.0)
+
+[sub_resource type="SphereShape3D" id="Sph"]
+radius = 17.0
+
+[sub_resource type="StandardMaterial3D" id="SelectionRing_mat"]
+albedo_color = Color(0, 1, 0, 0.5)
+transparency = 1
+shading_mode = 0
+
+[sub_resource type="BoxMesh" id="Body"]
+size = Vector3(30, 16, 30)
+material = SubResource("BodyMat")
+
+[sub_resource type="BoxMesh" id="Bar"]
+size = Vector3(26, 6, 6)
+material = SubResource("BarMat")
+
+[sub_resource type="CylinderMesh" id="SelectionRing_mesh"]
+top_radius = 20
+bottom_radius = 20
+height = 0.5
+material = SubResource("SelectionRing_mat")
+
+[node name="Artillery" type="CharacterBody3D" groups=["units", "artillery"]]
+script = ExtResource("1_s")
+
+[node name="Mesh" type="MeshInstance3D" parent="."]
+transform = Transform3D(1, 0, 0, 0, 1, 0, 0, 0, 1, 0, 9, 0)
+mesh = SubResource("Body")
+
+[node name="Barrel" type="Node3D" parent="."]
+transform = Transform3D(1, 0, 0, 0, 1, 0, 0, 0, 1, 0, 12, 0)
+
+[node name="BarrelRect" type="MeshInstance3D" parent="Barrel"]
+transform = Transform3D(1, 0, 0, 0, 1, 0, 0, 0, 1, 13, 0, 0)
+mesh = SubResource("Bar")
+
+[node name="CollisionShape3D" type="CollisionShape3D" parent="."]
+transform = Transform3D(1, 0, 0, 0, 1, 0, 0, 0, 1, 0, 12, 0)
+shape = SubResource("Sph")
+
+[node name="SelectionRing" type="MeshInstance3D" parent="."]
+transform = Transform3D(1, 0, 0, 0, 1, 0, 0, 0, 1, 0, 0.3, 0)
+visible = false
+mesh = SubResource("SelectionRing_mesh")
+
diff --git a/scenes/units/citizen.tscn b/scenes/units/citizen.tscn
new file mode 100644
index 0000000..b4bbd29
--- /dev/null
+++ b/scenes/units/citizen.tscn
@@ -0,0 +1,50 @@
+[gd_scene load_steps=7 format=3]
+
+[ext_resource type="Script" path="res://scripts/units/citizen.gd" id="1_s"]
+
+[sub_resource type="CapsuleShape3D" id="Cap"]
+radius = 8.0
+height = 24.0
+
+[sub_resource type="StandardMaterial3D" id="SelectionRing_mat"]
+albedo_color = Color(0, 1, 0, 0.5)
+transparency = 1
+shading_mode = 0
+
+[sub_resource type="BoxMesh" id="Body"]
+size = Vector3(16, 24, 16)
+
+[sub_resource type="BoxMesh" id="Icon"]
+size = Vector3(8, 8, 8)
+
+[sub_resource type="CylinderMesh" id="SelectionRing_mesh"]
+top_radius = 16
+bottom_radius = 16
+height = 0.5
+material = SubResource("SelectionRing_mat")
+
+[node name="Citizen" type="CharacterBody3D" groups=["units", "citizens"]]
+script = ExtResource("1_s")
+
+[node name="Mesh" type="MeshInstance3D" parent="."]
+transform = Transform3D(1, 0, 0, 0, 1, 0, 0, 0, 1, 0, 12, 0)
+mesh = SubResource("Body")
+
+[node name="JobIcon" type="MeshInstance3D" parent="."]
+transform = Transform3D(1, 0, 0, 0, 1, 0, 0, 0, 1, 0, 30, 0)
+mesh = SubResource("Icon")
+
+[node name="CarryIcon" type="MeshInstance3D" parent="."]
+transform = Transform3D(1, 0, 0, 0, 1, 0, 0, 0, 1, 10, 16, 0)
+visible = false
+mesh = SubResource("Icon")
+
+[node name="CollisionShape3D" type="CollisionShape3D" parent="."]
+transform = Transform3D(1, 0, 0, 0, 1, 0, 0, 0, 1, 0, 12, 0)
+shape = SubResource("Cap")
+
+[node name="SelectionRing" type="MeshInstance3D" parent="."]
+transform = Transform3D(1, 0, 0, 0, 1, 0, 0, 0, 1, 0, 0.3, 0)
+visible = false
+mesh = SubResource("SelectionRing_mesh")
+
diff --git a/scenes/units/soldier.tscn b/scenes/units/soldier.tscn
new file mode 100644
index 0000000..1bbabcb
--- /dev/null
+++ b/scenes/units/soldier.tscn
@@ -0,0 +1,42 @@
+[gd_scene load_steps=7 format=3]
+
+[ext_resource type="Script" path="res://scripts/units/soldier.gd" id="1_s"]
+
+[sub_resource type="StandardMaterial3D" id="BodyMat"]
+albedo_color = Color(0.78, 0.18, 0.18, 1.0)
+
+[sub_resource type="CapsuleShape3D" id="Cap"]
+radius = 9.0
+height = 26.0
+
+[sub_resource type="StandardMaterial3D" id="SelectionRing_mat"]
+albedo_color = Color(0, 1, 0, 0.5)
+transparency = 1
+shading_mode = 0
+
+[sub_resource type="BoxMesh" id="Body"]
+size = Vector3(22, 26, 22)
+material = SubResource("BodyMat")
+
+[sub_resource type="CylinderMesh" id="SelectionRing_mesh"]
+top_radius = 16
+bottom_radius = 16
+height = 0.5
+material = SubResource("SelectionRing_mat")
+
+[node name="Soldier" type="CharacterBody3D" groups=["units", "soldiers"]]
+script = ExtResource("1_s")
+
+[node name="Mesh" type="MeshInstance3D" parent="."]
+transform = Transform3D(1, 0, 0, 0, 1, 0, 0, 0, 1, 0, 13, 0)
+mesh = SubResource("Body")
+
+[node name="CollisionShape3D" type="CollisionShape3D" parent="."]
+transform = Transform3D(1, 0, 0, 0, 1, 0, 0, 0, 1, 0, 13, 0)
+shape = SubResource("Cap")
+
+[node name="SelectionRing" type="MeshInstance3D" parent="."]
+transform = Transform3D(1, 0, 0, 0, 1, 0, 0, 0, 1, 0, 0.3, 0)
+visible = false
+mesh = SubResource("SelectionRing_mesh")
+
diff --git a/scenes/village_center.tscn b/scenes/village_center.tscn
deleted file mode 100644
index 44bfda9..0000000
--- a/scenes/village_center.tscn
+++ /dev/null
@@ -1,53 +0,0 @@
-[gd_scene load_steps=3 format=3]
-
-[ext_resource type="Script" path="res://scripts/village_center.gd" id="1_vc"]
-
-[sub_resource type="RectangleShape2D" id="RectangleShape2D_vc"]
-size = Vector2(84, 84)
-
-[node name="VillageCenter" type="StaticBody2D" groups=["village_centers", "buildings"]]
-script = ExtResource("1_vc")
-max_health = 800
-
-[node name="FinishedSprite" type="ColorRect" parent="."]
-offset_left = -42.0
-offset_top = -42.0
-offset_right = 42.0
-offset_bottom = 42.0
-color = Color(0.6, 0.3, 0.1, 1)
-
-[node name="ConstructionSprite" type="ColorRect" parent="."]
-visible = false
-offset_left = -42.0
-offset_top = -42.0
-offset_right = 42.0
-offset_bottom = 42.0
-color = Color(0.6, 0.3, 0.1, 0.5)
-
-[node name="CollisionShape2D" type="CollisionShape2D" parent="."]
-shape = SubResource("RectangleShape2D_vc")
-
-[node name="SelectionRing" type="ColorRect" parent="."]
-visible = false
-offset_left = -47.0
-offset_top = -47.0
-offset_right = 47.0
-offset_bottom = 47.0
-color = Color(0, 1, 0, 0.5)
-
-[node name="HealthBar" type="ProgressBar" parent="."]
-offset_left = -42.0
-offset_top = -58.0
-offset_right = 42.0
-offset_bottom = -52.0
-show_percentage = false
-value = 100.0
-
-[node name="RecruitBar" type="ProgressBar" parent="."]
-visible = false
-offset_left = -42.0
-offset_top = -68.0
-offset_right = 42.0
-offset_bottom = -61.0
-value = 0.0
-
diff --git a/scenes/water_source.tscn b/scenes/water_source.tscn
deleted file mode 100644
index a55dcf9..0000000
--- a/scenes/water_source.tscn
+++ /dev/null
@@ -1,22 +0,0 @@
-[gd_scene load_steps=3 format=3]
-
-[ext_resource type="Script" path="res://scripts/resource_node.gd" id="1_node"]
-
-[sub_resource type="CircleShape2D" id="CircleShape2D_w"]
-radius = 22.0
-
-[node name="ResourceNode" type="StaticBody2D" groups=["resources", "water_sources"]]
-script = ExtResource("1_node")
-resource_type = "water"
-amount = 99999
-resource_group = "water_sources"
-
-[node name="Sprite2D" type="ColorRect" parent="."]
-offset_left = -20.0
-offset_top = -20.0
-offset_right = 20.0
-offset_bottom = 20.0
-color = Color(0.2, 0.5, 0.8, 0.85)
-
-[node name="CollisionShape2D" type="CollisionShape2D" parent="."]
-shape = SubResource("CircleShape2D_w")
diff --git a/scenes/worker.tscn b/scenes/worker.tscn
deleted file mode 100644
index b711551..0000000
--- a/scenes/worker.tscn
+++ /dev/null
@@ -1,52 +0,0 @@
-[gd_scene load_steps=4 format=3]
-
-[ext_resource type="Script" path="res://scripts/worker.gd" id="1_wk123"]
-
-[sub_resource type="CircleShape2D" id="CircleShape2D_wk"]
-radius = 12.0
-
-[sub_resource type="CircleShape2D" id="CircleShape2D_wk2"]
-radius = 35.0
-
-[node name="Worker" type="CharacterBody2D" groups=["units", "workers"]]
-script = ExtResource("1_wk123")
-
-[node name="Sprite2D" type="ColorRect" parent="."]
-offset_left = -10.0
-offset_top = -10.0
-offset_right = 10.0
-offset_bottom = 10.0
-color = Color(0.2, 0.5, 0.9, 1)
-
-[node name="CollisionShape2D" type="CollisionShape2D" parent="."]
-shape = SubResource("CircleShape2D_wk")
-
-[node name="SelectionRing" type="ColorRect" parent="."]
-visible = false
-offset_left = -14.0
-offset_top = -14.0
-offset_right = 14.0
-offset_bottom = 14.0
-color = Color(0, 1, 0, 0.6)
-
-[node name="HealthBar" type="ProgressBar" parent="."]
-offset_left = -15.0
-offset_top = -22.0
-offset_right = 15.0
-offset_bottom = -16.0
-value = 100.0
-
-[node name="InteractRange" type="Area2D" parent="."]
-
-[node name="CollisionShape2D" type="CollisionShape2D" parent="InteractRange"]
-shape = SubResource("CircleShape2D_wk2")
-
-[node name="WoodIcon" type="ColorRect" parent="."]
-visible = false
-offset_left = -6.0
-offset_top = -30.0
-offset_right = 6.0
-offset_bottom = -18.0
-color = Color(0.45, 0.27, 0.1, 1)
-
-[connection signal="input_event" from="." to="." method="_on_input_event"]
diff --git a/scenes/world/mountain.tscn b/scenes/world/mountain.tscn
new file mode 100644
index 0000000..df1d1ca
--- /dev/null
+++ b/scenes/world/mountain.tscn
@@ -0,0 +1,37 @@
+[gd_scene load_steps=7 format=3]
+
+[ext_resource type="Script" path="res://scripts/world/mountain.gd" id="1_s"]
+
+[sub_resource type="StandardMaterial3D" id="MMat"]
+albedo_color = Color(0.42, 0.4, 0.38, 1.0)
+
+[sub_resource type="StandardMaterial3D" id="VMat"]
+albedo_color = Color(0.5, 0.36, 0.28, 1.0)
+
+[sub_resource type="BoxShape3D" id="MShape"]
+size = Vector3(72, 60, 56)
+
+[sub_resource type="BoxMesh" id="MMesh"]
+size = Vector3(72, 60, 56)
+material = SubResource("MMat")
+
+[sub_resource type="BoxMesh" id="VMesh"]
+size = Vector3(30, 18, 30)
+material = SubResource("VMat")
+
+[node name="Mountain" type="StaticBody3D" groups=["mountains"]]
+script = ExtResource("1_s")
+
+[node name="Mesh" type="MeshInstance3D" parent="."]
+transform = Transform3D(1, 0, 0, 0, 1, 0, 0, 0, 1, 0, 30, 0)
+mesh = SubResource("MMesh")
+
+[node name="IronVein" type="MeshInstance3D" parent="."]
+transform = Transform3D(1, 0, 0, 0, 1, 0, 0, 0, 1, 0, 62, 0)
+visible = false
+mesh = SubResource("VMesh")
+
+[node name="CollisionShape3D" type="CollisionShape3D" parent="."]
+transform = Transform3D(1, 0, 0, 0, 1, 0, 0, 0, 1, 0, 30, 0)
+shape = SubResource("MShape")
+
diff --git a/scenes/world/tree.tscn b/scenes/world/tree.tscn
new file mode 100644
index 0000000..3d577d9
--- /dev/null
+++ b/scenes/world/tree.tscn
@@ -0,0 +1,31 @@
+[gd_scene load_steps=5 format=3]
+
+[ext_resource type="Script" path="res://scripts/world/resource_node.gd" id="1_s"]
+
+[sub_resource type="StandardMaterial3D" id="TMat"]
+albedo_color = Color(0.12, 0.5, 0.12, 1.0)
+
+[sub_resource type="CylinderShape3D" id="Cyl"]
+radius = 18.0
+height = 50.0
+
+[sub_resource type="CylinderMesh" id="Trunk"]
+top_radius = 16.0
+bottom_radius = 18.0
+height = 50.0
+material = SubResource("TMat")
+
+[node name="ResourceNode" type="StaticBody3D" groups=["resources", "wood_sources"]]
+script = ExtResource("1_s")
+resource_type = "wood"
+amount = 120
+resource_group = "wood_sources"
+
+[node name="Mesh" type="MeshInstance3D" parent="."]
+transform = Transform3D(1, 0, 0, 0, 1, 0, 0, 0, 1, 0, 25, 0)
+mesh = SubResource("Trunk")
+
+[node name="CollisionShape3D" type="CollisionShape3D" parent="."]
+transform = Transform3D(1, 0, 0, 0, 1, 0, 0, 0, 1, 0, 25, 0)
+shape = SubResource("Cyl")
+
diff --git a/scenes/world/water_source.tscn b/scenes/world/water_source.tscn
new file mode 100644
index 0000000..890b197
--- /dev/null
+++ b/scenes/world/water_source.tscn
@@ -0,0 +1,29 @@
+[gd_scene load_steps=5 format=3]
+
+[ext_resource type="Script" path="res://scripts/world/resource_node.gd" id="1_s"]
+
+[sub_resource type="StandardMaterial3D" id="WMat"]
+albedo_color = Color(0.2, 0.5, 0.8, 0.85)
+transparency = 1
+
+[sub_resource type="BoxShape3D" id="WShape"]
+size = Vector3(44, 4, 44)
+
+[sub_resource type="BoxMesh" id="WMesh"]
+size = Vector3(44, 4, 44)
+material = SubResource("WMat")
+
+[node name="ResourceNode" type="StaticBody3D" groups=["resources", "water_sources"]]
+script = ExtResource("1_s")
+resource_type = "water"
+amount = 99999
+resource_group = "water_sources"
+
+[node name="Mesh" type="MeshInstance3D" parent="."]
+transform = Transform3D(1, 0, 0, 0, 1, 0, 0, 0, 1, 0, 2, 0)
+mesh = SubResource("WMesh")
+
+[node name="CollisionShape3D" type="CollisionShape3D" parent="."]
+transform = Transform3D(1, 0, 0, 0, 1, 0, 0, 0, 1, 0, 2, 0)
+shape = SubResource("WShape")
+
diff --git a/scripts/attack_area_indicator.gd b/scripts/attack_area_indicator.gd
deleted file mode 100644
index 22e57de..0000000
--- a/scripts/attack_area_indicator.gd
+++ /dev/null
@@ -1,51 +0,0 @@
-class_name AttackAreaIndicator
-extends Node2D
-
-## Reusable area-of-effect visual. Two jobs, one drawing routine:
-##   - "targeting" mode: the orange ghost circle that follows the mouse while
-##     the player is in attack-position mode (after pressing T), showing
-##     exactly where and how big the blast will be before they commit.
-##   - "aiming" mode: pinned at the locked impact point while a shell winds
-##     up, shrinking and brightening as it gets closer to landing so the
-##     player has a fair warning window to react.
-## camera_controller.gd drives the first one; Artillery drives the second.
-
-var radius: float = 60.0
-var progress: float = 0.0  # 0..1, only used in "aiming" mode
-var mode: String = "targeting"  # "targeting" | "aiming"
-
-
-func _ready() -> void:
-	z_index = 50
-	top_level = true  # world-space visual, unaffected by parent transforms/zoom tricks
-
-
-func set_radius(r: float) -> void:
-	radius = r
-	queue_redraw()
-
-
-func set_progress(p: float) -> void:
-	progress = clampf(p, 0.0, 1.0)
-	queue_redraw()
-
-
-func set_mode(m: String) -> void:
-	mode = m
-	queue_redraw()
-
-
-func _draw() -> void:
-	if mode == "targeting":
-		draw_circle(Vector2.ZERO, radius, Color(1.0, 0.55, 0.1, 0.16))
-		draw_arc(Vector2.ZERO, radius, 0.0, TAU, 48, Color(1.0, 0.55, 0.1, 0.9), 2.5)
-		draw_line(Vector2(-14, 0), Vector2(14, 0), Color(1.0, 0.55, 0.1, 0.9), 2.0)
-		draw_line(Vector2(0, -14), Vector2(0, 14), Color(1.0, 0.55, 0.1, 0.9), 2.0)
-	else:
-		# Windup: a faint outer ring at full blast radius, plus a brighter
-		# ring that shrinks inward and shifts toward red as the shot nears.
-		var shrink = lerpf(radius, radius * 0.3, progress)
-		var col = Color(1.0, 0.85 - 0.65 * progress, 0.1, 0.9)
-		draw_circle(Vector2.ZERO, radius, Color(1.0, 0.2, 0.05, 0.10))
-		draw_arc(Vector2.ZERO, radius, 0.0, TAU, 48, Color(1.0, 0.2, 0.05, 0.5), 1.5)
-		draw_arc(Vector2.ZERO, shrink, 0.0, TAU, 48, col, 3.0)
diff --git a/scripts/building.gd b/scripts/building.gd
deleted file mode 100644
index fdb30ee..0000000
--- a/scripts/building.gd
+++ /dev/null
@@ -1,134 +0,0 @@
-class_name Building
-extends StaticBody2D
-
-## Shared base for all structures: village center, houses, resource buildings,
-## barracks. Handles construction progress, health, and selection — concrete
-## buildings add production/training behaviour on top.
-
-signal destroyed(building)
-
-@export var max_health: int = 400
-@export var build_time: float = 6.0
-@export var team: int = 0
-
-var health: int
-var build_progress: float = 0.0
-var is_constructed: bool = false
-var selected: bool = false
-
-@onready var construction_sprite: Node = get_node_or_null("ConstructionSprite")
-@onready var finished_sprite: Node = get_node_or_null("FinishedSprite")
-@onready var health_bar: ProgressBar = get_node_or_null("HealthBar")
-@onready var selection_ring: Node = get_node_or_null("SelectionRing")
-
-var construction_bar: ProgressBar = null
-var construction_label: Label = null
-
-
-func _ready() -> void:
-	health = max_health
-	add_to_group("buildings")
-	if not is_constructed:
-		add_to_group("construction_sites")
-		_create_construction_feedback()
-	_refresh_construction_visual()
-	_update_health_bar()
-	if selection_ring:
-		selection_ring.visible = false
-	if not is_constructed and build_progress >= build_time:
-		finish_building()
-
-
-func _create_construction_feedback() -> void:
-	# Built in code instead of hand-added to every building scene, so every
-	# building type gets the same always-visible "how far along is this"
-	# feedback with no risk of one scene drifting out of sync with another.
-	# Positioned dynamically above the building's own collision height so it
-	# never overlaps the health bar regardless of how big the building is.
-	var top_offset = _building_top_offset()
-
-	construction_bar = ProgressBar.new()
-	construction_bar.show_percentage = false
-	construction_bar.value = 0.0
-	construction_bar.size = Vector2(64, 8)
-	construction_bar.position = Vector2(-32, top_offset - 26.0)
-	construction_bar.modulate = Color(1.0, 0.85, 0.2)
-	add_child(construction_bar)
-
-	construction_label = Label.new()
-	construction_label.text = "0%"
-	construction_label.size = Vector2(64, 16)
-	construction_label.position = Vector2(-32, top_offset - 44.0)
-	construction_label.horizontal_alignment = HORIZONTAL_ALIGNMENT_CENTER
-	construction_label.add_theme_font_size_override("font_size", 12)
-	add_child(construction_label)
-
-
-func _building_top_offset() -> float:
-	# Half-height of this building's collision shape (negative = upward in
-	# Godot's Y-down coordinate space), so feedback widgets clear the
-	# building's own sprite regardless of size.
-	var shape_node = get_node_or_null("CollisionShape2D")
-	if shape_node and shape_node.shape is RectangleShape2D:
-		return -shape_node.shape.size.y * 0.5
-	return -32.0
-
-
-func _process(_delta: float) -> void:
-	if not is_constructed and construction_bar:
-		var pct = clampf(build_progress / build_time * 100.0, 0.0, 100.0)
-		construction_bar.value = pct
-		if construction_label:
-			construction_label.text = "%d%%" % int(pct)
-
-
-func add_build_progress(amount: float) -> void:
-	if is_constructed:
-		return
-	build_progress += amount
-	if build_progress >= build_time:
-		finish_building()
-
-
-func finish_building() -> void:
-	if is_constructed:
-		return
-	is_constructed = true
-	remove_from_group("construction_sites")
-	_refresh_construction_visual()
-	if construction_bar:
-		construction_bar.queue_free()
-		construction_bar = null
-	if construction_label:
-		construction_label.queue_free()
-		construction_label = null
-
-
-func _refresh_construction_visual() -> void:
-	if construction_sprite:
-		construction_sprite.visible = not is_constructed
-	if finished_sprite:
-		finished_sprite.visible = is_constructed
-
-
-func set_selected(value: bool) -> void:
-	selected = value
-	if selection_ring:
-		selection_ring.visible = value
-
-
-func take_damage(amount: int) -> void:
-	health -= amount
-	_update_health_bar()
-	if health <= 0:
-		destroy()
-
-
-func _update_health_bar() -> void:
-	if health_bar:
-		health_bar.value = float(health) / float(max_health) * 100.0
-
-
-func destroy() -> void:
-	destroyed.emit(self)
-	queue_free()
diff --git a/scripts/barracks.gd b/scripts/buildings/barracks.gd
similarity index 89%
rename from scripts/barracks.gd
rename to scripts/buildings/barracks.gd
index 7dbe222..0a3e7dd 100644
--- a/scripts/barracks.gd
+++ b/scripts/buildings/barracks.gd
@@ -5,7 +5,7 @@ extends Building
 ## cost food/gold the citizen economy produced, so a healthy economy is a
 ## prerequisite for a real army rather than the two systems being unrelated.
 
-@export var spawn_offset: Vector2 = Vector2(0, 70)
+@export var spawn_offset: Vector3 = Vector3(0, 0, 70)
 
 var train_queue: Array = []
 var train_elapsed: float = 0.0
@@ -80,9 +80,9 @@ func _start_next() -> void:
 
 func _complete_training() -> void:
 	var entry = train_queue.front()
-	var scene_path = "res://scenes/artillery.tscn" if entry == "artillery" else "res://scenes/soldier.tscn"
+	var scene_path = "res://scenes/units/artillery.tscn" if entry == "artillery" else "res://scenes/units/soldier.tscn"
 	var unit = load(scene_path).instantiate()
-	unit.global_position = global_position + spawn_offset + Vector2(randf_range(-18, 18), randf_range(-18, 18))
+	unit.global_position = global_position + spawn_offset + Vector3(randf_range(-18, 18), 0, randf_range(-18, 18))
 	unit.team = team
 	get_tree().current_scene.get_node("Units").add_child(unit)
 	if entry == "artillery":
diff --git a/scripts/barracks.gd.uid b/scripts/buildings/barracks.gd.uid
similarity index 100%
rename from scripts/barracks.gd.uid
rename to scripts/buildings/barracks.gd.uid
diff --git a/scripts/buildings/building.gd b/scripts/buildings/building.gd
new file mode 100644
index 0000000..ff78255
--- /dev/null
+++ b/scripts/buildings/building.gd
@@ -0,0 +1,87 @@
+class_name Building
+extends StaticBody3D
+
+## Shared base for all structures: village center, houses, resource buildings,
+## barracks. Handles construction progress, health, and selection — concrete
+## buildings add production/training behaviour on top.
+##
+## 3D port: construction progress is shown by swapping the translucent
+## ConstructionMesh for the solid FinishedMesh (and in the HUD when selected),
+## rather than the in-world 2D progress bar the 2D version drew.
+
+signal destroyed(building)
+
+@export var max_health: int = 400
+@export var build_time: float = 6.0
+@export var team: int = 0
+## When true the building is already finished on spawn (used for the
+## hand-placed starting buildings).
+@export var starts_built: bool = false
+
+var health: int
+var build_progress: float = 0.0
+var is_constructed: bool = false
+var selected: bool = false
+
+@onready var construction_sprite: Node = get_node_or_null("ConstructionMesh")
+@onready var finished_sprite: Node = get_node_or_null("FinishedMesh")
+@onready var selection_ring: Node = get_node_or_null("SelectionRing")
+
+
+func _ready() -> void:
+	health = max_health
+	add_to_group("buildings")
+	if starts_built:
+		build_progress = build_time
+	if not is_constructed and not starts_built:
+		add_to_group("construction_sites")
+	_refresh_construction_visual()
+	if selection_ring:
+		selection_ring.visible = false
+	if not is_constructed and build_progress >= build_time:
+		finish_building()
+
+
+func _process(_delta: float) -> void:
+	# Kept so subclasses can safely call super._process(delta).
+	pass
+
+
+func add_build_progress(amount: float) -> void:
+	if is_constructed:
+		return
+	build_progress += amount
+	if build_progress >= build_time:
+		finish_building()
+
+
+func finish_building() -> void:
+	if is_constructed:
+		return
+	is_constructed = true
+	remove_from_group("construction_sites")
+	_refresh_construction_visual()
+
+
+func _refresh_construction_visual() -> void:
+	if construction_sprite:
+		construction_sprite.visible = not is_constructed
+	if finished_sprite:
+		finished_sprite.visible = is_constructed
+
+
+func set_selected(value: bool) -> void:
+	selected = value
+	if selection_ring:
+		selection_ring.visible = value
+
+
+func take_damage(amount: int) -> void:
+	health -= amount
+	if health <= 0:
+		destroy()
+
+
+func destroy() -> void:
+	destroyed.emit(self)
+	queue_free()
diff --git a/scripts/building.gd.uid b/scripts/buildings/building.gd.uid
similarity index 100%
rename from scripts/building.gd.uid
rename to scripts/buildings/building.gd.uid
diff --git a/scripts/farm.gd b/scripts/buildings/farm.gd
similarity index 89%
rename from scripts/farm.gd
rename to scripts/buildings/farm.gd
index bb3fcc3..68d3005 100644
--- a/scripts/farm.gd
+++ b/scripts/buildings/farm.gd
@@ -4,6 +4,7 @@ extends ResourceBuilding
 
 func _ready() -> void:
 	resource_group = "farms"
+	yield_resource = "food"
 	max_workers = 3
 	base_production_rate = 1.4
 	max_stockpile = 60
diff --git a/scripts/farm.gd.uid b/scripts/buildings/farm.gd.uid
similarity index 100%
rename from scripts/farm.gd.uid
rename to scripts/buildings/farm.gd.uid
diff --git a/scripts/house.gd b/scripts/buildings/house.gd
similarity index 100%
rename from scripts/house.gd
rename to scripts/buildings/house.gd
diff --git a/scripts/house.gd.uid b/scripts/buildings/house.gd.uid
similarity index 100%
rename from scripts/house.gd.uid
rename to scripts/buildings/house.gd.uid
diff --git a/scripts/lumber_camp.gd b/scripts/buildings/lumber_camp.gd
similarity index 88%
rename from scripts/lumber_camp.gd
rename to scripts/buildings/lumber_camp.gd
index efc7714..56fdbd5 100644
--- a/scripts/lumber_camp.gd
+++ b/scripts/buildings/lumber_camp.gd
@@ -4,6 +4,7 @@ extends ResourceBuilding
 
 func _ready() -> void:
 	resource_group = "lumber_camps"
+	yield_resource = "wood"
 	max_workers = 3
 	base_production_rate = 1.2
 	max_stockpile = 60
diff --git a/scripts/lumber_camp.gd.uid b/scripts/buildings/lumber_camp.gd.uid
similarity index 100%
rename from scripts/lumber_camp.gd.uid
rename to scripts/buildings/lumber_camp.gd.uid
diff --git a/scripts/mine.gd b/scripts/buildings/mine.gd
similarity index 74%
rename from scripts/mine.gd
rename to scripts/buildings/mine.gd
index be1a6ec..ca048da 100644
--- a/scripts/mine.gd
+++ b/scripts/buildings/mine.gd
@@ -6,7 +6,8 @@ extends ResourceBuilding
 
 func _ready() -> void:
 	resource_group = "mines"
-	deposit_group = "iron_sources"   # must be built on an iron-ore deposit
+	yield_resource = "iron"
+	deposit_group = "iron_sources"   # must be built on an iron-bearing mountain
 	max_workers = 3
 	base_production_rate = 0.8
 	max_stockpile = 60
diff --git a/scripts/mine.gd.uid b/scripts/buildings/mine.gd.uid
similarity index 100%
rename from scripts/mine.gd.uid
rename to scripts/buildings/mine.gd.uid
diff --git a/scripts/quarry.gd b/scripts/buildings/quarry.gd
similarity index 90%
rename from scripts/quarry.gd
rename to scripts/buildings/quarry.gd
index 0eba190..54be49d 100644
--- a/scripts/quarry.gd
+++ b/scripts/buildings/quarry.gd
@@ -4,6 +4,7 @@ extends ResourceBuilding
 
 func _ready() -> void:
 	resource_group = "quarries"
+	yield_resource = "stone"
 	deposit_group = "stone_sources"   # must be built on a mountain
 	max_workers = 3
 	base_production_rate = 0.9
diff --git a/scripts/quarry.gd.uid b/scripts/buildings/quarry.gd.uid
similarity index 100%
rename from scripts/quarry.gd.uid
rename to scripts/buildings/quarry.gd.uid
diff --git a/scripts/resource_building.gd b/scripts/buildings/resource_building.gd
similarity index 63%
rename from scripts/resource_building.gd
rename to scripts/buildings/resource_building.gd
index 1aa5dd8..5511a52 100644
--- a/scripts/resource_building.gd
+++ b/scripts/buildings/resource_building.gd
@@ -6,21 +6,24 @@ extends Building
 ## that assigned citizens draw from with `harvest()`.
 ##
 ## DEPOSIT-BACKED buildings (Quarry, Mine) set `deposit_group` to the group
-## name of the world ResourceNode they must sit on ("stone_sources" /
-## "iron_sources"). On placement they bind to the nearest such node and their
-## production drains it; once the deposit is empty, production stops.
-## Buildings with an empty `deposit_group` (Farm, Lumber Camp) produce freely
-## as before.
+## name of the Mountain they must sit on ("stone_sources" / "iron_sources").
+## A Mountain holds separate stone and iron pools, so the SAME mountain can
+## host both a quarry (drawing its stone) and a mine (drawing its iron). On
+## placement the building binds to the nearest matching mountain and its
+## production drains that mountain's pool for `yield_resource`; once the pool
+## is empty, production stops. Buildings with an empty `deposit_group` (Farm,
+## Lumber Camp) produce freely.
 
 @export var max_workers: int = 3
 @export var base_production_rate: float = 1.0  # stockpile units / second / worker
 @export var max_stockpile: int = 60
 @export var resource_group: String = ""  # "farms" / "lumber_camps" / "quarries" / "mines"
 @export var deposit_group: String = ""   # "" = produces freely; else must bind to a deposit
+@export var yield_resource: String = ""  # what a worker carries away: food/wood/stone/iron
 
 var workers: Array = []
 var stockpile: float = 0.0
-var host_deposit: ResourceNode = null    # set by bind_to_deposit() for deposit-backed buildings
+var host_deposit: Node = null    # a Mountain, set by bind_to_deposit() for deposit-backed buildings
 var _depleted_notice_sent: bool = false
 
 
@@ -44,29 +47,33 @@ func _process(delta: float) -> void:
 
 	var produced = base_production_rate * active * delta
 
-	# Deposit-backed buildings can only produce what the deposit still holds.
+	# Deposit-backed buildings can only produce what the bound mountain still
+	# holds of *their* resource (stone for a quarry, iron for a mine).
 	if deposit_group != "":
-		if host_deposit == null or not is_instance_valid(host_deposit) or host_deposit.is_depleted():
+		if host_deposit == null or not is_instance_valid(host_deposit) or host_deposit.is_depleted(yield_resource):
 			if not _depleted_notice_sent:
 				_depleted_notice_sent = true
 				GameManager.notify("%s deposit exhausted." % resource_group.capitalize())
 			return
-		var pulled = host_deposit.harvest(int(ceil(produced)))
+		var pulled = host_deposit.harvest(yield_resource, int(ceil(produced)))
 		produced = minf(produced, float(pulled))
 
 	stockpile = minf(stockpile + produced, max_stockpile)
 
 
-## Called right after the building is placed. Finds the nearest world
-## ResourceNode in `deposit_group` within `radius`; returns false if none.
-func bind_to_deposit(radius: float = 48.0) -> bool:
+## Called right after the building is placed. Finds the nearest Mountain in
+## `deposit_group` (that still holds this building's resource) within `radius`;
+## returns false if none.
+func bind_to_deposit(radius: float = 64.0) -> bool:
 	if deposit_group == "":
 		return true  # free producers don't need a deposit
-	var best: ResourceNode = null
+	var best: Node = null
 	var best_dist = radius
 	for node in get_tree().get_nodes_in_group(deposit_group):
 		if not is_instance_valid(node):
 			continue
+		if node.has_method("remaining") and node.remaining(yield_resource) <= 0:
+			continue  # this mountain is tapped out for our resource
 		var d = global_position.distance_to(node.global_position)
 		if d <= best_dist:
 			best_dist = d
@@ -102,6 +109,6 @@ func worker_count() -> int:
 
 
 func deposit_remaining() -> int:
-	if host_deposit != null and is_instance_valid(host_deposit):
-		return host_deposit.amount
+	if host_deposit != null and is_instance_valid(host_deposit) and host_deposit.has_method("remaining"):
+		return host_deposit.remaining(yield_resource)
 	return -1   # not deposit-backed
diff --git a/scripts/resource_building.gd.uid b/scripts/buildings/resource_building.gd.uid
similarity index 100%
rename from scripts/resource_building.gd.uid
rename to scripts/buildings/resource_building.gd.uid
diff --git a/scripts/village_center.gd b/scripts/buildings/village_center.gd
similarity index 87%
rename from scripts/village_center.gd
rename to scripts/buildings/village_center.gd
index 818cbdc..44007de 100644
--- a/scripts/village_center.gd
+++ b/scripts/buildings/village_center.gd
@@ -5,7 +5,7 @@ extends Building
 ## here automatically) and where new citizens are recruited. Always starts
 ## already-built so a fresh game has somewhere to deliver to immediately.
 
-@export var spawn_offset: Vector2 = Vector2(0, 86)
+@export var spawn_offset: Vector3 = Vector3(0, 0, 86)
 @export var recruit_time: float = 5.0
 
 var recruit_queue: Array = []  # [{type, duration}]
@@ -43,12 +43,12 @@ func _process(delta: float) -> void:
 
 
 func queue_citizen() -> bool:
-	var cost = {"food": 40}
+	var cost = {"food": 40, "water": 8}
 	if GameManager.population >= GameManager.housing_capacity:
 		GameManager.notify("Need more housing before recruiting.")
 		return false
 	if not GameManager.can_afford(cost):
-		GameManager.notify("Not enough food to recruit a citizen.")
+		GameManager.notify("Not enough food or water to recruit a citizen.")
 		return false
 	GameManager.spend(cost)
 	recruit_queue.append({"type": "citizen", "duration": recruit_time})
@@ -71,8 +71,8 @@ func _start_next_recruit() -> void:
 
 
 func _complete_recruit() -> void:
-	var citizen = preload("res://scenes/citizen.tscn").instantiate()
-	citizen.global_position = global_position + spawn_offset + Vector2(randf_range(-18, 18), randf_range(-18, 18))
+	var citizen = preload("res://scenes/units/citizen.tscn").instantiate()
+	citizen.global_position = global_position + spawn_offset + Vector3(randf_range(-18, 18), 0, randf_range(-18, 18))
 	citizen.team = team
 	get_tree().current_scene.get_node("Units").add_child(citizen)
 	citizen.setup_as_adult()
diff --git a/scripts/village_center.gd.uid b/scripts/buildings/village_center.gd.uid
similarity index 100%
rename from scripts/village_center.gd.uid
rename to scripts/buildings/village_center.gd.uid
diff --git a/scripts/camera_controller.gd b/scripts/camera_controller.gd
deleted file mode 100644
index 5afd87b..0000000
--- a/scripts/camera_controller.gd
+++ /dev/null
@@ -1,379 +0,0 @@
-extends Camera2D
-
-## The whole RTS input layer lives here: WASD/arrow pan, wheel zoom,
-## left-click/drag box-select (works uniformly across citizens AND
-## soldiers), right-click context commands (move / gather / attack / build
-## depending on what's under the cursor and what's selected), and
-## ghost-following building placement.
-
-@export var move_speed: float = 560.0
-@export var zoom_speed: float = 0.1
-@export var min_zoom: float = 0.4
-@export var max_zoom: float = 2.5
-
-var is_dragging: bool = false
-var drag_start: Vector2 = Vector2.ZERO
-
-@onready var selection_box: ColorRect = $SelectionBox
-@onready var placement_ghost: ColorRect = $PlacementGhost
-@onready var attack_area_ghost: AttackAreaIndicator = $AttackAreaGhost
-
-const BUILDING_SCENES := {
-	"house": "res://scenes/house.tscn",
-	"farm": "res://scenes/farm.tscn",
-	"lumber_camp": "res://scenes/lumber_camp.tscn",
-	"quarry": "res://scenes/quarry.tscn",
-	"barracks": "res://scenes/barracks.tscn",
-}
-
-
-func _ready() -> void:
-	selection_box.visible = false
-	placement_ghost.visible = false
-	attack_area_ghost.visible = false
-	GameManager.placement_mode_changed.connect(_on_placement_mode_changed)
-	GameManager.attack_targeting_mode_changed.connect(_on_attack_targeting_mode_changed)
-
-
-func _process(delta: float) -> void:
-	var input_dir = Vector2.ZERO
-	input_dir.x = Input.get_axis("ui_left", "ui_right")
-	input_dir.y = Input.get_axis("ui_up", "ui_down")
-	if input_dir != Vector2.ZERO:
-		position += input_dir.normalized() * move_speed * delta / zoom.x
-
-	if GameManager.is_placing_building and placement_ghost.visible:
-		placement_ghost.global_position = get_global_mouse_position() - placement_ghost.size * 0.5
-
-	if GameManager.is_targeting_attack_position and attack_area_ghost.visible:
-		attack_area_ghost.global_position = get_global_mouse_position()
-
-
-func _input(event: InputEvent) -> void:
-	if event is InputEventKey and event.pressed and event.keycode == KEY_T:
-		_toggle_attack_targeting()
-		return
-
-	if GameManager.is_placing_building:
-		_handle_placement_input(event)
-		return
-
-	if GameManager.is_targeting_attack_position:
-		_handle_attack_targeting_input(event)
-		return
-
-	if event is InputEventMouseButton:
-		if _is_over_ui(event.position):
-			return
-
-		match event.button_index:
-			MOUSE_BUTTON_WHEEL_UP:
-				_zoom_by(zoom_speed)
-			MOUSE_BUTTON_WHEEL_DOWN:
-				_zoom_by(-zoom_speed)
-			MOUSE_BUTTON_LEFT:
-				if event.pressed:
-					drag_start = get_global_mouse_position()
-					is_dragging = true
-					selection_box.visible = true
-				else:
-					is_dragging = false
-					selection_box.visible = false
-					_finish_left_drag()
-			MOUSE_BUTTON_RIGHT:
-				if event.pressed:
-					_handle_right_click()
-
-	elif event is InputEventMouseMotion and is_dragging:
-		_update_selection_box()
-
-	elif event is InputEventKey and event.pressed and event.keycode == KEY_ESCAPE:
-		GameManager.clear_selection()
-
-
-func _zoom_by(amount: float) -> void:
-	zoom = (zoom + Vector2.ONE * amount).clamp(Vector2.ONE * min_zoom, Vector2.ONE * max_zoom)
-
-
-# ---------------------------------------------------------------------------
-# Selection (left click / drag)
-# ---------------------------------------------------------------------------
-func _update_selection_box() -> void:
-	var current = get_global_mouse_position()
-	var top_left = Vector2(minf(drag_start.x, current.x), minf(drag_start.y, current.y))
-	selection_box.global_position = top_left
-	selection_box.size = (current - drag_start).abs()
-
-
-func _finish_left_drag() -> void:
-	var rect = Rect2(selection_box.global_position, selection_box.size)
-	if rect.size.length() < 8.0:
-		_handle_single_click(drag_start)
-		return
-
-	var found: Array = []
-	for unit in get_tree().get_nodes_in_group("units"):
-		if is_instance_valid(unit) and rect.has_point(unit.global_position) and unit.is_selectable_by_player():
-			found.append(unit)
-
-	if found.is_empty():
-		GameManager.clear_selection()
-	else:
-		GameManager.select_units(found)
-
-
-# ---------------------------------------------------------------------------
-# Single click selection — checks units first (smaller radius, in front),
-# then resource nodes (trees etc, for the remaining-amount tooltip), then
-# buildings. Iterating nodes directly instead of using a physics point query
-# avoids physics-query timing edge cases entirely, which matters a lot at
-# this project's scale (a few dozen units/buildings at most) and is far
-# easier to reason about / debug than a physics-space query fired from
-# _input().
-# ---------------------------------------------------------------------------
-const UNIT_CLICK_RADIUS: float = 16.0
-const BUILDING_CLICK_PADDING: float = 6.0  # extra margin around a building's collision box
-
-
-func _handle_single_click(click_pos: Vector2) -> void:
-	var unit = _find_clickable_unit(click_pos)
-	if unit:
-		GameManager.select_units([unit])
-		return
-
-	var resource = _nearest_in_radius(click_pos, "resources", RESOURCE_CLICK_RADIUS)
-	if resource:
-		GameManager.select_resource_node(resource)
-		return
-
-	var building = _find_clickable_building(click_pos)
-	if building:
-		GameManager.select_building(building)
-		return
-
-	GameManager.clear_selection()
-
-
-func _find_clickable_unit(world_pos: Vector2):
-	var best = null
-	var best_dist = UNIT_CLICK_RADIUS
-	for unit in get_tree().get_nodes_in_group("units"):
-		if not is_instance_valid(unit) or not unit.is_selectable_by_player():
-			continue
-		var d = world_pos.distance_to(unit.global_position)
-		if d <= best_dist:
-			best_dist = d
-			best = unit
-	return best
-
-
-func _find_clickable_building(world_pos: Vector2):
-	for building in get_tree().get_nodes_in_group("buildings"):
-		if not is_instance_valid(building):
-			continue
-		if _is_point_in_building(world_pos, building):
-			return building
-	return null
-
-
-func _is_point_in_building(world_pos: Vector2, building: Node) -> bool:
-	var local_pos = world_pos - building.global_position
-	var half_size = _building_half_size(building)
-	return absf(local_pos.x) <= half_size.x + BUILDING_CLICK_PADDING and absf(local_pos.y) <= half_size.y + BUILDING_CLICK_PADDING
-
-
-func _building_half_size(building: Node) -> Vector2:
-	# Every building scene's CollisionShape2D uses a RectangleShape2D — read
-	# its size directly so click tolerance always matches the actual sprite,
-	# without needing a physics query.
-	var shape_node = building.get_node_or_null("CollisionShape2D")
-	if shape_node and shape_node.shape is RectangleShape2D:
-		return shape_node.shape.size * 0.5
-	return Vector2(32, 32)  # sane fallback if a building is missing the node
-
-
-# ---------------------------------------------------------------------------
-# Right-click context commands — also resolved by direct node lookup so the
-# same target (tree / resource building / construction site / enemy) is
-# always found regardless of physics-frame timing.
-# ---------------------------------------------------------------------------
-const RESOURCE_CLICK_RADIUS: float = 26.0
-const ENEMY_CLICK_RADIUS: float = 20.0
-
-
-func _handle_right_click() -> void:
-	if GameManager.selected_units.is_empty():
-		return
-
-	var mouse_pos = get_global_mouse_position()
-	var target = _find_right_click_target(mouse_pos)
-
-	for unit in GameManager.selected_units:
-		if not is_instance_valid(unit) or not unit.is_alive:
-			continue
-		_issue_order(unit, target, mouse_pos)
-
-
-func _find_right_click_target(world_pos: Vector2):
-	# Priority: enemy > depletable resource node > construction site >
-	# finished resource building > any other building. This mirrors what a
-	# player expects right-clicking near overlapping things to do.
-	var enemy = _nearest_in_radius(world_pos, "enemies", ENEMY_CLICK_RADIUS)
-	if enemy:
-		return enemy
-
-	var resource = _nearest_in_radius(world_pos, "resources", RESOURCE_CLICK_RADIUS)
-	if resource:
-		return resource
-
-	var building = _find_clickable_building(world_pos)
-	if building:
-		return building
-
-	return null
-
-
-func _nearest_in_radius(world_pos: Vector2, group_name: String, radius: float):
-	var best = null
-	var best_dist = radius
-	for node in get_tree().get_nodes_in_group(group_name):
-		if not is_instance_valid(node):
-			continue
-		var d = world_pos.distance_to(node.global_position)
-		if d <= best_dist:
-			best_dist = d
-			best = node
-	return best
-
-
-func _issue_order(unit, target, mouse_pos: Vector2) -> void:
-	if target == null:
-		unit.command_move(mouse_pos)
-		return
-
-	if target.is_in_group("enemies"):
-		unit.command_attack(target)
-	elif target.is_in_group("resources"):
-		unit.command_gather(target)
-	elif target.is_in_group("construction_sites"):
-		unit.command_build(target)
-	elif target.is_in_group("farms") or target.is_in_group("lumber_camps") or target.is_in_group("quarries"):
-		# Right-clicking a finished resource building also assigns the worker there.
-		unit.command_gather(target)
-	else:
-		unit.command_move(mouse_pos)
-
-
-# ---------------------------------------------------------------------------
-# Building placement (ghost follows mouse until confirmed)
-# ---------------------------------------------------------------------------
-func _handle_placement_input(event: InputEvent) -> void:
-	if event is InputEventMouseButton and event.pressed:
-		if event.button_index == MOUSE_BUTTON_LEFT and not _is_over_ui(event.position):
-			_confirm_placement()
-		elif event.button_index == MOUSE_BUTTON_RIGHT:
-			GameManager.cancel_building_placement()
-	elif event is InputEventKey and event.pressed and event.keycode == KEY_ESCAPE:
-		GameManager.cancel_building_placement()
-
-
-func _confirm_placement() -> void:
-	var building_id = GameManager.placement_building_id
-	var scene_path = BUILDING_SCENES.get(building_id)
-	var cost = GameManager.COSTS.get(building_id)
-	if scene_path == null or cost == null:
-		GameManager.cancel_building_placement()
-		return
-	if not GameManager.can_afford(cost):
-		GameManager.notify("Not enough resources for that building.")
-		GameManager.cancel_building_placement()
-		return
-
-	GameManager.spend(cost)
-	var building = load(scene_path).instantiate()
-	building.global_position = get_global_mouse_position()
-	get_tree().current_scene.get_node("Buildings").add_child(building)
-
-	var builder = GameManager.placement_builder
-	if is_instance_valid(builder) and builder.has_method("command_build"):
-		builder.command_build(building)
-	elif not GameManager.selected_units.is_empty():
-		# No specific builder was chosen — send every currently selected
-		# citizen to help construct it, same as a real RTS queue order.
-		for unit in GameManager.selected_units:
-			if is_instance_valid(unit) and unit.has_method("command_build"):
-				unit.command_build(building)
-
-	GameManager.cancel_building_placement()
-
-
-func _on_placement_mode_changed(active: bool, _building_id: String) -> void:
-	placement_ghost.visible = active
-	if active:
-		is_dragging = false
-		selection_box.visible = false
-
-
-# ---------------------------------------------------------------------------
-# Artillery attack-position targeting (T then left-click an area)
-# ---------------------------------------------------------------------------
-func _toggle_attack_targeting() -> void:
-	if GameManager.is_placing_building:
-		return
-
-	if GameManager.is_targeting_attack_position:
-		GameManager.cancel_attack_position_targeting()
-		return
-
-	var radius := 0.0
-	var any_artillery := false
-	for unit in GameManager.selected_units:
-		if is_instance_valid(unit) and unit.is_alive and unit is Artillery:
-			any_artillery = true
-			radius = maxf(radius, unit.splash_radius)
-
-	if any_artillery:
-		GameManager.start_attack_position_targeting(radius)
-	else:
-		GameManager.notify("Select an artillery unit to give it an attack-position order.")
-
-
-func _handle_attack_targeting_input(event: InputEvent) -> void:
-	if event is InputEventMouseButton and event.pressed:
-		if event.button_index == MOUSE_BUTTON_LEFT and not _is_over_ui(event.position):
-			_confirm_attack_position()
-		elif event.button_index == MOUSE_BUTTON_RIGHT:
-			GameManager.cancel_attack_position_targeting()
-	elif event is InputEventKey and event.pressed and event.keycode == KEY_ESCAPE:
-		GameManager.cancel_attack_position_targeting()
-
-
-func _confirm_attack_position() -> void:
-	var pos = get_global_mouse_position()
-	for unit in GameManager.selected_units:
-		if is_instance_valid(unit) and unit.is_alive and unit is Artillery:
-			unit.command_attack_position(pos)
-	GameManager.cancel_attack_position_targeting()
-
-
-func _on_attack_targeting_mode_changed(active: bool, radius: float) -> void:
-	attack_area_ghost.visible = active
-	if active:
-		attack_area_ghost.set_mode("targeting")
-		attack_area_ghost.set_radius(radius)
-		is_dragging = false
-		selection_box.visible = false
-
-
-# ---------------------------------------------------------------------------
-# Helpers
-# ---------------------------------------------------------------------------
-func _is_over_ui(screen_pos: Vector2) -> bool:
-	var ui = get_tree().current_scene.get_node_or_null("UI")
-	if ui == null:
-		return false
-	for panel_name in ["TopBar", "SelectionPanel", "BuildMenu", "TimePanel"]:
-		var panel = ui.get_node_or_null(panel_name)
-		if panel and panel.visible and panel.get_global_rect().has_point(screen_pos):
-			return true
-	return false
diff --git a/scripts/camera_controller_changes.gd b/scripts/camera_controller_changes.gd
deleted file mode 100644
index e141898..0000000
--- a/scripts/camera_controller_changes.gd
+++ /dev/null
@@ -1,58 +0,0 @@
-# ===========================================================================
-# CHANGES FOR camera_controller.gd
-# ===========================================================================
-#
-# 1) Add "mine" to the BUILDING_SCENES dictionary:
-
-const BUILDING_SCENES := {
-	"house": "res://scenes/house.tscn",
-	"farm": "res://scenes/farm.tscn",
-	"lumber_camp": "res://scenes/lumber_camp.tscn",
-	"quarry": "res://scenes/quarry.tscn",
-	"mine": "res://scenes/mine.tscn",
-	"barracks": "res://scenes/barracks.tscn",
-}
-
-
-# 2) REPLACE the whole _confirm_placement() function with this version.
-#    The only new part is the deposit bind/validation block — it instantiates
-#    the building, tries to bind it to a deposit if it needs one, and rolls
-#    everything back (refund + free the node) if there's no deposit under it.
-
-func _confirm_placement() -> void:
-	var building_id = GameManager.placement_building_id
-	var scene_path = BUILDING_SCENES.get(building_id)
-	var cost = GameManager.COSTS.get(building_id)
-	if scene_path == null or cost == null:
-		GameManager.cancel_building_placement()
-		return
-	if not GameManager.can_afford(cost):
-		GameManager.notify("Not enough resources for that building.")
-		GameManager.cancel_building_placement()
-		return
-
-	var building = load(scene_path).instantiate()
-	building.global_position = get_global_mouse_position()
-	get_tree().current_scene.get_node("Buildings").add_child(building)
-
-	# Deposit-backed buildings (quarry/mine) must sit on a matching deposit.
-	if building is ResourceBuilding and building.deposit_group != "":
-		if not building.bind_to_deposit(48.0):
-			var what = "a mountain" if building.deposit_group == "stone_sources" else "an iron-ore deposit"
-			GameManager.notify("A %s must be placed on %s." % [building_id, what])
-			building.queue_free()
-			GameManager.cancel_building_placement()
-			return
-
-	# Only spend once we know the placement is valid.
-	GameManager.spend(cost)
-
-	var builder = GameManager.placement_builder
-	if is_instance_valid(builder) and builder.has_method("command_build"):
-		builder.command_build(building)
-	elif not GameManager.selected_units.is_empty():
-		for unit in GameManager.selected_units:
-			if is_instance_valid(unit) and unit.has_method("command_build"):
-				unit.command_build(building)
-
-	GameManager.cancel_building_placement()
diff --git a/scripts/camera_controller_changes.gd.uid b/scripts/camera_controller_changes.gd.uid
deleted file mode 100644
index b43a921..0000000
--- a/scripts/camera_controller_changes.gd.uid
+++ /dev/null
@@ -1 +0,0 @@
-uid://bodnurt44kn3b
diff --git a/scripts/game_manager.gd b/scripts/core/game_manager.gd
similarity index 91%
rename from scripts/game_manager.gd
rename to scripts/core/game_manager.gd
index 4eecf62..f4126df 100644
--- a/scripts/game_manager.gd
+++ b/scripts/core/game_manager.gd
@@ -6,7 +6,7 @@ extends Node
 ## buildings, citizens and soldiers never talk to each other directly
 ## for resources/population, they always go through GameManager.
 
-signal resources_changed(food: int, wood: int, stone: int, gold: int, population: int, max_population: int)
+signal resources_changed(food: int, wood: int, stone: int, gold: int, iron: int, water: int, population: int, max_population: int)
 signal selection_changed(units: Array, building, resource_node)
 signal time_changed(year: int, season: String, season_progress: float)
 signal citizen_born(citizen)
@@ -22,6 +22,8 @@ var food: int = 250
 var wood: int = 200
 var stone: int = 100
 var gold: int = 50
+var iron: int = 0    # mined from iron-ore deposits; used for advanced units
+var water: int = 60  # gathered from rivers; consumed when recruiting citizens
 
 # ---------------------------------------------------------------------------
 # Population
@@ -52,9 +54,10 @@ const COSTS := {
 	"farm":        {"wood": 50},
 	"lumber_camp": {"wood": 30, "stone": 10},
 	"quarry":      {"wood": 40},
+	"mine":        {"wood": 40, "stone": 20},
 	"barracks":    {"wood": 80, "stone": 40},
 	"soldier":     {"food": 60, "gold": 10},
-	"artillery":   {"food": 90, "wood": 20, "gold": 40},
+	"artillery":   {"food": 90, "wood": 20, "iron": 20, "gold": 20},
 }
 
 const BUILD_TIMES := {
@@ -62,6 +65,7 @@ const BUILD_TIMES := {
 	"farm": 6.0,
 	"lumber_camp": 7.0,
 	"quarry": 7.0,
+	"mine": 8.0,
 	"barracks": 10.0,
 	"soldier": 8.0,
 	"artillery": 14.0,
@@ -150,8 +154,8 @@ func _spawn_child() -> void:
 	if not is_instance_valid(house):
 		return
 
-	var child = preload("res://scenes/citizen.tscn").instantiate()
-	child.global_position = house.global_position + Vector2(randf_range(-24, 24), randf_range(-24, 24))
+	var child = preload("res://scenes/units/citizen.tscn").instantiate()
+	child.global_position = house.global_position + Vector3(randf_range(-24, 24), 0, randf_range(-24, 24))
 	get_tree().current_scene.get_node("Units").add_child(child)
 	child.setup_as_child()
 
@@ -170,7 +174,9 @@ func can_afford(cost: Dictionary) -> bool:
 	return (food >= cost.get("food", 0)
 		and wood >= cost.get("wood", 0)
 		and stone >= cost.get("stone", 0)
-		and gold >= cost.get("gold", 0))
+		and gold >= cost.get("gold", 0)
+		and iron >= cost.get("iron", 0)
+		and water >= cost.get("water", 0))
 
 
 func spend(cost: Dictionary) -> void:
@@ -178,19 +184,23 @@ func spend(cost: Dictionary) -> void:
 	wood -= cost.get("wood", 0)
 	stone -= cost.get("stone", 0)
 	gold -= cost.get("gold", 0)
+	iron -= cost.get("iron", 0)
+	water -= cost.get("water", 0)
 	update_ui()
 
 
-func add_resources(f: int = 0, w: int = 0, s: int = 0, g: int = 0) -> void:
+func add_resources(f: int = 0, w: int = 0, s: int = 0, g: int = 0, i: int = 0, wa: int = 0) -> void:
 	food += f
 	wood += w
 	stone += s
 	gold += g
+	iron += i
+	water += wa
 	update_ui()
 
 
 func update_ui() -> void:
-	resources_changed.emit(food, wood, stone, gold, population, housing_capacity)
+	resources_changed.emit(food, wood, stone, gold, iron, water, population, housing_capacity)
 
 
 # ---------------------------------------------------------------------------
diff --git a/scripts/game_manager.gd.uid b/scripts/core/game_manager.gd.uid
similarity index 100%
rename from scripts/game_manager.gd.uid
rename to scripts/core/game_manager.gd.uid
diff --git a/scripts/main.gd b/scripts/core/main.gd
similarity index 97%
rename from scripts/main.gd
rename to scripts/core/main.gd
index a30ed57..7069847 100644
--- a/scripts/main.gd
+++ b/scripts/core/main.gd
@@ -1,4 +1,4 @@
-extends Node2D
+extends Node3D
 
 ## Registers the hand-placed starting citizens with GameManager on boot, and
 ## seeds initial housing from the starting house(s) already in the scene
diff --git a/scripts/main.gd.uid b/scripts/core/main.gd.uid
similarity index 100%
rename from scripts/main.gd.uid
rename to scripts/core/main.gd.uid
diff --git a/scripts/main_menu.gd b/scripts/core/main_menu.gd
similarity index 96%
rename from scripts/main_menu.gd
rename to scripts/core/main_menu.gd
index 4663282..9a448f0 100644
--- a/scripts/main_menu.gd
+++ b/scripts/core/main_menu.gd
@@ -12,7 +12,7 @@ extends Control
 ##       ├── Button             "large_btn"     — Large  (4096 × 2304)
 ##       └── Button             "huge_btn"      — Huge   (6144 × 3456)
 
-const GAME_SCENE := "res://scenes/main.tscn"
+const GAME_SCENE := "res://scenes/core/main.tscn"
 
 @onready var small_btn:  Button = $VBoxContainer/SmallButton
 @onready var medium_btn: Button = $VBoxContainer/MediumButton
diff --git a/scripts/main_menu.gd.uid b/scripts/core/main_menu.gd.uid
similarity index 100%
rename from scripts/main_menu.gd.uid
rename to scripts/core/main_menu.gd.uid
diff --git a/scripts/map_settings.gd b/scripts/core/map_settings.gd
similarity index 100%
rename from scripts/map_settings.gd
rename to scripts/core/map_settings.gd
diff --git a/scripts/map_settings.gd.uid b/scripts/core/map_settings.gd.uid
similarity index 100%
rename from scripts/map_settings.gd.uid
rename to scripts/core/map_settings.gd.uid
diff --git a/scripts/effects/attack_area_indicator.gd b/scripts/effects/attack_area_indicator.gd
new file mode 100644
index 0000000..8cba01e
--- /dev/null
+++ b/scripts/effects/attack_area_indicator.gd
@@ -0,0 +1,58 @@
+class_name AttackAreaIndicator
+extends Node3D
+
+## Reusable area-of-effect ground marker. Two modes share one flat disc:
+##   - "targeting": the orange ghost that follows the mouse while the player is
+##     choosing where artillery will strike.
+##   - "aiming": pinned at the locked impact point while a shell winds up,
+##     shrinking and reddening as it nears landing for a fair warning window.
+## Built in code so the scene is a bare Node3D + this script.
+
+var radius: float = 60.0
+var progress: float = 0.0      # 0..1, only used in "aiming" mode
+var mode: String = "targeting" # "targeting" | "aiming"
+
+var _disc: MeshInstance3D
+var _mat: StandardMaterial3D
+
+
+func _ready() -> void:
+	_disc = MeshInstance3D.new()
+	var cyl := CylinderMesh.new()
+	cyl.top_radius = 1.0
+	cyl.bottom_radius = 1.0
+	cyl.height = 0.5
+	_disc.mesh = cyl
+	_mat = StandardMaterial3D.new()
+	_mat.shading_mode = BaseMaterial3D.SHADING_MODE_UNSHADED
+	_mat.transparency = BaseMaterial3D.TRANSPARENCY_ALPHA
+	_disc.material_override = _mat
+	add_child(_disc)
+	_apply()
+
+
+func set_radius(r: float) -> void:
+	radius = r
+	_apply()
+
+
+func set_progress(p: float) -> void:
+	progress = clampf(p, 0.0, 1.0)
+	_apply()
+
+
+func set_mode(m: String) -> void:
+	mode = m
+	_apply()
+
+
+func _apply() -> void:
+	if _disc == null:
+		return
+	if mode == "targeting":
+		_disc.scale = Vector3(radius, 1.0, radius)
+		_mat.albedo_color = Color(1.0, 0.55, 0.1, 0.28)
+	else:
+		var shrink := lerpf(radius, radius * 0.3, progress)
+		_disc.scale = Vector3(shrink, 1.0, shrink)
+		_mat.albedo_color = Color(1.0, 0.85 - 0.65 * progress, 0.1, 0.5)
diff --git a/scripts/attack_area_indicator.gd.uid b/scripts/effects/attack_area_indicator.gd.uid
similarity index 100%
rename from scripts/attack_area_indicator.gd.uid
rename to scripts/effects/attack_area_indicator.gd.uid
diff --git a/scripts/effects/explosion_effect.gd b/scripts/effects/explosion_effect.gd
new file mode 100644
index 0000000..b876cae
--- /dev/null
+++ b/scripts/effects/explosion_effect.gd
@@ -0,0 +1,46 @@
+class_name ExplosionEffect
+extends Node3D
+
+## Self-contained, self-cleaning impact visual: an expanding dome + flash that
+## fades over a fraction of a second. Spawned at the exact impact point. Builds
+## its own mesh so the scene is just a bare Node3D + this script.
+
+var _radius: float = 60.0
+var _t: float = 0.0
+var _duration: float = 0.45
+
+var _flash: MeshInstance3D
+var _mat: StandardMaterial3D
+
+
+func _ready() -> void:
+	_flash = MeshInstance3D.new()
+	var sphere := SphereMesh.new()
+	sphere.radius = 1.0
+	sphere.height = 2.0
+	_flash.mesh = sphere
+	_mat = StandardMaterial3D.new()
+	_mat.shading_mode = BaseMaterial3D.SHADING_MODE_UNSHADED
+	_mat.transparency = BaseMaterial3D.TRANSPARENCY_ALPHA
+	_mat.albedo_color = Color(1.0, 0.6, 0.15, 0.65)
+	_flash.material_override = _mat
+	add_child(_flash)
+	set_process(false)
+
+
+func detonate(radius: float) -> void:
+	_radius = radius
+	_t = 0.0
+	set_process(true)
+
+
+func _process(delta: float) -> void:
+	_t += delta
+	if _t >= _duration:
+		queue_free()
+		return
+	var p := clampf(_t / _duration, 0.0, 1.0)
+	var r := lerpf(_radius * 0.15, _radius, ease(p, 0.3))
+	# Flattened dome so it reads as a ground blast rather than a full sphere.
+	_flash.scale = Vector3(r, r * 0.5, r)
+	_mat.albedo_color.a = (1.0 - p) * 0.7
diff --git a/scripts/explosion_effect.gd.uid b/scripts/effects/explosion_effect.gd.uid
similarity index 100%
rename from scripts/explosion_effect.gd.uid
rename to scripts/effects/explosion_effect.gd.uid
diff --git a/scripts/explosion_effect.gd b/scripts/explosion_effect.gd
deleted file mode 100644
index 3ef662e..0000000
--- a/scripts/explosion_effect.gd
+++ /dev/null
@@ -1,41 +0,0 @@
-class_name ExplosionEffect
-extends Node2D
-
-## Self-contained, self-cleaning impact visual: an expanding ring + flash
-## that fades out over a fraction of a second. Spawned at the exact impact
-## point so the player gets unambiguous feedback about which area was just
-## hit, independent from (and outliving) the unit that fired the shot.
-
-var _radius: float = 60.0
-var _t: float = 0.0
-var _duration: float = 0.45
-
-
-func _ready() -> void:
-	z_index = 60
-	top_level = true
-	set_process(false)
-
-
-func detonate(radius: float) -> void:
-	_radius = radius
-	_t = 0.0
-	set_process(true)
-	queue_redraw()
-
-
-func _process(delta: float) -> void:
-	_t += delta
-	if _t >= _duration:
-		queue_free()
-		return
-	queue_redraw()
-
-
-func _draw() -> void:
-	var p = clampf(_t / _duration, 0.0, 1.0)
-	var r = lerpf(_radius * 0.15, _radius, ease(p, 0.3))
-	var alpha = 1.0 - p
-	draw_circle(Vector2.ZERO, r, Color(1.0, 0.55, 0.1, 0.35 * alpha))
-	draw_arc(Vector2.ZERO, r, 0.0, TAU, 48, Color(1.0, 0.85, 0.3, alpha), 4.0)
-	draw_circle(Vector2.ZERO, r * 0.3, Color(1.0, 0.95, 0.7, alpha))
diff --git a/scripts/input/camera_controller.gd b/scripts/input/camera_controller.gd
new file mode 100644
index 0000000..0a20361
--- /dev/null
+++ b/scripts/input/camera_controller.gd
@@ -0,0 +1,402 @@
+extends Node3D
+
+## 3D RTS camera rig + input layer (Empire-Earth-style angled top-down).
+##
+## This Node3D is the FOCUS POINT that slides across the ground (XZ). A child
+## Camera3D sits up-and-back on a boom and looks down at the focus, so zooming
+## just lengthens/shortens the boom and rotating spins the rig's yaw.
+##
+## Handles: WASD/arrow + edge pan (screen-relative), wheel zoom, Q/E rotate,
+## left-click/drag box-select (projects unit positions to screen), right-click
+## context commands (move/gather/attack/build via raycast), ghost-follow
+## building placement, and artillery attack-position targeting.
+
+@export var pan_speed: float = 600.0
+@export var rotate_speed: float = 1.6
+@export var zoom_step: float = 0.08
+@export var boom_min: float = 350.0    # closest view (boom length)
+@export var boom_max: float = 2200.0   # farthest view
+@export var pitch_degrees: float = 55.0
+
+@export var edge_pan_enabled: bool = true
+@export var edge_pan_margin: float = 24.0
+
+var _zoom: float = 0.35   # 0 = closest, 1 = farthest
+var is_dragging: bool = false
+var drag_start_screen: Vector2 = Vector2.ZERO
+
+var _map_size: Vector2 = Vector2.ZERO
+
+@onready var cam: Camera3D = $Camera3D
+var selection_box: Control = null            # 2D overlay in the UI layer
+var placement_ghost: Node3D = null           # world-space ghost (MeshInstance3D)
+var attack_area_ghost: AttackAreaIndicator = null
+
+const BUILDING_SCENES := {
+	"house": "res://scenes/buildings/house.tscn",
+	"farm": "res://scenes/buildings/farm.tscn",
+	"lumber_camp": "res://scenes/buildings/lumber_camp.tscn",
+	"quarry": "res://scenes/buildings/quarry.tscn",
+	"mine": "res://scenes/buildings/mine.tscn",
+	"barracks": "res://scenes/buildings/barracks.tscn",
+}
+
+
+func _ready() -> void:
+	var scene := get_tree().current_scene
+	selection_box = scene.get_node_or_null("UI/SelectionBox")
+	placement_ghost = scene.get_node_or_null("PlacementGhost")
+	attack_area_ghost = scene.get_node_or_null("AttackAreaGhost")
+	if selection_box:
+		selection_box.visible = false
+	if placement_ghost:
+		placement_ghost.visible = false
+	if attack_area_ghost:
+		attack_area_ghost.visible = false
+
+	GameManager.placement_mode_changed.connect(_on_placement_mode_changed)
+	GameManager.attack_targeting_mode_changed.connect(_on_attack_targeting_mode_changed)
+
+	_map_size = MapSettings.map_size
+	_apply_zoom()
+
+
+# ---------------------------------------------------------------------------
+# Per-frame: panning, rotation, and ghost-follow
+# ---------------------------------------------------------------------------
+func _process(delta: float) -> void:
+	var input_dir := Vector2.ZERO
+	input_dir.x = Input.get_axis("ui_left", "ui_right")
+	input_dir.y = Input.get_axis("ui_up", "ui_down")
+	input_dir += _edge_pan_dir()
+	if input_dir != Vector2.ZERO:
+		# Screen-relative: rotate the raw input by the rig's yaw so "up" always
+		# pans the way the camera faces. Pan faster when zoomed out.
+		var move := Vector3(input_dir.x, 0.0, input_dir.y).rotated(Vector3.UP, rotation.y)
+		var speed := pan_speed * lerpf(0.5, 2.2, _zoom)
+		position += move.normalized() * speed * delta
+		_clamp_to_bounds()
+
+	if Input.is_action_pressed("ui_page_up") or Input.is_key_pressed(KEY_Q):
+		rotation.y += rotate_speed * delta
+	if Input.is_action_pressed("ui_page_down") or Input.is_key_pressed(KEY_E):
+		rotation.y -= rotate_speed * delta
+
+	if GameManager.is_placing_building and placement_ghost and placement_ghost.visible:
+		placement_ghost.global_position = _ground_point(_mouse())
+
+	if GameManager.is_targeting_attack_position and attack_area_ghost and attack_area_ghost.visible:
+		attack_area_ghost.global_position = _ground_point(_mouse())
+
+
+func _edge_pan_dir() -> Vector2:
+	if not edge_pan_enabled or is_dragging:
+		return Vector2.ZERO
+	var vp := get_viewport().get_visible_rect().size
+	var m := _mouse()
+	if m.x < 0 or m.y < 0 or m.x > vp.x or m.y > vp.y:
+		return Vector2.ZERO
+	var dir := Vector2.ZERO
+	if m.x < edge_pan_margin: dir.x = -1.0
+	elif m.x > vp.x - edge_pan_margin: dir.x = 1.0
+	if m.y < edge_pan_margin: dir.y = -1.0
+	elif m.y > vp.y - edge_pan_margin: dir.y = 1.0
+	return dir
+
+
+func _clamp_to_bounds() -> void:
+	if _map_size == Vector2.ZERO:
+		return
+	position.x = clampf(position.x, 0.0, _map_size.x)
+	position.z = clampf(position.z, 0.0, _map_size.y)
+	position.y = 0.0
+
+
+func _apply_zoom() -> void:
+	# Boom: camera sits up and back, looking down at the rig origin.
+	var boom := lerpf(boom_min, boom_max, _zoom)
+	var pitch := deg_to_rad(pitch_degrees)
+	cam.position = Vector3(0.0, boom * sin(pitch), boom * cos(pitch))
+	cam.rotation = Vector3(-pitch, 0.0, 0.0)
+
+
+# ---------------------------------------------------------------------------
+# Picking helpers
+# ---------------------------------------------------------------------------
+func _mouse() -> Vector2:
+	return get_viewport().get_mouse_position()
+
+
+func _ground_point(screen_pos: Vector2) -> Vector3:
+	# Intersect the camera ray with the y = 0 ground plane.
+	var o := cam.project_ray_origin(screen_pos)
+	var d := cam.project_ray_normal(screen_pos)
+	if absf(d.y) < 0.0001:
+		return Vector3(position.x, 0.0, position.z)
+	var t := -o.y / d.y
+	if t < 0.0:
+		return Vector3(position.x, 0.0, position.z)
+	return o + d * t
+
+
+func _raycast(screen_pos: Vector2):
+	var o := cam.project_ray_origin(screen_pos)
+	var d := cam.project_ray_normal(screen_pos)
+	var params := PhysicsRayQueryParameters3D.create(o, o + d * 100000.0)
+	params.collide_with_areas = false
+	params.collide_with_bodies = true
+	var hit := get_world_3d().direct_space_state.intersect_ray(params)
+	return hit.get("collider") if hit else null
+
+
+# ---------------------------------------------------------------------------
+# Input
+# ---------------------------------------------------------------------------
+func _input(event: InputEvent) -> void:
+	if event is InputEventKey and event.pressed and event.keycode == KEY_T:
+		_toggle_attack_targeting()
+		return
+
+	if GameManager.is_placing_building:
+		_handle_placement_input(event)
+		return
+
+	if GameManager.is_targeting_attack_position:
+		_handle_attack_targeting_input(event)
+		return
+
+	if event is InputEventMouseButton:
+		if _is_over_ui(event.position):
+			return
+		match event.button_index:
+			MOUSE_BUTTON_WHEEL_UP:
+				_zoom = clampf(_zoom - zoom_step, 0.0, 1.0); _apply_zoom()
+			MOUSE_BUTTON_WHEEL_DOWN:
+				_zoom = clampf(_zoom + zoom_step, 0.0, 1.0); _apply_zoom()
+			MOUSE_BUTTON_LEFT:
+				if event.pressed:
+					drag_start_screen = event.position
+					is_dragging = true
+					if selection_box:
+						selection_box.visible = true
+						selection_box.position = drag_start_screen
+						selection_box.size = Vector2.ZERO
+				else:
+					is_dragging = false
+					if selection_box:
+						selection_box.visible = false
+					_finish_left_drag(event.position)
+			MOUSE_BUTTON_RIGHT:
+				if event.pressed:
+					_handle_right_click()
+
+	elif event is InputEventMouseMotion and is_dragging:
+		_update_selection_box(event.position)
+
+	elif event is InputEventKey and event.pressed and event.keycode == KEY_ESCAPE:
+		GameManager.clear_selection()
+
+
+# ---------------------------------------------------------------------------
+# Selection
+# ---------------------------------------------------------------------------
+func _update_selection_box(current: Vector2) -> void:
+	if selection_box == null:
+		return
+	var tl := Vector2(minf(drag_start_screen.x, current.x), minf(drag_start_screen.y, current.y))
+	selection_box.position = tl
+	selection_box.size = (current - drag_start_screen).abs()
+
+
+func _finish_left_drag(end_screen: Vector2) -> void:
+	if drag_start_screen.distance_to(end_screen) < 8.0:
+		_handle_single_click(end_screen)
+		return
+
+	var rect := Rect2(
+		Vector2(minf(drag_start_screen.x, end_screen.x), minf(drag_start_screen.y, end_screen.y)),
+		(end_screen - drag_start_screen).abs())
+
+	var found: Array = []
+	for unit in get_tree().get_nodes_in_group("units"):
+		if not is_instance_valid(unit) or not unit.is_selectable_by_player():
+			continue
+		# Skip anything behind the camera, then test its projected position.
+		if cam.is_position_behind(unit.global_position):
+			continue
+		if rect.has_point(cam.unproject_position(unit.global_position)):
+			found.append(unit)
+
+	if found.is_empty():
+		GameManager.clear_selection()
+	else:
+		GameManager.select_units(found)
+
+
+func _handle_single_click(screen_pos: Vector2) -> void:
+	var obj = _raycast(screen_pos)
+	if obj == null:
+		GameManager.clear_selection()
+		return
+	if obj is Unit and obj.is_selectable_by_player():
+		GameManager.select_units([obj])
+	elif obj is Building:
+		GameManager.select_building(obj)
+	elif obj is Mountain or obj is ResourceNode:
+		GameManager.select_resource_node(obj)
+	else:
+		GameManager.clear_selection()
+
+
+# ---------------------------------------------------------------------------
+# Right-click context commands
+# ---------------------------------------------------------------------------
+func _handle_right_click() -> void:
+	if GameManager.selected_units.is_empty():
+		return
+	var screen := _mouse()
+	var target = _raycast(screen)
+	var ground := _ground_point(screen)
+	for unit in GameManager.selected_units:
+		if is_instance_valid(unit) and unit.is_alive:
+			_issue_order(unit, target, ground)
+
+
+func _issue_order(unit, target, ground: Vector3) -> void:
+	if target == null or target == unit:
+		unit.command_move(ground)
+		return
+	if target.is_in_group("enemies"):
+		unit.command_attack(target)
+	elif target.is_in_group("resources"):
+		unit.command_gather(target)
+	elif target.is_in_group("construction_sites"):
+		unit.command_build(target)
+	elif target.is_in_group("farms") or target.is_in_group("lumber_camps") or target.is_in_group("quarries") or target.is_in_group("mines"):
+		unit.command_gather(target)
+	else:
+		unit.command_move(ground)
+
+
+# ---------------------------------------------------------------------------
+# Building placement
+# ---------------------------------------------------------------------------
+func _handle_placement_input(event: InputEvent) -> void:
+	if event is InputEventMouseButton and event.pressed:
+		if event.button_index == MOUSE_BUTTON_LEFT and not _is_over_ui(event.position):
+			_confirm_placement()
+		elif event.button_index == MOUSE_BUTTON_RIGHT:
+			GameManager.cancel_building_placement()
+	elif event is InputEventKey and event.pressed and event.keycode == KEY_ESCAPE:
+		GameManager.cancel_building_placement()
+
+
+func _confirm_placement() -> void:
+	var building_id = GameManager.placement_building_id
+	var scene_path = BUILDING_SCENES.get(building_id)
+	var cost = GameManager.COSTS.get(building_id)
+	if scene_path == null or cost == null:
+		GameManager.cancel_building_placement()
+		return
+	if not GameManager.can_afford(cost):
+		GameManager.notify("Not enough resources for that building.")
+		GameManager.cancel_building_placement()
+		return
+
+	var building = load(scene_path).instantiate()
+	get_tree().current_scene.get_node("Buildings").add_child(building)
+	building.global_position = _ground_point(_mouse())
+
+	if building is ResourceBuilding and building.deposit_group != "":
+		if not building.bind_to_deposit(64.0):
+			var what = "a mountain" if building.deposit_group == "stone_sources" else "an iron-bearing mountain"
+			GameManager.notify("A %s must be placed on %s." % [building_id, what])
+			building.queue_free()
+			GameManager.cancel_building_placement()
+			return
+
+	GameManager.spend(cost)
+
+	var builder = GameManager.placement_builder
+	if is_instance_valid(builder) and builder.has_method("command_build"):
+		builder.command_build(building)
+	elif not GameManager.selected_units.is_empty():
+		for unit in GameManager.selected_units:
+			if is_instance_valid(unit) and unit.has_method("command_build"):
+				unit.command_build(building)
+
+	GameManager.cancel_building_placement()
+
+
+func _on_placement_mode_changed(active: bool, _building_id: String) -> void:
+	if placement_ghost:
+		placement_ghost.visible = active
+	if active:
+		is_dragging = false
+		if selection_box:
+			selection_box.visible = false
+
+
+# ---------------------------------------------------------------------------
+# Artillery attack-position targeting
+# ---------------------------------------------------------------------------
+func _toggle_attack_targeting() -> void:
+	if GameManager.is_placing_building:
+		return
+	if GameManager.is_targeting_attack_position:
+		GameManager.cancel_attack_position_targeting()
+		return
+	var radius := 0.0
+	var any_artillery := false
+	for unit in GameManager.selected_units:
+		if is_instance_valid(unit) and unit.is_alive and unit is Artillery:
+			any_artillery = true
+			radius = maxf(radius, unit.splash_radius)
+	if any_artillery:
+		GameManager.start_attack_position_targeting(radius)
+	else:
+		GameManager.notify("Select an artillery unit to give it an attack-position order.")
+
+
+func _handle_attack_targeting_input(event: InputEvent) -> void:
+	if event is InputEventMouseButton and event.pressed:
+		if event.button_index == MOUSE_BUTTON_LEFT and not _is_over_ui(event.position):
+			_confirm_attack_position()
+		elif event.button_index == MOUSE_BUTTON_RIGHT:
+			GameManager.cancel_attack_position_targeting()
+	elif event is InputEventKey and event.pressed and event.keycode == KEY_ESCAPE:
+		GameManager.cancel_attack_position_targeting()
+
+
+func _confirm_attack_position() -> void:
+	var pos := _ground_point(_mouse())
+	for unit in GameManager.selected_units:
+		if is_instance_valid(unit) and unit.is_alive and unit is Artillery:
+			unit.command_attack_position(pos)
+	GameManager.cancel_attack_position_targeting()
+
+
+func _on_attack_targeting_mode_changed(active: bool, radius: float) -> void:
+	if attack_area_ghost:
+		attack_area_ghost.visible = active
+		if active:
+			attack_area_ghost.set_mode("targeting")
+			attack_area_ghost.set_radius(radius)
+	if active:
+		is_dragging = false
+		if selection_box:
+			selection_box.visible = false
+
+
+# ---------------------------------------------------------------------------
+# Helpers
+# ---------------------------------------------------------------------------
+func _is_over_ui(screen_pos: Vector2) -> bool:
+	var ui = get_tree().current_scene.get_node_or_null("UI")
+	if ui == null:
+		return false
+	for panel_name in ["TopBar", "SelectionPanel", "BuildMenu", "TimePanel"]:
+		var panel = ui.get_node_or_null(panel_name)
+		if panel and panel.visible and panel.get_global_rect().has_point(screen_pos):
+			return true
+	return false
diff --git a/scripts/camera_controller.gd.uid b/scripts/input/camera_controller.gd.uid
similarity index 100%
rename from scripts/camera_controller.gd.uid
rename to scripts/input/camera_controller.gd.uid
diff --git a/scripts/resource_node.gd b/scripts/resource_node.gd
deleted file mode 100644
index 4c22230..0000000
--- a/scripts/resource_node.gd
+++ /dev/null
@@ -1,67 +0,0 @@
-class_name ResourceNode
-extends StaticBody2D
-
-## A depletable world resource (tree, berry bush, stone deposit...) that
-## citizens gather directly via command_gather / the woodcutter auto-job.
-## Distinct from ResourceBuilding: this has no worker-slot cap and shrinks
-## visually as it's consumed, then disappears when empty.
-
-@export var resource_type: String = "wood"  # "wood", "food", "stone"
-@export var amount: int = 120
-@export var resource_group: String = "wood_sources"
-
-var max_amount: int
-var selected: bool = false
-var _selection_ring: ColorRect = null
-
-@onready var sprite: Node = get_node_or_null("Sprite2D")
-
-
-func _ready() -> void:
-	max_amount = amount
-	add_to_group("resources")
-	add_to_group(resource_group)
-
-
-func harvest(requested: int) -> int:
-	var actual = mini(requested, amount)
-	amount -= actual
-	_refresh_visual()
-	if amount <= 0:
-		_deplete()
-	return actual
-
-
-func is_depleted() -> bool:
-	return amount <= 0
-
-
-func set_selected(value: bool) -> void:
-	selected = value
-	if value and _selection_ring == null:
-		_selection_ring = ColorRect.new()
-		_selection_ring.color = Color(0, 1, 0, 0.5)
-		_selection_ring.size = Vector2(46, 46)
-		_selection_ring.position = Vector2(-23, -23)
-		add_child(_selection_ring)
-		move_child(_selection_ring, 0)  # draw behind the resource sprite, not on top of it
-	if _selection_ring:
-		_selection_ring.visible = value
-
-
-func _refresh_visual() -> void:
-	if not sprite:
-		return
-	var ratio = float(amount) / float(max_axis_amount())
-	sprite.scale = Vector2.ONE * maxf(ratio, 0.35)
-
-
-func max_axis_amount() -> int:
-	return max_amount if max_amount > 0 else 1
-
-
-func _deplete() -> void:
-	remove_from_group("resources")
-	remove_from_group(resource_group)
-	if sprite:
-		sprite.modulate = Color(0.45, 0.3, 0.15, 0.6)
diff --git a/scripts/resource_tree.gd b/scripts/resource_tree.gd
deleted file mode 100644
index b239513..0000000
--- a/scripts/resource_tree.gd
+++ /dev/null
@@ -1,28 +0,0 @@
-class_name ResourceTree
-extends StaticBody2D
-
-@export var wood_amount: int = 100
-
-@onready var sprite = $Sprite2D
-
-func _ready():
-	add_to_group("resources")
-	add_to_group("wood_sources")
-
-func gather(amount: int) -> int:
-	var actual = mini(amount, wood_amount)
-	wood_amount -= actual
-
-	var ratio = float(wood_amount) / 100.0
-	if sprite:
-		sprite.scale = Vector2.ONE * max(ratio, 0.3)
-		if wood_amount <= 0:
-			sprite.modulate = Color(0.5, 0.3, 0.1)
-
-	return actual
-
-func is_depleted() -> bool:
-	return wood_amount <= 0
-
-func _on_input_event(_viewport, event, _shape_idx):
-	pass
diff --git a/scripts/resource_tree.gd.uid b/scripts/resource_tree.gd.uid
deleted file mode 100644
index 80a92e7..0000000
--- a/scripts/resource_tree.gd.uid
+++ /dev/null
@@ -1 +0,0 @@
-uid://5i413k7e0lrr
diff --git a/scripts/tree.gd b/scripts/tree.gd
deleted file mode 100644
index 41aaac2..0000000
--- a/scripts/tree.gd
+++ /dev/null
@@ -1,28 +0,0 @@
-class_name ResourceTree
-extends StaticBody2D
-
-@export var wood_amount: int = 100
-
-@onready var sprite = $Sprite2D
-
-func _ready():
-	add_to_group("resources")
-	add_to_group("wood_sources")
-
-func gather(amount: int) -> int:
-	var actual = min(amount, wood_amount)
-	wood_amount -= actual
-
-	var ratio = float(wood_amount) / 100.0
-	sprite.scale = Vector2.ONE * max(ratio, 0.3)
-
-	if wood_amount <= 0:
-		sprite.modulate = Color(0.5, 0.3, 0.1)
-
-	return actual
-
-func is_depleted() -> bool:
-	return wood_amount <= 0
-
-func _on_input_event(_viewport, event, _shape_idx):
-	pass # Trees aren't selectable; right-click while a worker is selected to assign gathering.
diff --git a/scripts/tree.gd.uid b/scripts/tree.gd.uid
deleted file mode 100644
index 5580a74..0000000
--- a/scripts/tree.gd.uid
+++ /dev/null
@@ -1 +0,0 @@
-uid://bqiodkyimwdwi
diff --git a/scripts/ui_controller.gd b/scripts/ui/ui_controller.gd
similarity index 91%
rename from scripts/ui_controller.gd
rename to scripts/ui/ui_controller.gd
index 86ed45c..883cabc 100644
--- a/scripts/ui_controller.gd
+++ b/scripts/ui/ui_controller.gd
@@ -6,6 +6,8 @@ extends CanvasLayer
 @onready var food_label: Label = $TopBar/Resources/FoodLabel
 @onready var wood_label: Label = $TopBar/Resources/WoodLabel
 @onready var stone_label: Label = $TopBar/Resources/StoneLabel
+@onready var iron_label: Label = $TopBar/Resources/IronLabel
+@onready var water_label: Label = $TopBar/Resources/WaterLabel
 @onready var gold_label: Label = $TopBar/Resources/GoldLabel
 @onready var pop_label: Label = $TopBar/Resources/PopLabel
 
@@ -23,6 +25,7 @@ extends CanvasLayer
 @onready var build_farm_btn: Button = $BuildMenu/FarmButton
 @onready var build_lumber_btn: Button = $BuildMenu/LumberButton
 @onready var build_quarry_btn: Button = $BuildMenu/QuarryButton
+@onready var build_mine_btn: Button = $BuildMenu/MineButton
 @onready var build_barracks_btn: Button = $BuildMenu/BarracksButton
 
 @onready var village_actions: VBoxContainer = $SelectionPanel/VillageActions
@@ -49,6 +52,7 @@ func _ready() -> void:
 	build_farm_btn.pressed.connect(func(): _start_placement("farm"))
 	build_lumber_btn.pressed.connect(func(): _start_placement("lumber_camp"))
 	build_quarry_btn.pressed.connect(func(): _start_placement("quarry"))
+	build_mine_btn.pressed.connect(func(): _start_placement("mine"))
 	build_barracks_btn.pressed.connect(func(): _start_placement("barracks"))
 
 	recruit_citizen_btn.pressed.connect(_on_recruit_citizen_pressed)
@@ -79,10 +83,12 @@ func _process(delta: float) -> void:
 			notification_label.visible = false
 
 
-func _on_resources_changed(food: int, wood: int, stone: int, gold: int, population: int, max_population: int) -> void:
+func _on_resources_changed(food: int, wood: int, stone: int, gold: int, iron: int, water: int, population: int, max_population: int) -> void:
 	food_label.text = "Food: %d" % food
 	wood_label.text = "Wood: %d" % wood
 	stone_label.text = "Stone: %d" % stone
+	iron_label.text = "Iron: %d" % iron
+	water_label.text = "Water: %d" % water
 	gold_label.text = "Gold: %d" % gold
 	pop_label.text = "Pop: %d / %d" % [population, max_population]
 
@@ -126,6 +132,19 @@ func _on_selection_changed(units: Array, building, resource_node) -> void:
 
 
 func _show_resource_node_selection(resource_node) -> void:
+	if resource_node is Mountain:
+		selection_title.text = "Mountain"
+		var lines: Array[String] = []
+		lines.append("Stone: %d / %d" % [resource_node.remaining("stone"), resource_node.max_stone])
+		if resource_node.max_iron > 0:
+			lines.append("Iron: %d / %d" % [resource_node.remaining("iron"), resource_node.max_iron])
+		selection_info.text = "\n".join(lines)
+		if resource_node.max_iron > 0:
+			selection_hint.text = "Build a Quarry on it for stone, or a Mine for iron."
+		else:
+			selection_hint.text = "Build a Quarry on it to extract stone."
+		return
+
 	var label = _resource_type_label(resource_node.resource_type)
 	selection_title.text = label
 	if resource_node.is_depleted():
@@ -140,6 +159,8 @@ func _resource_type_label(resource_type: String) -> String:
 		"wood": return "Tree"
 		"stone": return "Stone Deposit"
 		"food": return "Berry Bush"
+		"iron": return "Iron Ore"
+		"water": return "Water"
 	return "Resource"
 
 
@@ -227,7 +248,7 @@ func _refresh_selected_building_info() -> void:
 			selection_info.text = "Training citizen: %d%%\nQueued: %d" % [pct, building.recruit_queue.size() - 1]
 		else:
 			selection_info.text = "Idle — recruit a new citizen."
-		selection_hint.text = "Costs 40 food, needs free housing."
+		selection_hint.text = "Costs 40 food + 8 water, needs free housing."
 
 	elif building is Barracks:
 		if not building.is_constructed:
@@ -239,8 +260,6 @@ func _refresh_selected_building_info() -> void:
 			selection_info.text = "Training %s: %d%%\nQueued: %d" % [current_id.capitalize(), pct, building.train_queue.size() - 1]
 		else:
 			selection_info.text = "Idle — train a soldier or artillery."
-		var soldier_cost = GameManager.COSTS.get("soldier", {})
-		var artillery_cost = GameManager.COSTS.get("artillery", {})
 		selection_hint.text = ""
 
 	elif building is ResourceBuilding:
@@ -291,6 +310,7 @@ func _on_placement_mode_changed(active: bool, building_id: String) -> void:
 	build_farm_btn.disabled = active
 	build_lumber_btn.disabled = active
 	build_quarry_btn.disabled = active
+	build_mine_btn.disabled = active
 	build_barracks_btn.disabled = active
 	if active:
 		var cost = GameManager.COSTS.get(building_id, {})
diff --git a/scripts/ui_controller.gd.uid b/scripts/ui/ui_controller.gd.uid
similarity index 100%
rename from scripts/ui_controller.gd.uid
rename to scripts/ui/ui_controller.gd.uid
diff --git a/scripts/ui_manager.gd b/scripts/ui_manager.gd
deleted file mode 100644
index b7aab6d..0000000
--- a/scripts/ui_manager.gd
+++ /dev/null
@@ -1,265 +0,0 @@
-extends CanvasLayer
-
-# Game references
-var game_manager: Node
-var economy_system: EconomySystem
-var merchant_manager: MerchantManager
-var player_data: PlayerData
-
-# UI elements
-var money_label: Label
-var wealth_label: Label
-var items_label: Label
-var inv_list_vbox: VBoxContainer
-
-# ── Colour palette ──────────────────────────────────────────────
-const C_PARCHMENT      := Color(0.910, 0.835, 0.639, 0.93)
-const C_PARCHMENT_DARK := Color(0.820, 0.745, 0.549, 0.97)
-const C_INK            := Color(0.172, 0.094, 0.063, 1.0)
-const C_BRASS          := Color(0.788, 0.573, 0.165, 1.0)
-const C_BRASS_DARK     := Color(0.580, 0.400, 0.090, 1.0)
-const C_NAVY           := Color(0.102, 0.153, 0.267, 0.88)
-const C_SEA            := Color(0.290, 0.486, 0.557, 1.0)
-const C_RED_WAX        := Color(0.600, 0.137, 0.137, 1.0)
-
-const FS_TITLE  := 15
-const FS_BODY   := 12
-const FS_SMALL  := 11
-
-# ────────────────────────────────────────────────────────────────
-func _ready():
-	# FIX: get_tree().root.get_child(0) is unreliable — autoload
-	# singletons are added as children of root BEFORE the main
-	# scene, so child 0 may be an autoload (e.g. game_clock.gd)
-	# instead of your actual game manager.
-	#
-	# get_tree().current_scene always points to the root node of
-	# your currently loaded scene, which is what you actually want.
-	game_manager = get_tree().current_scene
-	if not game_manager:
-		push_error("UIManager: game_manager not found")
-		return
-
-	await get_tree().process_frame
-
-	economy_system   = game_manager.economy_system
-	merchant_manager = game_manager.merchant_manager
-	player_data      = game_manager.player_data
-
-	if not player_data:
-		push_error("UIManager: player_data is null")
-		return
-
-	create_ui()
-
-# ═══════════════════════════════════════════════════════════════
-#  THEME HELPERS
-# ═══════════════════════════════════════════════════════════════
-
-func _panel_theme(bg: Color, border: Color = C_BRASS_DARK, radius: int = 4) -> StyleBoxFlat:
-	var s := StyleBoxFlat.new()
-	s.bg_color     = bg
-	s.border_color = border
-	s.set_border_width_all(2)
-	s.set_corner_radius_all(radius)
-	s.set_content_margin_all(8)
-	return s
-
-func _label_ink(lbl: Label, size: int = FS_BODY) -> void:
-	lbl.add_theme_color_override("font_color", C_INK)
-	lbl.add_theme_font_size_override("font_size", size)
-
-func _section_header(text: String) -> Label:
-	var lbl := Label.new()
-	lbl.text = text
-	lbl.add_theme_color_override("font_color", C_BRASS_DARK)
-	lbl.add_theme_font_size_override("font_size", FS_TITLE)
-	return lbl
-
-func _sep() -> HSeparator:
-	var s := HSeparator.new()
-	var style := StyleBoxFlat.new()
-	style.bg_color = C_BRASS
-	style.set_content_margin(SIDE_TOP, 2)
-	style.set_content_margin(SIDE_BOTTOM, 2)
-	s.add_theme_stylebox_override("separator", style)
-	return s
-
-# ═══════════════════════════════════════════════════════════════
-#  CREATE UI  –  map overlay (no merchant list — use cities!)
-# ═══════════════════════════════════════════════════════════════
-
-func create_ui():
-	var root := Control.new()
-	root.set_anchors_and_offsets_preset(Control.PRESET_FULL_RECT)
-	root.mouse_filter = Control.MOUSE_FILTER_IGNORE
-	add_child(root)
-
-	_build_player_panel(root)
-	_build_inventory_panel(root)
-	_build_market_ticker(root)
-	update_ui()
-
-# ── TOP-LEFT: Player status ──────────────────────────────────────
-func _build_player_panel(root: Control) -> void:
-	var panel := PanelContainer.new()
-	panel.add_theme_stylebox_override("panel", _panel_theme(C_PARCHMENT, C_BRASS_DARK))
-	panel.custom_minimum_size = Vector2(260, 0)
-	panel.anchor_left   = 0.0
-	panel.anchor_top    = 0.0
-	panel.anchor_right  = 0.0
-	panel.anchor_bottom = 0.0
-	panel.offset_left   = 12
-	panel.offset_top    = 12
-	panel.offset_right  = 272
-	panel.offset_bottom = 120
-	root.add_child(panel)
-
-	var vbox := VBoxContainer.new()
-	vbox.add_theme_constant_override("separation", 4)
-	panel.add_child(vbox)
-
-	var title := _section_header("⚓  Merchant Ledger")
-	vbox.add_child(title)
-	vbox.add_child(_sep())
-
-	money_label  = Label.new(); _label_ink(money_label)
-	wealth_label = Label.new(); _label_ink(wealth_label)
-	items_label  = Label.new(); _label_ink(items_label, FS_SMALL)
-	vbox.add_child(money_label)
-	vbox.add_child(wealth_label)
-	vbox.add_child(items_label)
-
-	# Hint to player
-	var hint := Label.new()
-	hint.text = "Enter a city to trade."
-	hint.add_theme_color_override("font_color", Color(C_BRASS_DARK, 0.75))
-	hint.add_theme_font_size_override("font_size", FS_SMALL)
-	vbox.add_child(hint)
-
-# ── BOTTOM-RIGHT: Inventory ──────────────────────────────────────
-func _build_inventory_panel(root: Control) -> void:
-	var panel := PanelContainer.new()
-	panel.add_theme_stylebox_override("panel", _panel_theme(C_PARCHMENT_DARK, C_BRASS_DARK))
-	panel.anchor_left   = 1.0
-	panel.anchor_top    = 1.0
-	panel.anchor_right  = 1.0
-	panel.anchor_bottom = 1.0
-	panel.offset_left   = -282
-	panel.offset_top    = -280
-	panel.offset_right  = -12
-	panel.offset_bottom = -12
-	root.add_child(panel)
-
-	var vbox := VBoxContainer.new()
-	vbox.add_theme_constant_override("separation", 5)
-	vbox.size_flags_vertical = Control.SIZE_EXPAND_FILL
-	panel.add_child(vbox)
-
-	var title := _section_header("🎒  Cargo Hold")
-	vbox.add_child(title)
-	vbox.add_child(_sep())
-
-	var scroll := ScrollContainer.new()
-	scroll.size_flags_vertical = Control.SIZE_EXPAND_FILL
-	vbox.add_child(scroll)
-
-	inv_list_vbox = VBoxContainer.new()
-	inv_list_vbox.size_flags_horizontal = Control.SIZE_EXPAND_FILL
-	inv_list_vbox.add_theme_constant_override("separation", 4)
-	scroll.add_child(inv_list_vbox)
-
-# ── BOTTOM-LEFT: Market price ticker ─────────────────────────────
-func _build_market_ticker(root: Control) -> void:
-	var panel := PanelContainer.new()
-	panel.add_theme_stylebox_override("panel", _panel_theme(C_NAVY, C_SEA))
-	panel.anchor_left   = 0.0
-	panel.anchor_top    = 1.0
-	panel.anchor_right  = 0.0
-	panel.anchor_bottom = 1.0
-	panel.offset_left   = 12
-	panel.offset_top    = -160
-	panel.offset_right  = 272
-	panel.offset_bottom = -12
-	root.add_child(panel)
-
-	var vbox := VBoxContainer.new()
-	vbox.add_theme_constant_override("separation", 4)
-	panel.add_child(vbox)
-
-	var title := Label.new()
-	title.text = "📈  Market Prices"
-	title.add_theme_color_override("font_color", C_PARCHMENT)
-	title.add_theme_font_size_override("font_size", FS_TITLE)
-	vbox.add_child(title)
-	vbox.add_child(_sep())
-
-	if economy_system:
-		for item in economy_system.get_all_items():
-			var row := HBoxContainer.new()
-			row.add_theme_constant_override("separation", 4)
-			vbox.add_child(row)
-
-			var name_lbl := Label.new()
-			name_lbl.text = item.capitalize()
-			name_lbl.size_flags_horizontal = Control.SIZE_EXPAND_FILL
-			name_lbl.add_theme_color_override("font_color", Color(C_PARCHMENT, 0.8))
-			name_lbl.add_theme_font_size_override("font_size", FS_SMALL)
-			row.add_child(name_lbl)
-
-			var trend := economy_system.get_trend_arrow(item)
-			var price_lbl := Label.new()
-			price_lbl.text = "%s %d 💰" % [trend, economy_system.get_price(item)]
-			price_lbl.add_theme_color_override("font_color", C_BRASS)
-			price_lbl.add_theme_font_size_override("font_size", FS_SMALL)
-			row.add_child(price_lbl)
-
-# ═══════════════════════════════════════════════════════════════
-#  UPDATE  –  refresh live panels
-# ═══════════════════════════════════════════════════════════════
-
-func update_ui():
-	var item_count := 0
-	for item in player_data.inventory:
-		item_count += player_data.inventory[item]
-
-	money_label.text  = "💰  %d coins" % player_data.money
-	wealth_label.text = "⚖   Wealth: %d" % player_data.get_total_wealth(economy_system)
-	items_label.text  = "📦  %d items in hold" % item_count
-
-	update_inventory_list()
-
-func update_inventory_list():
-	if not player_data:
-		return
-	for child in inv_list_vbox.get_children():
-		child.queue_free()
-
-	var items := player_data.get_inventory_items()
-	if items.is_empty():
-		var lbl := Label.new()
-		lbl.text = "The hold is empty."
-		_label_ink(lbl, FS_SMALL)
-		inv_list_vbox.add_child(lbl)
-		return
-
-	for item in items:
-		var amount := player_data.get_item_amount(item)
-		var price  := economy_system.get_price(item)
-		var value  := amount * price
-
-		var row := HBoxContainer.new()
-		row.add_theme_constant_override("separation", 6)
-		inv_list_vbox.add_child(row)
-
-		var lbl := Label.new()
-		lbl.text = "%d×  %-12s" % [amount, item.capitalize()]
-		_label_ink(lbl, FS_SMALL)
-		lbl.size_flags_horizontal = Control.SIZE_EXPAND_FILL
-		row.add_child(lbl)
-
-		var val_lbl := Label.new()
-		val_lbl.text = "%d 💰" % value
-		val_lbl.add_theme_color_override("font_color", C_BRASS_DARK)
-		val_lbl.add_theme_font_size_override("font_size", FS_SMALL)
-		row.add_child(val_lbl)
diff --git a/scripts/ui_manager.gd.uid b/scripts/ui_manager.gd.uid
deleted file mode 100644
index c478b76..0000000
--- a/scripts/ui_manager.gd.uid
+++ /dev/null
@@ -1 +0,0 @@
-uid://dfc23du7rjabe
diff --git a/scripts/artillery.gd b/scripts/units/artillery.gd
similarity index 86%
rename from scripts/artillery.gd
rename to scripts/units/artillery.gd
index ad11346..6881f41 100644
--- a/scripts/artillery.gd
+++ b/scripts/units/artillery.gd
@@ -22,21 +22,21 @@ extends Unit
 @export var friendly_fire: bool = true   # area strikes hit anyone in the blast, including own team
 
 var attack_target = null              # enemy Node, set by right-click-on-enemy orders
-var attack_position: Vector2 = Vector2.ZERO
+var attack_position: Vector3 = Vector3.ZERO
 var has_attack_position: bool = false  # true while a T+left-click bombardment is pending
 var has_player_order: bool = false
 
 var _attack_timer: float = 0.0
 var _aiming: bool = false
 var _aim_timer: float = 0.0
-var _aim_point: Vector2 = Vector2.ZERO
+var _aim_point: Vector3 = Vector3.ZERO
 var _reticle: AttackAreaIndicator = null
 
-@onready var sprite: Node = get_node_or_null("Sprite2D")
-@onready var barrel: Node2D = get_node_or_null("Barrel")
+@onready var sprite: Node = get_node_or_null("Mesh")
+@onready var barrel: Node3D = get_node_or_null("Barrel")
 
-const EXPLOSION_SCENE := preload("res://scenes/explosion_effect.tscn")
-const RETICLE_SCENE := preload("res://scenes/attack_area_indicator.tscn")
+const EXPLOSION_SCENE := preload("res://scenes/effects/explosion_effect.tscn")
+const RETICLE_SCENE := preload("res://scenes/effects/attack_area_indicator.tscn")
 
 
 func _ready() -> void:
@@ -67,9 +67,11 @@ func _has_valid_attack_target() -> bool:
 	return true
 
 
-func _run_attack_cycle(delta: float, target_pos: Vector2, target_unit) -> void:
+func _run_attack_cycle(delta: float, target_pos: Vector3, target_unit) -> void:
 	if barrel:
-		barrel.rotation = (target_pos - global_position).angle()
+		# Yaw the barrel toward the target on the ground plane.
+		var to_t := target_pos - global_position
+		barrel.rotation.y = atan2(to_t.x, to_t.z)
 
 	var dist = global_position.distance_to(target_pos)
 	if dist > attack_range or dist < min_range:
@@ -94,15 +96,15 @@ func _run_attack_cycle(delta: float, target_pos: Vector2, target_unit) -> void:
 			_start_aim(target_pos)
 
 
-func _move_into_firing_envelope(target_pos: Vector2, dist: float) -> void:
+func _move_into_firing_envelope(target_pos: Vector3, dist: float) -> void:
 	var dir = (global_position - target_pos).normalized()
-	if dir == Vector2.ZERO:
-		dir = Vector2.RIGHT
+	if dir == Vector3.ZERO:
+		dir = Vector3.RIGHT
 	var stand_dist = clampf(dist, min_range + 20.0, attack_range - 30.0)
 	move_to(target_pos + dir * stand_dist)
 
 
-func _start_aim(pos: Vector2) -> void:
+func _start_aim(pos: Vector3) -> void:
 	_aiming = true
 	_aim_timer = 0.0
 	_aim_point = pos
@@ -124,7 +126,7 @@ func _cancel_aim() -> void:
 		_reticle = null
 
 
-func _fire(pos: Vector2) -> void:
+func _fire(pos: Vector3) -> void:
 	if _reticle:
 		_reticle.queue_free()
 		_reticle = null
@@ -137,7 +139,7 @@ func _fire(pos: Vector2) -> void:
 	_apply_splash_damage(pos)
 
 
-func _apply_splash_damage(pos: Vector2) -> void:
+func _apply_splash_damage(pos: Vector3) -> void:
 	var targets: Array = []
 	for u in get_tree().get_nodes_in_group("units"):
 		if is_instance_valid(u) and u != self:
@@ -173,7 +175,7 @@ func _effects_parent() -> Node:
 # ---------------------------------------------------------------------------
 # RTS commands
 # ---------------------------------------------------------------------------
-func command_move(pos: Vector2) -> void:
+func command_move(pos: Vector3) -> void:
 	has_player_order = true
 	attack_target = null
 	has_attack_position = false
@@ -188,7 +190,7 @@ func command_attack(target) -> void:
 	_cancel_aim()
 
 
-func command_attack_position(pos: Vector2) -> void:
+func command_attack_position(pos: Vector3) -> void:
 	has_player_order = true
 	attack_target = null
 	attack_position = pos
diff --git a/scripts/artillery.gd.uid b/scripts/units/artillery.gd.uid
similarity index 100%
rename from scripts/artillery.gd.uid
rename to scripts/units/artillery.gd.uid
diff --git a/scripts/citizen.gd b/scripts/units/citizen.gd
similarity index 73%
rename from scripts/citizen.gd
rename to scripts/units/citizen.gd
index 2c7e9ac..1b3ef08 100644
--- a/scripts/citizen.gd
+++ b/scripts/units/citizen.gd
@@ -12,7 +12,7 @@ extends Unit
 ## drives a citizen when nobody has told it what to do.
 
 enum LifeStage { CHILD, ADULT, ELDER }
-enum Job { NONE, FARMER, WOODCUTTER, MINER, BUILDER, TRADER }
+enum Job { NONE, FARMER, WOODCUTTER, MINER, BUILDER, TRADER, WATERCARRIER }
 
 const CHILD_TO_ADULT_AGE := 14
 const ADULT_TO_ELDER_AGE := 50
@@ -41,37 +41,32 @@ var order_kind: String = ""  # "gather", "build", "move"
 var _gather_timer: float = 0.0
 var _job_search_cooldown: float = 0.0
 
-@onready var sprite: Node = get_node_or_null("Sprite2D")
+@onready var sprite: Node = get_node_or_null("Mesh")
 @onready var job_icon: Node = get_node_or_null("JobIcon")
 @onready var carry_icon: Node = get_node_or_null("CarryIcon")
 
 const INTERACTION_MARGIN: float = 14.0  # slack beyond physical contact so movement noise can't strand a citizen just outside range
-const OWN_RADIUS: float = 12.0  # matches Citizen's CircleShape2D in citizen.tscn
+const OWN_RADIUS: float = 12.0  # matches Citizen's collision radius in citizen.tscn
 
 
 func _interaction_range(target: Node) -> float:
 	# How close this citizen can actually get to a target before solid
-	# collision physically stops it, plus a small margin. A single fixed
-	# number here was the original bug: it was smaller than the real
-	# contact distance for the Village Center and Barracks (both have a
-	# large collision box), so citizens would walk into the wall, stop
-	# there, and never register as "arrived" — wood/stone/food piled up
-	# undelivered, and barracks construction never started.
-	#
-	# Uses the half-diagonal (not half-side) of a rectangular building so
-	# this is correct no matter which angle the citizen approaches from —
-	# a citizen walking in toward a corner can physically stop farther
-	# from the building's center than one walking straight at a face.
+	# collision physically stops it, plus a small margin. Uses the horizontal
+	# half-diagonal of a building's box so this is correct no matter which
+	# angle the citizen approaches from.
 	var target_half_size: float = 16.0  # sane default for a small/point target
-	if target is Building:
-		var shape_node = target.get_node_or_null("CollisionShape2D")
-		if shape_node and shape_node.shape is RectangleShape2D:
-			var size: Vector2 = shape_node.shape.size
-			target_half_size = size.length() * 0.5  # half-diagonal
-	else:
-		var shape_node = target.get_node_or_null("CollisionShape2D") if target.has_method("get_node_or_null") else null
-		if shape_node and shape_node.shape is CircleShape2D:
-			target_half_size = shape_node.shape.radius
+	var shape_node = target.get_node_or_null("CollisionShape3D")
+	if shape_node and shape_node.shape:
+		var sh = shape_node.shape
+		if sh is BoxShape3D:
+			# Footprint diagonal on the ground plane (ignore height).
+			target_half_size = Vector2(sh.size.x, sh.size.z).length() * 0.5
+		elif sh is CylinderShape3D:
+			target_half_size = sh.radius
+		elif sh is SphereShape3D:
+			target_half_size = sh.radius
+		elif sh is CapsuleShape3D:
+			target_half_size = sh.radius
 
 	return target_half_size + OWN_RADIUS + INTERACTION_MARGIN
 
@@ -175,6 +170,7 @@ func _try_autofind_job() -> bool:
 		{"group": "farms", "job": Job.FARMER, "resource": "food"},
 		{"group": "lumber_camps", "job": Job.WOODCUTTER, "resource": "wood"},
 		{"group": "quarries", "job": Job.MINER, "resource": "stone"},
+		{"group": "mines", "job": Job.MINER, "resource": "iron"},
 	]
 	for c in candidates:
 		for site in get_tree().get_nodes_in_group(c["group"]):
@@ -205,7 +201,7 @@ func _auto_work_job(delta: float) -> void:
 		return
 	_gather_timer = 0.0
 
-	var resource_type = _resource_for_job(current_job)
+	var resource_type = _carried_type_for(workplace)
 	var got = workplace.harvest(carry_capacity - carried_amount)
 	if got > 0:
 		carried_amount += got
@@ -228,9 +224,21 @@ func _resource_for_job(job: Job) -> String:
 		Job.FARMER: return "food"
 		Job.WOODCUTTER: return "wood"
 		Job.MINER: return "stone"
+		Job.WATERCARRIER: return "water"
 	return ""
 
 
+# What a citizen ends up carrying from a given worksite. World resource nodes
+# carry exactly what they are (so mining a mountain yields stone, not "");
+# resource buildings map through the citizen's current job.
+func _carried_type_for(node) -> String:
+	if "resource_type" in node:
+		return node.resource_type
+	if "yield_resource" in node and node.yield_resource != "":
+		return node.yield_resource
+	return _resource_for_job(current_job)
+
+
 func _auto_build(_delta: float) -> void:
 	var site = order_target if is_instance_valid(order_target) else null
 	if site == null:
@@ -257,22 +265,42 @@ func _auto_build(_delta: float) -> void:
 func command_gather(resource_node) -> void:
 	if is_delivering:
 		return  # finish dropping off the current load before taking a new job
+	var job := _job_for_target(resource_node)
+	if job == Job.NONE:
+		# e.g. an iron-ore or water node — not hand-gatherable (yet).
+		GameManager.notify("Citizens can't gather that directly.")
+		return
 	_leave_workplace()
+	# Resource *buildings* have worker slots; claim one so the building also
+	# starts producing. World nodes (trees, mountains) have no assign_worker.
+	if resource_node.has_method("assign_worker") and not resource_node.assign_worker(self):
+		GameManager.notify("That workplace is already full.")
+		return
 	has_player_order = true
 	order_kind = "gather"
 	order_target = resource_node
 	workplace = resource_node
-	current_job = Job.WOODCUTTER if resource_node.is_in_group("wood_sources") else _job_for_resource_building(resource_node)
+	current_job = job
 	_gather_timer = 0.0
 
 
-func _job_for_resource_building(node) -> Job:
+# Works out which job a gather target implies — whether it's a depletable
+# world node (reads resource_type) or a resource building (reads its group).
+# Returns Job.NONE for anything not hand-gatherable.
+func _job_for_target(node) -> Job:
+	if "resource_type" in node:
+		match node.resource_type:
+			"wood": return Job.WOODCUTTER
+			"stone", "iron": return Job.MINER
+			"food": return Job.FARMER
+			"water": return Job.WATERCARRIER
+		return Job.NONE
 	if node.is_in_group("farms"):
 		return Job.FARMER
-	if node.is_in_group("quarries"):
-		return Job.MINER
 	if node.is_in_group("lumber_camps"):
 		return Job.WOODCUTTER
+	if node.is_in_group("quarries") or node.is_in_group("mines"):
+		return Job.MINER
 	return Job.NONE
 
 
@@ -285,7 +313,7 @@ func command_build(site) -> void:
 	current_job = Job.BUILDER
 
 
-func command_move(pos: Vector2) -> void:
+func command_move(pos: Vector3) -> void:
 	_leave_workplace()
 	_abandon_delivery()
 	has_player_order = true
@@ -360,10 +388,12 @@ func _continue_delivery() -> void:
 		return
 	stop()
 	match carried_resource:
-		"food": GameManager.add_resources(carried_amount, 0, 0, 0)
-		"wood": GameManager.add_resources(0, carried_amount, 0, 0)
-		"stone": GameManager.add_resources(0, 0, carried_amount, 0)
-		"gold": GameManager.add_resources(0, 0, 0, carried_amount)
+		"food": GameManager.add_resources(carried_amount, 0, 0, 0, 0, 0)
+		"wood": GameManager.add_resources(0, carried_amount, 0, 0, 0, 0)
+		"stone": GameManager.add_resources(0, 0, carried_amount, 0, 0, 0)
+		"gold": GameManager.add_resources(0, 0, 0, carried_amount, 0, 0)
+		"iron": GameManager.add_resources(0, 0, 0, 0, carried_amount, 0)
+		"water": GameManager.add_resources(0, 0, 0, 0, 0, carried_amount)
 	carried_amount = 0
 	carried_resource = ""
 	is_delivering = false
@@ -403,26 +433,42 @@ func die(cause: String = "unknown") -> void:
 # ---------------------------------------------------------------------------
 # Appearance
 # ---------------------------------------------------------------------------
+func _set_mesh_color(node: Node, c: Color) -> void:
+	# Per-instance colour via material_override, so recolouring one citizen
+	# (by life stage / job / carried resource) doesn't tint all the others.
+	if node == null:
+		return
+	var mat := node.material_override
+	if not (mat is StandardMaterial3D):
+		mat = StandardMaterial3D.new()
+		node.material_override = mat
+	# Alpha 0 = hide the marker entirely (used for the "no job" icon).
+	if c.a < 1.0:
+		mat.transparency = BaseMaterial3D.TRANSPARENCY_ALPHA
+	mat.albedo_color = c
+
+
 func _refresh_appearance() -> void:
 	if sprite:
 		match life_stage:
 			LifeStage.CHILD:
-				sprite.color = Color(0.92, 0.74, 0.52)
-				sprite.scale = Vector2(0.6, 0.6)
+				_set_mesh_color(sprite, Color(0.92, 0.74, 0.52))
+				sprite.scale = Vector3(0.6, 0.6, 0.6)
 			LifeStage.ADULT:
-				sprite.color = Color(0.22, 0.52, 0.88)
-				sprite.scale = Vector2(1.0, 1.0)
+				_set_mesh_color(sprite, Color(0.22, 0.52, 0.88))
+				sprite.scale = Vector3.ONE
 			LifeStage.ELDER:
-				sprite.color = Color(0.55, 0.55, 0.6)
-				sprite.scale = Vector2(0.9, 0.9)
+				_set_mesh_color(sprite, Color(0.55, 0.55, 0.6))
+				sprite.scale = Vector3(0.9, 0.9, 0.9)
 
 	if job_icon:
 		match current_job:
-			Job.FARMER: job_icon.color = Color(0.2, 0.8, 0.2)
-			Job.WOODCUTTER: job_icon.color = Color(0.5, 0.32, 0.1)
-			Job.MINER: job_icon.color = Color(0.55, 0.55, 0.55)
-			Job.BUILDER: job_icon.color = Color(0.9, 0.7, 0.2)
-			_: job_icon.color = Color(1, 1, 1, 0)
+			Job.FARMER: _set_mesh_color(job_icon, Color(0.2, 0.8, 0.2))
+			Job.WOODCUTTER: _set_mesh_color(job_icon, Color(0.5, 0.32, 0.1))
+			Job.MINER: _set_mesh_color(job_icon, Color(0.55, 0.55, 0.55))
+			Job.BUILDER: _set_mesh_color(job_icon, Color(0.9, 0.7, 0.2))
+			Job.WATERCARRIER: _set_mesh_color(job_icon, Color(0.3, 0.55, 0.85))
+			_: _set_mesh_color(job_icon, Color(1, 1, 1, 0))
 
 
 func _physics_process(delta: float) -> void:
@@ -430,7 +476,7 @@ func _physics_process(delta: float) -> void:
 	if carry_icon:
 		carry_icon.visible = carried_amount > 0
 		if carried_amount > 0:
-			carry_icon.color = _color_for_resource(carried_resource)
+			_set_mesh_color(carry_icon, _color_for_resource(carried_resource))
 
 
 func _color_for_resource(res: String) -> Color:
@@ -438,6 +484,8 @@ func _color_for_resource(res: String) -> Color:
 		"wood": return Color(0.45, 0.27, 0.1)
 		"stone": return Color(0.6, 0.6, 0.6)
 		"food": return Color(0.9, 0.75, 0.2)
+		"iron": return Color(0.36, 0.36, 0.42)
+		"water": return Color(0.3, 0.55, 0.85)
 	return Color(1, 1, 1)
 
 
@@ -448,6 +496,7 @@ func job_label() -> String:
 		Job.MINER: return "Miner"
 		Job.BUILDER: return "Builder"
 		Job.TRADER: return "Trader"
+		Job.WATERCARRIER: return "Water Carrier"
 	return "Idle"
 
 
diff --git a/scripts/citizen.gd.uid b/scripts/units/citizen.gd.uid
similarity index 100%
rename from scripts/citizen.gd.uid
rename to scripts/units/citizen.gd.uid
diff --git a/scripts/soldier.gd b/scripts/units/soldier.gd
similarity index 96%
rename from scripts/soldier.gd
rename to scripts/units/soldier.gd
index 18312c2..3b468ff 100644
--- a/scripts/soldier.gd
+++ b/scripts/units/soldier.gd
@@ -16,7 +16,7 @@ var attack_target = null
 var has_player_order: bool = false
 var _attack_timer: float = 0.0
 
-@onready var sprite: Node = get_node_or_null("Sprite2D")
+@onready var sprite: Node = get_node_or_null("Mesh")
 
 
 func _ready() -> void:
@@ -74,7 +74,7 @@ func _look_for_nearby_enemy() -> void:
 # ---------------------------------------------------------------------------
 # RTS commands
 # ---------------------------------------------------------------------------
-func command_move(pos: Vector2) -> void:
+func command_move(pos: Vector3) -> void:
 	has_player_order = true
 	attack_target = null
 	move_to(pos)
diff --git a/scripts/soldier.gd.uid b/scripts/units/soldier.gd.uid
similarity index 100%
rename from scripts/soldier.gd.uid
rename to scripts/units/soldier.gd.uid
diff --git a/scripts/unit.gd b/scripts/units/unit.gd
similarity index 66%
rename from scripts/unit.gd
rename to scripts/units/unit.gd
index 5162307..904af9c 100644
--- a/scripts/unit.gd
+++ b/scripts/units/unit.gd
@@ -1,11 +1,10 @@
 class_name Unit
-extends CharacterBody2D
+extends CharacterBody3D
 
 ## Shared base for every player/AI controlled unit (citizens AND soldiers).
-## Handles: movement, selection ring, health bar, basic combat resolution.
-## Subclasses override `_on_command_move/_on_command_attack/_on_command_gather`
-## hooks rather than re-implementing input handling, so the camera controller
-## (RTS input layer) can treat every unit uniformly.
+## 3D port: units live on the ground plane (XZ); Y is height and stays 0.
+## Movement, selection ring, health, and basic combat all resolve here so the
+## camera/input layer can treat every unit uniformly.
 
 signal died(unit)
 
@@ -17,12 +16,12 @@ var health: int
 var selected: bool = false
 var is_alive: bool = true
 
-var target_position: Vector2 = Vector2.ZERO
+var target_position: Vector3 = Vector3.ZERO
 var is_moving: bool = false
 var move_arrival_radius: float = 6.0
 
 @onready var selection_ring: Node = get_node_or_null("SelectionRing")
-@onready var health_bar: ProgressBar = get_node_or_null("HealthBar")
+@onready var health_bar: Node = get_node_or_null("HealthBar")
 
 
 func _ready() -> void:
@@ -39,13 +38,20 @@ func _physics_process(delta: float) -> void:
 
 
 func _step_toward_target(_delta: float) -> void:
-	var to_target = target_position - global_position
-	var distance = to_target.length()
+	# Move only on the ground plane — ignore any Y component so units never
+	# try to fly toward a target that's at a different height.
+	var to_target := target_position - global_position
+	to_target.y = 0.0
+	var distance := to_target.length()
 	if distance > move_arrival_radius:
 		velocity = to_target.normalized() * speed
 		move_and_slide()
+		# Face the direction of travel (yaw only).
+		if velocity.length() > 0.01:
+			var yaw := atan2(velocity.x, velocity.z)
+			rotation.y = lerp_angle(rotation.y, yaw, 0.2)
 	else:
-		velocity = Vector2.ZERO
+		velocity = Vector3.ZERO
 		is_moving = false
 		_on_reached_target()
 
@@ -54,14 +60,14 @@ func _on_reached_target() -> void:
 	pass
 
 
-func move_to(pos: Vector2) -> void:
+func move_to(pos: Vector3) -> void:
 	target_position = pos
 	is_moving = true
 
 
 func stop() -> void:
 	is_moving = false
-	velocity = Vector2.ZERO
+	velocity = Vector3.ZERO
 
 
 # ---------------------------------------------------------------------------
@@ -79,11 +85,8 @@ func is_selectable_by_player() -> bool:
 
 # ---------------------------------------------------------------------------
 # RTS command hooks — every concrete unit implements the ones it cares about.
-# The camera/input controller calls these without knowing which subclass it
-# is talking to, which is what keeps multi-unit right-click orders working
-# uniformly across citizens and soldiers.
 # ---------------------------------------------------------------------------
-func command_move(pos: Vector2) -> void:
+func command_move(pos: Vector3) -> void:
 	stop_special_orders()
 	move_to(pos)
 
@@ -100,12 +103,12 @@ func command_build(_site) -> void:
 	pass  # overridden by Citizen
 
 
-func command_attack_position(_world_pos: Vector2) -> void:
-	pass  # overridden by Artillery — ground-targeted area attack
+func command_attack_position(_world_pos: Vector3) -> void:
+	pass  # overridden by Artillery
 
 
 func stop_special_orders() -> void:
-	pass  # subclasses clear job/target state here
+	pass
 
 
 # ---------------------------------------------------------------------------
@@ -127,8 +130,10 @@ func _on_attacked(_attacker) -> void:
 
 
 func _update_health_bar() -> void:
-	if health_bar:
-		health_bar.value = float(health) / float(max_health) * 100.0
+	# Health bars are shown in the HUD selection panel in the 3D build; if a
+	# unit scene provides its own 3D bar with set_ratio(), drive it here.
+	if health_bar and health_bar.has_method("set_ratio"):
+		health_bar.set_ratio(float(health) / float(max_health))
 
 
 func die(_cause: String = "unknown") -> void:
diff --git a/scripts/unit.gd.uid b/scripts/units/unit.gd.uid
similarity index 100%
rename from scripts/unit.gd.uid
rename to scripts/units/unit.gd.uid
diff --git a/scripts/worker.gd b/scripts/worker.gd
deleted file mode 100644
index b63244e..0000000
--- a/scripts/worker.gd
+++ /dev/null
@@ -1,229 +0,0 @@
-class_name Worker
-extends Unit
-
-enum Task { IDLE, MOVING_TO_RESOURCE, GATHERING_WOOD, GATHERING_FOOD, RETURNING, BUILDING }
-
-@export var gather_speed: float = 1.0
-@export var carry_capacity: int = 10
-@export var gather_range: float = 60.0
-@export var deliver_range: float = 75.0
-@export var chop_bob_height: float = 4.0
-@export var chop_bob_speed: float = 8.0
-
-var current_task_enum: Task = Task.IDLE
-var target_resource = null
-var target_building = null
-var carried_resource: String = ""
-var carried_amount: int = 0
-var gather_timer: float = 0.0
-var chop_time: float = 0.0
-var last_job_type: String = "wood"
-
-@onready var interact_range = $InteractRange
-@onready var sprite = $Sprite2D
-@onready var wood_icon: ColorRect = $WoodIcon
-
-var sprite_base_pos: Vector2
-
-func _ready():
-	super._ready()
-	if sprite:
-		sprite_base_pos = sprite.position
-	if wood_icon:
-		wood_icon.visible = false
-
-func _process(delta):
-	match current_task_enum:
-		Task.MOVING_TO_RESOURCE:
-			_approach_resource()
-		Task.GATHERING_WOOD, Task.GATHERING_FOOD:
-			_gather_resource(delta)
-		Task.RETURNING:
-			_approach_delivery()
-		Task.BUILDING:
-			_build_structure(delta)
-
-	_update_chop_animation(delta)
-	_update_carry_indicator()
-
-func _approach_resource():
-	if not is_instance_valid(target_resource):
-		_find_more_work()
-		return
-
-	if global_position.distance_to(target_resource.global_position) > gather_range:
-		move_to(target_resource.global_position)
-	else:
-		is_moving = false
-		current_task_enum = Task.GATHERING_WOOD if target_resource.is_in_group("wood_sources") else Task.GATHERING_FOOD
-		gather_timer = 0.0
-
-func _gather_resource(delta):
-	if not is_instance_valid(target_resource) or (target_resource.is_depleted() and not _is_standing_job(target_resource)):
-		if carried_amount > 0:
-			_begin_return()
-		else:
-			_find_more_work()
-		return
-
-	if global_position.distance_to(target_resource.global_position) > gather_range:
-		current_task_enum = Task.MOVING_TO_RESOURCE
-		return
-
-	is_moving = false
-	gather_timer += delta
-
-	if gather_timer >= 1.0 / gather_speed:
-		gather_timer = 0.0
-		var amount = min(carry_capacity - carried_amount, target_resource.gather(1))
-		carried_amount += amount
-		if amount > 0:
-			carried_resource = "wood" if target_resource.is_in_group("wood_sources") else "food"
-
-		if carried_amount >= carry_capacity:
-			_begin_return()
-		elif target_resource.is_depleted() and not _is_standing_job(target_resource):
-			_begin_return()
-
-func _is_standing_job(resource) -> bool:
-	# Farmsteads (and similar production buildings) are a standing post:
-	# a worker should wait there for more output rather than wander off.
-	return resource.is_in_group("farmsteads")
-
-func _begin_return():
-	var centers = get_tree().get_nodes_in_group("village_centers")
-	var nearest = null
-	var min_dist = INF
-
-	for center in centers:
-		if center.team == team and is_instance_valid(center):
-			var dist = global_position.distance_to(center.global_position)
-			if dist < min_dist:
-				min_dist = dist
-				nearest = center
-
-	if nearest == null:
-		# Nowhere to deliver to; just keep chopping where possible.
-		current_task_enum = Task.GATHERING_WOOD if carried_resource != "food" else Task.GATHERING_FOOD
-		return
-
-	target_building = nearest
-	current_task_enum = Task.RETURNING
-	move_to(nearest.global_position)
-
-func _approach_delivery():
-	if not is_instance_valid(target_building):
-		_find_more_work()
-		return
-
-	if global_position.distance_to(target_building.global_position) > deliver_range:
-		move_to(target_building.global_position)
-	else:
-		is_moving = false
-		_deliver_resources()
-
-func _deliver_resources():
-	if carried_amount > 0:
-		if carried_resource == "food":
-			GameManager.add_resources(carried_amount, 0)
-		else:
-			GameManager.add_resources(0, carried_amount)
-
-	carried_amount = 0
-	carried_resource = ""
-
-	# Keep the job: head back to the same job site if it's still good,
-	# otherwise look for more work of the same kind.
-	if is_instance_valid(target_resource) and (_is_standing_job(target_resource) or not target_resource.is_depleted()):
-		current_task_enum = Task.MOVING_TO_RESOURCE
-	else:
-		_find_more_work()
-
-func _find_more_work():
-	if last_job_type == "food":
-		_find_new_resource_or_idle("food_sources")
-	else:
-		_find_new_resource_or_idle("wood_sources")
-
-func _find_new_resource_or_idle(group_name: String):
-	var sources = get_tree().get_nodes_in_group(group_name)
-	var nearest = null
-	var min_dist = INF
-
-	for source in sources:
-		if is_instance_valid(source) and (not source.is_depleted() or _is_standing_job(source)):
-			var dist = global_position.distance_to(source.global_position)
-			if dist < min_dist:
-				min_dist = dist
-				nearest = source
-
-	if nearest:
-		target_resource = nearest
-		current_task_enum = Task.MOVING_TO_RESOURCE
-	else:
-		target_resource = null
-		current_task_enum = Task.IDLE
-		is_moving = false
-
-func _build_structure(delta):
-	if not is_instance_valid(target_building):
-		current_task_enum = Task.IDLE
-		return
-
-	if global_position.distance_to(target_building.global_position) > deliver_range:
-		move_to(target_building.global_position)
-		return
-
-	is_moving = false
-	target_building.build_progress += delta * 10
-
-	if target_building.build_progress >= target_building.build_time:
-		target_building.finish_building()
-		current_task_enum = Task.IDLE
-		target_building = null
-
-func _update_chop_animation(delta):
-	if not sprite:
-		return
-
-	if current_task_enum == Task.GATHERING_WOOD or current_task_enum == Task.GATHERING_FOOD:
-		chop_time += delta * chop_bob_speed
-		sprite.position = sprite_base_pos + Vector2(0, -abs(sin(chop_time)) * chop_bob_height)
-	else:
-		chop_time = 0.0
-		sprite.position = sprite_base_pos
-
-func _update_carry_indicator():
-	if not wood_icon:
-		return
-
-	if carried_amount > 0:
-		wood_icon.visible = true
-		wood_icon.color = Color(0.45, 0.27, 0.1, 1) if carried_resource == "wood" else Color(0.9, 0.75, 0.2, 1)
-		wood_icon.scale = Vector2.ONE * clamp(float(carried_amount) / carry_capacity, 0.4, 1.0)
-	else:
-		wood_icon.visible = false
-
-func assign_gather(resource):
-	target_resource = resource
-	current_task_enum = Task.MOVING_TO_RESOURCE
-	gather_timer = 0.0
-	last_job_type = "food" if resource.is_in_group("food_sources") else "wood"
-	
-func assign_return(building):
-	target_building = building
-	current_task_enum = Task.RETURNING
-
-func assign_build(building):
-	target_building = building
-	current_task_enum = Task.BUILDING
-
-func command_move(pos: Vector2):
-	# Player override: drop whatever job we were doing and just walk.
-	current_task_enum = Task.IDLE
-	target_resource = null
-	target_building = null
-	move_to(pos)
-
-func _on_input_event(_viewport, event, _shape_idx):
-	super._on_input_event(_viewport, event, _shape_idx)
diff --git a/scripts/worker.gd.uid b/scripts/worker.gd.uid
deleted file mode 100644
index eaf8623..0000000
--- a/scripts/worker.gd.uid
+++ /dev/null
@@ -1 +0,0 @@
-uid://erm44303n4t6
diff --git a/scripts/map_generator.gd b/scripts/world/map_generator.gd
similarity index 69%
rename from scripts/map_generator.gd
rename to scripts/world/map_generator.gd
index d982ef4..2719dd3 100644
--- a/scripts/map_generator.gd
+++ b/scripts/world/map_generator.gd
@@ -14,10 +14,9 @@ extends Node
 # ---------------------------------------------------------------------------
 # Scenes
 # ---------------------------------------------------------------------------
-const TREE_SCENE     := "res://scenes/tree.tscn"
-const MOUNTAIN_SCENE := "res://scenes/mountain.tscn"
-const IRON_SCENE     := "res://scenes/iron_ore.tscn"
-const WATER_SCENE    := "res://scenes/water_source.tscn"
+const TREE_SCENE     := "res://scenes/world/tree.tscn"
+const MOUNTAIN_SCENE := "res://scenes/world/mountain.tscn"
+const WATER_SCENE    := "res://scenes/world/water_source.tscn"
 
 ## Clear radius around the village spawn so the start area isn't cluttered.
 const SPAWN_SAFE_RADIUS := 220.0
@@ -31,20 +30,18 @@ const TREES_PER_CLUSTER_MAX   := 14
 const FOREST_SPREAD           := 90.0
 
 # ---------------------------------------------------------------------------
-# Mountains (stone) — each is one ResourceNode the quarry can be built on
+# Mountains — solid terrain ranges. Every mountain tile holds stone; a fraction
+# of them also hold iron. Quarries (stone) and mines (iron) are built on them.
 # ---------------------------------------------------------------------------
-const MOUNTAIN_CLUSTERS_PER_MPX := 5.0
-const MOUNTAINS_PER_CLUSTER_MIN := 2
-const MOUNTAINS_PER_CLUSTER_MAX := 5
-const MOUNTAIN_SPREAD           := 80.0
-
-# ---------------------------------------------------------------------------
-# Iron ore — sparser than stone
-# ---------------------------------------------------------------------------
-const IRON_CLUSTERS_PER_MPX := 3.0
-const IRON_PER_CLUSTER_MIN  := 1
-const IRON_PER_CLUSTER_MAX  := 3
-const IRON_SPREAD           := 60.0
+const MOUNTAIN_RANGES_PER_MPX := 4.0    # mountain ranges per 1,000,000 px²
+const MOUNTAINS_PER_RANGE_MIN := 6
+const MOUNTAINS_PER_RANGE_MAX := 16
+const MOUNTAIN_SPREAD         := 66.0   # tile spacing within a range (tight = connected ridge)
+const MOUNTAIN_STONE_MIN      := 400
+const MOUNTAIN_STONE_MAX      := 900
+const IRON_MOUNTAIN_CHANCE    := 0.35   # chance a given mountain tile also bears iron
+const MOUNTAIN_IRON_MIN       := 150
+const MOUNTAIN_IRON_MAX       := 450
 
 # ---------------------------------------------------------------------------
 # Water (rivers = chains of water ResourceNodes + a decorative line)
@@ -73,8 +70,7 @@ func _ready() -> void:
 	_resize_ground(size)
 	_recenter_start(size)
 	_generate_rivers()
-	_generate_mountains()
-	_generate_iron()
+	_generate_mountain_ranges()
 	_generate_forests()
 
 
@@ -86,8 +82,10 @@ func _resize_ground(size: Vector2) -> void:
 	if ground == null:
 		push_warning("MapGenerator: no Ground node found — skipping resize.")
 		return
-	ground.position = Vector2.ZERO
-	ground.size     = size
+	if ground.mesh is PlaneMesh:
+		ground.mesh.size = size
+	# PlaneMesh is centred on its origin, so move the ground to the map centre.
+	ground.position = Vector3(size.x * 0.5, 0.0, size.y * 0.5)
 
 
 # ---------------------------------------------------------------------------
@@ -101,48 +99,44 @@ func _recenter_start(size: Vector2) -> void:
 	# Shift everything by the delta between that and the new map centre.
 	var old_anchor := Vector2(640, 360)
 	var delta := _center - old_anchor
+	var delta3 := Vector3(delta.x, 0.0, delta.y)
 
 	var buildings := scene.get_node_or_null("Buildings")
 	if buildings:
 		for b in buildings.get_children():
-			b.position += delta
+			b.position += delta3
 
 	var units := scene.get_node_or_null("Units")
 	if units:
 		for u in units.get_children():
-			u.position += delta
+			u.position += delta3
+
+	# The hand-placed starting trees live under Resources and were authored
+	# around the old anchor too. Procedural resources are added *after* this
+	# runs (in absolute map coords), so at this point Resources only holds the
+	# starter nodes — shift them so the player still spawns next to wood.
+	var resources := scene.get_node_or_null("Resources")
+	if resources:
+		for r in resources.get_children():
+			r.position += delta3
 
 	var cam := scene.get_node_or_null("Camera")
 	if cam:
-		cam.position = _center
+		cam.position = Vector3(_center.x, 0.0, _center.y)
 
 
 # ---------------------------------------------------------------------------
 # Rivers — a decorative Line2D plus gatherable water ResourceNodes along it
 # ---------------------------------------------------------------------------
 func _generate_rivers() -> void:
-	var env := _get_or_create("Environment")
 	var resources := _get_or_create("Resources")
 	var water_scene := load(WATER_SCENE)
+	if water_scene == null:
+		return
 	var count := _rng.randi_range(RIVER_COUNT_MIN, RIVER_COUNT_MAX)
 
 	for _i in count:
 		var points := _river_path()
-
-		# Decorative line under the nodes.
-		var line := Line2D.new()
-		line.width          = RIVER_LINE_WIDTH
-		line.default_color  = RIVER_COLOR
-		line.joint_mode     = Line2D.LINE_JOINT_ROUND
-		line.begin_cap_mode = Line2D.LINE_CAP_ROUND
-		line.end_cap_mode   = Line2D.LINE_CAP_ROUND
-		for p in points:
-			line.add_point(p)
-		env.add_child(line)
-
-		# Gatherable water nodes spaced along the river.
-		if water_scene == null:
-			continue
 		var dist_acc := 0.0
 		for i in range(1, points.size()):
 			var seg :Vector2 = points[i] - points[i - 1]
@@ -153,9 +147,9 @@ func _generate_rivers() -> void:
 				var pos: Vector2 = points[i]
 				if pos.distance_to(_center) < SPAWN_SAFE_RADIUS:
 					continue
-				var water: Node2D = water_scene.instantiate()
-				water.global_position = pos
+				var water: Node3D = water_scene.instantiate()
 				resources.add_child(water)
+				water.global_position = _to3(pos)
 
 
 func _river_path() -> Array:
@@ -191,25 +185,45 @@ func _river_path() -> Array:
 
 
 # ---------------------------------------------------------------------------
-# Mountains (stone deposits)
+# Mountain ranges — elongated ridges of solid mountain tiles. Every tile holds
+# stone; a fraction also hold iron (shown by a vein marker). Quarries and mines
+# are built directly on these.
 # ---------------------------------------------------------------------------
-func _generate_mountains() -> void:
-	_scatter_clusters(
-		MOUNTAIN_SCENE, MOUNTAIN_CLUSTERS_PER_MPX,
-		MOUNTAINS_PER_CLUSTER_MIN, MOUNTAINS_PER_CLUSTER_MAX,
-		MOUNTAIN_SPREAD, SPAWN_SAFE_RADIUS * 1.3
-	)
+func _generate_mountain_ranges() -> void:
+	var resources := _get_or_create("Resources")
+	var scene := load(MOUNTAIN_SCENE)
+	if scene == null:
+		push_warning("MapGenerator: could not load " + MOUNTAIN_SCENE)
+		return
 
+	var area_mpx := (_map_rect.size.x * _map_rect.size.y) / 1_000_000.0
+	var ranges := max(int(round(area_mpx * MOUNTAIN_RANGES_PER_MPX)), 1)
+	var safe_radius := SPAWN_SAFE_RADIUS * 1.3
 
-# ---------------------------------------------------------------------------
-# Iron ore deposits
-# ---------------------------------------------------------------------------
-func _generate_iron() -> void:
-	_scatter_clusters(
-		IRON_SCENE, IRON_CLUSTERS_PER_MPX,
-		IRON_PER_CLUSTER_MIN, IRON_PER_CLUSTER_MAX,
-		IRON_SPREAD, SPAWN_SAFE_RADIUS * 1.5
-	)
+	for _r in ranges:
+		var centre := _random_map_point(safe_radius)
+		# Give each range a random orientation so it reads as a ridge line
+		# rather than a circular blob.
+		var dir := Vector2.RIGHT.rotated(_rng.randf() * TAU)
+		var perp := dir.orthogonal()
+		var count := _rng.randi_range(MOUNTAINS_PER_RANGE_MIN, MOUNTAINS_PER_RANGE_MAX)
+
+		for _n in count:
+			var along := _rng.randf_range(-1.0, 1.0)
+			var across := _rng.randf_range(-0.45, 0.45)
+			var pos: Vector2 = centre + dir * along * MOUNTAIN_SPREAD * 2.4 + perp * across * MOUNTAIN_SPREAD
+			if not _map_rect.has_point(pos):
+				continue
+			if pos.distance_to(_center) < safe_radius:
+				continue
+			var m: Node3D = scene.instantiate()
+			resources.add_child(m)
+			m.global_position = _to3(pos)
+			m.stone_amount = _rng.randi_range(MOUNTAIN_STONE_MIN, MOUNTAIN_STONE_MAX)
+			if _rng.randf() < IRON_MOUNTAIN_CHANCE:
+				m.iron_amount = _rng.randi_range(MOUNTAIN_IRON_MIN, MOUNTAIN_IRON_MAX)
+			else:
+				m.iron_amount = 0
 
 
 # ---------------------------------------------------------------------------
@@ -254,9 +268,9 @@ func _scatter_clusters(
 				continue
 			if pos.distance_to(_center) < safe_radius:
 				continue
-			var node: Node2D = scene.instantiate()
-			node.global_position = pos
+			var node: Node3D = scene.instantiate()
 			resources.add_child(node)
+			node.global_position = _to3(pos)
 
 
 # ---------------------------------------------------------------------------
@@ -275,11 +289,15 @@ func _random_map_point(min_dist_from_center: float = 0.0) -> Vector2:
 	return _center + Vector2(min_dist_from_center, min_dist_from_center)
 
 
-func _get_or_create(node_name: String) -> Node2D:
+func _to3(v: Vector2, y: float = 0.0) -> Vector3:
+	return Vector3(v.x, y, v.y)
+
+
+func _get_or_create(node_name: String) -> Node3D:
 	var scene := get_tree().current_scene
 	var n := scene.get_node_or_null(node_name)
 	if n == null:
-		n = Node2D.new()
+		n = Node3D.new()
 		n.name = node_name
 		scene.add_child(n)
 	return n
diff --git a/scripts/map_generator.gd.uid b/scripts/world/map_generator.gd.uid
similarity index 100%
rename from scripts/map_generator.gd.uid
rename to scripts/world/map_generator.gd.uid
diff --git a/scripts/world/mountain.gd b/scripts/world/mountain.gd
new file mode 100644
index 0000000..941179e
--- /dev/null
+++ b/scripts/world/mountain.gd
@@ -0,0 +1,108 @@
+class_name Mountain
+extends StaticBody3D
+
+## A mountain is solid terrain that holds two independent mineral pools:
+## STONE (every mountain has some) and IRON (only some mountains do). You don't
+## gather a mountain by hand — instead you build a Quarry on it to extract its
+## stone, and/or a Mine on it to extract its iron. Because the pools are
+## separate, the same mountain can host both a quarry and a mine at once.
+##
+## A mountain advertises itself to deposit-backed buildings through groups:
+##   "stone_sources" while it still has stone, "iron_sources" while it has iron.
+## ResourceBuilding.bind_to_deposit() looks the mountain up by those groups and
+## ResourceBuilding._process() drains the matching pool via harvest().
+
+@export var stone_amount: int = 600
+@export var iron_amount: int = 0  # 0 = an ordinary mountain with no iron
+
+var max_stone: int
+var max_iron: int
+
+var selected: bool = false
+var _selection_ring: MeshInstance3D = null
+
+@onready var iron_vein: Node = get_node_or_null("IronVein")
+
+
+func _ready() -> void:
+	max_stone = stone_amount
+	max_iron = iron_amount
+	add_to_group("mountains")
+	_refresh_groups()
+	# The iron vein marker is only shown on iron-bearing mountains so the
+	# player can tell at a glance where to put a mine.
+	if iron_vein:
+		iron_vein.visible = iron_amount > 0
+
+
+## How much of a given resource ("stone" / "iron") this mountain still holds.
+func remaining(resource: String) -> int:
+	match resource:
+		"stone": return stone_amount
+		"iron": return iron_amount
+	return 0
+
+
+## Pull up to `requested` units of `resource` out of the mountain.
+func harvest(resource: String, requested: int) -> int:
+	var actual := 0
+	match resource:
+		"stone":
+			actual = mini(requested, stone_amount)
+			stone_amount -= actual
+		"iron":
+			actual = mini(requested, iron_amount)
+			iron_amount -= actual
+	_refresh_groups()
+	_refresh_visual()
+	return actual
+
+
+## With no argument: true only when BOTH pools are empty. With a resource name:
+## true when that specific pool is empty (what a quarry/mine checks).
+func is_depleted(resource: String = "") -> bool:
+	match resource:
+		"stone": return stone_amount <= 0
+		"iron": return iron_amount <= 0
+	return stone_amount <= 0 and iron_amount <= 0
+
+
+func _refresh_groups() -> void:
+	_set_in_group("stone_sources", stone_amount > 0)
+	_set_in_group("iron_sources", iron_amount > 0)
+
+
+func _set_in_group(group_name: String, want: bool) -> void:
+	if want and not is_in_group(group_name):
+		add_to_group(group_name)
+	elif not want and is_in_group(group_name):
+		remove_from_group(group_name)
+
+
+func _refresh_visual() -> void:
+	if iron_vein:
+		iron_vein.visible = iron_amount > 0
+
+
+# ---------------------------------------------------------------------------
+# Selection (left-click) — terrain isn't commandable, but selecting it shows
+# how much stone/iron is left and reminds the player how to extract it.
+# ---------------------------------------------------------------------------
+func set_selected(value: bool) -> void:
+	selected = value
+	if value and _selection_ring == null:
+		_selection_ring = MeshInstance3D.new()
+		var disc := CylinderMesh.new()
+		disc.top_radius = 48.0
+		disc.bottom_radius = 48.0
+		disc.height = 0.5
+		_selection_ring.mesh = disc
+		_selection_ring.position.y = 0.3
+		var mat := StandardMaterial3D.new()
+		mat.shading_mode = BaseMaterial3D.SHADING_MODE_UNSHADED
+		mat.transparency = BaseMaterial3D.TRANSPARENCY_ALPHA
+		mat.albedo_color = Color(0, 1, 0, 0.5)
+		_selection_ring.material_override = mat
+		add_child(_selection_ring)
+	if _selection_ring:
+		_selection_ring.visible = value
diff --git a/scripts/world/resource_node.gd b/scripts/world/resource_node.gd
new file mode 100644
index 0000000..93258a8
--- /dev/null
+++ b/scripts/world/resource_node.gd
@@ -0,0 +1,80 @@
+class_name ResourceNode
+extends StaticBody3D
+
+## A depletable world resource (tree, water, ...) that citizens gather directly
+## via command_gather / the auto-job AI. Shrinks visually as it's consumed and
+## dims when empty. 3D port: lives on the ground plane.
+
+@export var resource_type: String = "wood"  # "wood", "food", "water"
+@export var amount: int = 120
+@export var resource_group: String = "wood_sources"
+
+var max_amount: int
+var selected: bool = false
+var _selection_ring: MeshInstance3D = null
+
+@onready var sprite: Node = get_node_or_null("Mesh")
+
+
+func _ready() -> void:
+	max_amount = amount
+	add_to_group("resources")
+	add_to_group(resource_group)
+
+
+func harvest(requested: int) -> int:
+	var actual = mini(requested, amount)
+	amount -= actual
+	_refresh_visual()
+	if amount <= 0:
+		_deplete()
+	return actual
+
+
+func is_depleted() -> bool:
+	return amount <= 0
+
+
+func set_selected(value: bool) -> void:
+	selected = value
+	if value and _selection_ring == null:
+		_selection_ring = _make_ground_ring(28.0, Color(0, 1, 0, 0.5))
+		add_child(_selection_ring)
+	if _selection_ring:
+		_selection_ring.visible = value
+
+
+func _make_ground_ring(r: float, c: Color) -> MeshInstance3D:
+	var mi := MeshInstance3D.new()
+	var disc := CylinderMesh.new()
+	disc.top_radius = r
+	disc.bottom_radius = r
+	disc.height = 0.5
+	mi.mesh = disc
+	mi.position.y = 0.3
+	var mat := StandardMaterial3D.new()
+	mat.shading_mode = BaseMaterial3D.SHADING_MODE_UNSHADED
+	mat.transparency = BaseMaterial3D.TRANSPARENCY_ALPHA
+	mat.albedo_color = c
+	mi.material_override = mat
+	return mi
+
+
+func _refresh_visual() -> void:
+	if not sprite:
+		return
+	var ratio = float(amount) / float(max_axis_amount())
+	sprite.scale = Vector3.ONE * maxf(ratio, 0.35)
+
+
+func max_axis_amount() -> int:
+	return max_amount if max_amount > 0 else 1
+
+
+func _deplete() -> void:
+	remove_from_group("resources")
+	remove_from_group(resource_group)
+	if sprite and sprite is MeshInstance3D:
+		var mat := StandardMaterial3D.new()
+		mat.albedo_color = Color(0.45, 0.3, 0.15)
+		sprite.material_override = mat
diff --git a/scripts/resource_node.gd.uid b/scripts/world/resource_node.gd.uid
similarity index 100%
rename from scripts/resource_node.gd.uid
rename to scripts/world/resource_node.gd.uid
